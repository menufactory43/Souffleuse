---
phase: 01-foundation-hypothesis-validation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift
autonomous: true
requirements: [BUILDER-03, TEST-02]
tags: [swift, testing, swift-testing, prompt-builder, snapshot-tests]

must_haves:
  truths:
    - "Le PromptBuilder est testable en isolation (sans charger MLX) via un mock `WordCountTokenCounter`."
    - "L'assemblage du prompt est déterministe pour un (counter, budget, inputs) triple donné — 100 invocations produisent des BuiltPrompt Equatable-equal."
    - "L'invariant D-11 'never cuts mid-word' est testé : un slot `beforeCursor` head-truncated produit toujours une string qui commence à une frontière de mot."
    - "L'invariant D-11 'sentence-boundary preferred' est testé : quand une frontière de phrase rentre dans le budget, c'est elle qui est choisie."
    - "L'invariant D-04 'no cross-slot stealing' est testé : un slot qui dépasse son per-slot budget ne vole pas le budget d'un autre slot."
    - "Les slots vides ne contribuent pas au prompt assemblé (pas de blank lines superflues)."
    - "Le slot reserved `afterCursor` etc. existent dans l'enum mais ne sont pas remplis par `build(...)` (Phase 1)."
  artifacts:
    - path: "Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift"
      provides: "Suite Swift Testing avec ≥ 7 @Test fonctions + mock WordCountTokenCounter co-located"
      min_lines: 150
      contains: "@Test func builder"
  key_links:
    - from: "Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift"
      to: "Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift"
      via: "@testable import SouffleusePrompt"
      pattern: "@testable import SouffleusePrompt"
    - from: "Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift"
      to: "Souffleuse/Sources/SouffleusePrompt/TokenCounting.swift"
      via: "test-private `struct WordCountTokenCounter: TokenCounting`"
      pattern: "WordCountTokenCounter.*TokenCounting"
---

<objective>
Créer la suite de tests `PromptBuilderTests.swift` dans `Tests/SouffleuseTests/` qui exerce le PromptBuilder du plan 01-01 en isolation totale de MLX. Mock `TokenCounting` co-located dans le fichier de test (per pattern `MockOCRCaretLocator` dans `CaretResolverTests.swift`). Snapshot tests sur l'assemblage déterministe, tests de comportement d'eviction (D-04, D-11), test d'invariant never-cuts-mid-word.

Purpose: Satisfaire BUILDER-03 ("testable en isolation") et TEST-02 ("nouveaux tests pour PromptBuilder : budget allocation, assemblage déterministe"). Tests rapides, CI-friendly, indépendants du tokenizer MLX. Servent de garde-fou de non-régression pour le futur refactor Phase 2/3.

Output: ≥ 7 @Test functions Swift Testing, `swift test --filter PromptBuilderTests` exit code 0, aucune régression sur les 94 tests existants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-hypothesis-validation/01-CONTEXT.md
@.planning/phases/01-foundation-hypothesis-validation/01-RESEARCH.md
@.planning/phases/01-foundation-hypothesis-validation/01-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

<interfaces>
<!-- Analog interfaces extracted from existing tests. Executor uses these directly. -->

From Souffleuse/Tests/SouffleuseTests/ChunkSplitterTests.swift (Swift Testing analog for pure-function suite):
```swift
import Testing
@testable import SouffleuseTyping

@Test func chunkSplitsFirstWordWithTrailingSpace() {
    #expect(ChunkSplitter.nextChunk("Je m'appelle Gabriel", trailingSpace: true) == "Je ")
}

@Test func chunkSplitsFirstWordWithoutTrailingSpace() {
    #expect(ChunkSplitter.nextChunk("Je m'appelle Gabriel", trailingSpace: false) == "Je")
}
```

From Souffleuse/Tests/SouffleuseTests/CaretResolverTests.swift (mock-in-test-file analog for WordCountTokenCounter):
```swift
// MARK: - Test doubles

actor MockOCRCaretLocator: OCRCaretLocating {
    private(set) var callCount: Int = 0
    var nextResult: OCRCaretResult? = nil
    // ...
}
```
NB: The TokenCounting protocol is sync (not async like OCRCaretLocating), so the mock is a `struct`, not `actor`.

From Souffleuse/Sources/SouffleusePrompt/* (built by plan 01-01):
```swift
public protocol TokenCounting: Sendable {
    func countTokens(_ text: String) -> Int
    func truncateHead(_ text: String, toBudget budget: Int) -> String
}

public struct PromptBuilder: Sendable {
    public init(counter: TokenCounting, budget: PromptBudget = .phase1Default)
    public func build(
        system: String,
        customInstructions: String,
        contextPrefix: String,
        fewShot: String,
        beforeCursor: String
    ) -> BuiltPrompt
}

public struct BuiltPrompt: Sendable, Equatable {
    public let text: String
    public let slotTexts: [PromptSlot: String]
    public let slotTokenCounts: [PromptSlot: Int]
    public let truncatedSlots: Set<PromptSlot>
    public let totalTokens: Int
}

public struct PromptBudget: Sendable, Equatable {
    public init(global: Int, perSlot: [PromptSlot: Int])
    public static let phase1Default: PromptBudget
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Créer PromptBuilderTests.swift avec mock + ≥ 7 @Test functions</name>
  <files>Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift</files>
  <read_first>
    - Souffleuse/Tests/SouffleuseTests/ChunkSplitterTests.swift (Swift Testing analog, pattern @Test func sans préfixe test_)
    - Souffleuse/Tests/SouffleuseTests/CaretResolverTests.swift (analog mock co-located avec `// MARK: - Test doubles`)
    - Souffleuse/Tests/SouffleuseTests/SimilarHistoryRetrievalTests.swift (analog helpers + @Test pour structs Sendable)
    - Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift (l'objet sous test, créé par plan 01-01)
    - Souffleuse/Sources/SouffleusePrompt/TokenCounting.swift (protocol à mocker)
    - Souffleuse/Sources/SouffleusePrompt/BuiltPrompt.swift (struct dont on assert Equatable)
    - Souffleuse/Sources/SouffleusePrompt/PromptBudget.swift (config + `.phase1Default`)
    - Souffleuse/Sources/SouffleusePrompt/PromptSlot.swift (enum cases pour `truncatedSlots.contains(.beforeCursor)` etc.)
    - .planning/phases/01-foundation-hypothesis-validation/01-RESEARCH.md §8 (test strategy + WordCountTokenCounter spec)
    - .planning/phases/01-foundation-hypothesis-validation/01-PATTERNS.md §PromptBuilderTests.swift (pattern assignment)
    - .planning/codebase/TESTING.md (project test conventions)
  </read_first>
  <action>
Créer `Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift`. Le fichier contient :

**A. Imports (CONVENTIONS.md §Import Organization)** :
```swift
import Testing
import Foundation
@testable import SouffleusePrompt
```
NB : `import Testing` (Swift Testing, pas XCTest) — précédent verrouillé par `ChunkSplitterTests.swift:1-2`.

**B. Section `// MARK: - Test doubles`** (per pattern CaretResolverTests.swift) avec deux mocks :

```swift
// MARK: - Test doubles

/// Deterministic mock: counts tokens as whitespace-separated words.
/// Truncation: drop words from the head until count ≤ budget.
/// Word-count proxy is enough to exercise the builder's eviction logic without
/// loading MLX. Real tokenizer behavior is exercised by the replay harness
/// (plan 01-04) and validated via human eyeball, not snapshot equality (R3).
struct WordCountTokenCounter: TokenCounting {
    func countTokens(_ text: String) -> Int {
        text.split(whereSeparator: { $0.isWhitespace }).count
    }

    func truncateHead(_ text: String, toBudget budget: Int) -> String {
        guard budget >= 1 else { return "" }
        let words = text.split(whereSeparator: { $0.isWhitespace }).map(String.init)
        guard words.count > budget else { return text }
        // Drop words from head until ≤ budget. NEVER mid-word (we operate on
        // whole-word boundaries by construction).
        return words.suffix(budget).joined(separator: " ")
    }
}

/// Stricter mock that exercises sentence-boundary preference (D-11 step (a)).
/// When a sentence terminator (". " / "? " / "! " / "\n\n") sits within the
/// budget, head-truncate at the SMALLEST cut that lands on a sentence end.
/// Otherwise fall back to word-boundary truncation.
struct SentenceAwareTokenCounter: TokenCounting {
    func countTokens(_ text: String) -> Int {
        text.split(whereSeparator: { $0.isWhitespace }).count
    }

    func truncateHead(_ text: String, toBudget budget: Int) -> String {
        guard budget >= 1 else { return "" }
        let words = text.split(whereSeparator: { $0.isWhitespace }).map(String.init)
        guard words.count > budget else { return text }
        // Search for a sentence-terminator that, if we cut right after it,
        // leaves ≤ budget words. Prefer the smallest cut (= most retained).
        let terminators: [Character] = [".", "?", "!"]
        for cutWordIdx in 0..<(words.count - budget) {
            let lastChar = words[cutWordIdx].last
            if let lc = lastChar, terminators.contains(lc) {
                // Cut right after this word.
                let remaining = words.suffix(words.count - cutWordIdx - 1)
                if remaining.count <= budget {
                    return remaining.joined(separator: " ")
                }
            }
        }
        // Fallback: word boundary
        return words.suffix(budget).joined(separator: " ")
    }
}
```

**C. Section `// MARK: - Tests`** avec ≥ 7 `@Test func` fonctions :

```swift
// MARK: - Tests

@Test func builderAssemblesAllSlotsInOrder() {
    let counter = WordCountTokenCounter()
    let builder = PromptBuilder(counter: counter, budget: .phase1Default)
    let built = builder.build(
        system: "You are an inline autocomplete.",
        customInstructions: "Be concise.",
        contextPrefix: "App Slack window equipe.",
        fewShot: "Hello team how is it going",
        beforeCursor: "Bonjour, je voulais vous dire"
    )
    let expected = """
You are an inline autocomplete.

Be concise.

App Slack window equipe.

Hello team how is it going

Bonjour, je voulais vous dire
"""
    #expect(built.text == expected)
    #expect(built.truncatedSlots.isEmpty)
}

@Test func builderHandlesEmptySlotsWithoutBlankLines() {
    let counter = WordCountTokenCounter()
    let builder = PromptBuilder(counter: counter)
    let built = builder.build(
        system: "Sys",
        customInstructions: "",
        contextPrefix: "",
        fewShot: "",
        beforeCursor: "User text"
    )
    // Slots vides ne contribuent rien — pas de blank lines superflues
    #expect(built.text == "Sys\n\nUser text")
    #expect(built.slotTexts.count == 2)
    #expect(built.slotTexts[.system] == "Sys")
    #expect(built.slotTexts[.beforeCursor] == "User text")
}

@Test func builderTruncatesBeforeCursorAtWordBoundary() {
    let counter = WordCountTokenCounter()
    // beforeCursor budget = 3 words ; input = 5 words ; head-truncated to 3 last words
    let budget = PromptBudget(global: 100, perSlot: [.beforeCursor: 3])
    let builder = PromptBuilder(counter: counter, budget: budget)
    let text = "Salutations bienveillantes mon ami fidèle"
    let built = builder.build(
        system: "", customInstructions: "", contextPrefix: "", fewShot: "",
        beforeCursor: text
    )
    #expect(built.text == "mon ami fidèle")
    #expect(built.truncatedSlots.contains(.beforeCursor))
}

@Test func builderNeverCutsMidWord() {
    // Invariant D-11(c) : la sortie commence TOUJOURS par un caractère de mot,
    // jamais au milieu d'un mot. Avec WordCountTokenCounter, c'est garanti
    // par construction (split sur whitespace). Le test verrouille l'invariant
    // contre toute future regression.
    let counter = WordCountTokenCounter()
    let budget = PromptBudget(global: 100, perSlot: [.beforeCursor: 2])
    let builder = PromptBuilder(counter: counter, budget: budget)
    let text = "Premier deuxième troisième quatrième cinquième"
    let built = builder.build(
        system: "", customInstructions: "", contextPrefix: "", fewShot: "",
        beforeCursor: text
    )
    let first = built.text.first
    #expect(first != nil)
    #expect(first!.isLetter || first!.isNumber)
    #expect(built.text == "quatrième cinquième")
}

@Test func builderPrefersSentenceBoundaryOverWordBoundary() {
    // D-11 step (a) > step (b) : si une frontière de phrase rentre dans le
    // budget, c'est elle qui est choisie. SentenceAwareTokenCounter encode
    // cette préférence.
    let counter = SentenceAwareTokenCounter()
    let budget = PromptBudget(global: 100, perSlot: [.beforeCursor: 6])
    let builder = PromptBuilder(counter: counter, budget: budget)
    // 4 words + ". " + 5 words + ". " + 2 words = 11 words. Budget 6.
    // Sentence cut after word 4 ("here.") leaves 7 words — too many.
    // Sentence cut after word 9 ("here.") leaves 2 words — fits but over-trim.
    // Best: cut after word 4 si reste ≤ budget — non, 7 > 6.
    // Donc on cut après word 9 → "Third short." (2 mots), OU on prend
    // les 6 derniers mots via fallback word-boundary → "Second sentence here. Third short."
    // SentenceAware retient la dernière frontière phrase qui fit ≤ budget.
    let longTail = "First sentence is here. Second sentence is here. Third short."
    let built = builder.build(
        system: "", customInstructions: "", contextPrefix: "", fewShot: "",
        beforeCursor: longTail
    )
    // L'attente exacte dépend du comportement de SentenceAwareTokenCounter.
    // L'invariant testé: la sortie commence par un mot complet et est ≤ 6 words.
    let words = built.text.split(whereSeparator: { $0.isWhitespace })
    #expect(words.count <= 6)
    #expect(built.truncatedSlots.contains(.beforeCursor))
    // La sortie est un suffixe du longTail (preserves the tail per D-11)
    #expect(longTail.hasSuffix(built.text))
}

@Test func builderHonorsPerSlotBudgetsIndependently() {
    // D-04 : pas de cross-slot stealing. Si contextPrefix fits dans son budget
    // de 5 mais beforeCursor overflows son budget de 3, SEUL beforeCursor est
    // tronqué.
    let counter = WordCountTokenCounter()
    let budget = PromptBudget(
        global: 1000,
        perSlot: [
            .contextPrefix: 5,
            .beforeCursor: 3,
        ]
    )
    let builder = PromptBuilder(counter: counter, budget: budget)
    let built = builder.build(
        system: "",
        customInstructions: "",
        contextPrefix: "one two three four five",   // exactly 5 words, fits
        fewShot: "",
        beforeCursor: "alpha beta gamma delta epsilon"  // 5 words, overflows budget 3
    )
    #expect(!built.truncatedSlots.contains(.contextPrefix))
    #expect(built.truncatedSlots.contains(.beforeCursor))
    #expect(built.slotTexts[.contextPrefix] == "one two three four five")
    #expect(built.slotTexts[.beforeCursor] == "gamma delta epsilon")
}

@Test func builderIsDeterministic() {
    // 100 invocations sur les mêmes inputs doivent produire des BuiltPrompt
    // Equatable-equal. Catche les régressions de nondéterminisme (Set iter,
    // dictionary ordering, etc.).
    let counter = WordCountTokenCounter()
    let builder = PromptBuilder(counter: counter)
    let first = builder.build(
        system: "s", customInstructions: "c", contextPrefix: "p", fewShot: "f",
        beforeCursor: "b"
    )
    for _ in 0..<99 {
        let again = builder.build(
            system: "s", customInstructions: "c", contextPrefix: "p", fewShot: "f",
            beforeCursor: "b"
        )
        #expect(again == first)
    }
}

@Test func builderRecordsTokenCountsPerSlot() {
    // BuiltPrompt.slotTokenCounts doit être populé correctement post-eviction
    // et sa somme doit égaler totalTokens.
    let counter = WordCountTokenCounter()
    let builder = PromptBuilder(counter: counter)
    let built = builder.build(
        system: "one two",          // 2
        customInstructions: "",
        contextPrefix: "three",      // 1
        fewShot: "",
        beforeCursor: "four five six"  // 3
    )
    #expect(built.slotTokenCounts[.system] == 2)
    #expect(built.slotTokenCounts[.contextPrefix] == 1)
    #expect(built.slotTokenCounts[.beforeCursor] == 3)
    #expect(built.slotTokenCounts[.customInstructions] == nil)
    #expect(built.slotTokenCounts[.fewShot] == nil)
    #expect(built.totalTokens == 6)
}

@Test func builderReservedPhase2SlotsAreNotFilled() {
    // PromptSlot enum déclare 5 slots reserved Phase 2/3 (afterCursor,
    // fieldContext, previousUserInputs, clipboardContext, screenContext).
    // En Phase 1, `build(...)` n'expose pas de paramètres pour eux et
    // slotTexts ne doit jamais les contenir.
    let counter = WordCountTokenCounter()
    let builder = PromptBuilder(counter: counter)
    let built = builder.build(
        system: "s", customInstructions: "c", contextPrefix: "p", fewShot: "f",
        beforeCursor: "b"
    )
    let reservedSlots: [PromptSlot] = [
        .afterCursor, .fieldContext, .previousUserInputs,
        .clipboardContext, .screenContext,
    ]
    for slot in reservedSlots {
        #expect(built.slotTexts[slot] == nil)
        #expect(built.slotTokenCounts[slot] == nil)
        #expect(!built.truncatedSlots.contains(slot))
    }
}

@Test func builderHonorsGlobalCapViaEvictionPriority() {
    // Global cap fires : sum per-slot tokens > global. Selon evictionPriority
    // (fewShot first → customInstructions → contextPrefix → beforeCursor →
    // system), le slot fewShot est droppé en premier.
    let counter = WordCountTokenCounter()
    let budget = PromptBudget(
        global: 5,  // very tight
        perSlot: [
            .system: 10,
            .customInstructions: 10,
            .contextPrefix: 10,
            .fewShot: 10,
            .beforeCursor: 10,
        ]
    )
    let builder = PromptBuilder(counter: counter, budget: budget)
    let built = builder.build(
        system: "one two",         // 2
        customInstructions: "three",  // 1
        contextPrefix: "four",        // 1
        fewShot: "five six seven",    // 3
        beforeCursor: "eight"         // 1
    )
    // Total per-slot: 2+1+1+3+1 = 8 > 5 → eviction must kick in
    // fewShot droppé en premier (3 tokens) → reste 5, fits.
    #expect(built.totalTokens <= 5)
    #expect(built.truncatedSlots.contains(.fewShot))
    #expect(built.slotTexts[.fewShot] == nil)
    // Les autres slots restent intacts
    #expect(built.slotTexts[.system] == "one two")
    #expect(built.slotTexts[.beforeCursor] == "eight")
}
```

**Conventions à respecter** :
- File name = `PromptBuilderTests.swift` (mirror du `ChunkSplitterTests.swift` pattern)
- Pas de class XCTestCase. Top-level `@Test func`. Pas de `test_` prefix.
- `#expect(actual == expected)`, jamais `XCTAssert*`
- French inline comments OK (cohérent avec le projet)
- Mock co-located, pas dans un fichier `Mocks/` séparé (project precedent)

**Privacy invariants (T1, T3 du security gate)** :
- Les tests utilisent des strings inventées ("Bonjour", "Salutations", "alpha beta") — aucun user text réel
- Aucun read de history.aes, AX, clipboard, screen capture dans les tests
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/cocotypist/Souffleuse && test -f Tests/SouffleuseTests/PromptBuilderTests.swift && grep -c "^@Test func builder" Tests/SouffleuseTests/PromptBuilderTests.swift && grep -q "@testable import SouffleusePrompt" Tests/SouffleuseTests/PromptBuilderTests.swift && grep -q "struct WordCountTokenCounter: TokenCounting" Tests/SouffleuseTests/PromptBuilderTests.swift && swift test --filter PromptBuilderTests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `test -f Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift`
    - `grep -q "^import Testing" Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift`
    - `grep -q "@testable import SouffleusePrompt" Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift`
    - `grep -q "struct WordCountTokenCounter: TokenCounting" Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift`
    - `grep -c "^@Test func builder" Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift` retourne ≥ 7
    - Test names couvrent : `builderAssemblesAllSlotsInOrder`, `builderHandlesEmptySlotsWithoutBlankLines`, `builderTruncatesBeforeCursorAtWordBoundary`, `builderNeverCutsMidWord`, `builderHonorsPerSlotBudgetsIndependently`, `builderIsDeterministic`, `builderRecordsTokenCountsPerSlot`, `builderReservedPhase2SlotsAreNotFilled`, `builderHonorsGlobalCapViaEvictionPriority` (au moins 7 de la liste)
    - `cd Souffleuse && swift test --filter PromptBuilderTests` exit code 0
    - `cd Souffleuse && swift test --filter PromptBuilderTests` output contient `Test run with N tests passed` (N ≥ 7)
    - `cd Souffleuse && swift test` (full suite) exit code 0 — pas de régression sur les 94 tests existants
    - Aucun appel `print(`, `NSLog(`, `os_log(`, `Log\\.(info|warn|error)` dans le fichier de test (les tests sont scoped to test target, mais cohérence)
  </acceptance_criteria>
  <done>
    Suite de tests passe en exit code 0, BUILDER-03 et TEST-02 satisfaits via ≥ 7 snapshot/invariant tests sans dépendance MLX.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test target | Lit/écrit code in-process, n'expose pas de surface utilisateur. Strings de test inventées. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `PromptBuilderTests.swift` test inputs | accept | Les inputs sont des strings inventées ("Bonjour", "Salutations") — aucun user text réel, aucun read de history.aes/AX/clipboard/screen. T2 du security gate respecté par construction. |
| T-02-02 | Tampering | Test runner | accept | Tests purement synchrones sur value-types ; aucune surface mutable. Determinism testé explicitement (`builderIsDeterministic`). |
| T-02-03 | Repudiation | aucun | accept | Pas de logging dans les tests. |
</threat_model>

<verification>
- `swift test --filter PromptBuilderTests` exit code 0
- `swift test` (full suite) exit code 0 — ≥ 94 tests + nouveaux tests PromptBuilder, aucun échoué
- `@Test func` count ≥ 7 dans PromptBuilderTests.swift
- Mock `WordCountTokenCounter` est `struct` (PAS `actor` — protocol est sync, cohérent avec note dans PATTERNS.md)
- BUILDER-03 satisfait : builder testable en isolation de MLX (aucun import `MLXLLM`/`MLXLMCommon` requis dans le test)
- TEST-02 satisfait : tests couvrent assemblage déterministe, budget allocation, eviction policy, never-cuts-mid-word invariant
</verification>

<success_criteria>
- Suite de tests passe (exit code 0)
- 94 tests existants restent verts (TEST-01 conservé)
- Invariants D-04 (per-slot independence), D-11 (never mid-word, sentence-preferred), et determinism testés
- PromptBuilder validé en isolation totale de MLX, prêt pour le wiring d'intégration (plan 01-03)
- Couvre BUILDER-03 (testable en isolation) et TEST-02 (nouveaux tests PromptBuilder)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-hypothesis-validation/01-02-SUMMARY.md` documenting:
- Nombre de @Test functions ajoutées (≥ 7)
- Liste des invariants testés (D-04, D-11(a), D-11(c), determinism, etc.)
- Output `swift test --filter PromptBuilderTests` (test count, durée)
- Confirmation `swift test` full suite reste vert
- Si SentenceAwareTokenCounter mock a dû être ajusté (le comportement exact dépend de comment PromptBuilder appelle truncateHead) — documenter
</output>

---
phase: 01-foundation-hypothesis-validation
plan: 05
type: execute
wave: 4
depends_on: [01-03, 01-04]
files_modified:
  - .planning/phases/01-foundation-hypothesis-validation/REPLAY-RESULTS.md
  - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift
autonomous: false
requirements: [AUDIT-02, TEST-01]
tags: [verdict, cleanup, bench, audit, feature-flag-removal]

must_haves:
  truths:
    - "Le SouffleuseBench post-intégration est exécuté pour confirmer que TTFT du nouveau path reste dans l'enveloppe ~80ms (RESEARCH §4 baseline)."
    - "Le `--replay` est exécuté avec `SOUFFLEUSE_PROMPT_BUILDER=1` ; REPLAY-RESULTS.md généré avec 12 scénarios × 2 variantes (24 ghosts)."
    - "Le human verdict est appliqué (cochage ✓/=/✗ par scénario) dans REPLAY-RESULTS.md, et le tally est rempli."
    - "Si verdict ≥ 6/12 ✓ : le feature flag `SOUFFLEUSE_PROMPT_BUILDER` est retiré du code, le path legacy (basePreamble + basePromptText + llmTail) est supprimé, le PromptBuilder devient le seul path actif."
    - "Si verdict < 6/12 ✓ : ESCALER au user (checkpoint:decision). Ne PAS supprimer le legacy path. Marquer le milestone à revisiter avant Phase 2."
    - "Après cleanup, `swift test` retourne exit 0 avec ≥ 94 + PromptBuilderTests passés, `audit.sh` retourne exit 0."
  artifacts:
    - path: ".planning/phases/01-foundation-hypothesis-validation/REPLAY-RESULTS.md"
      provides: "12 sections scénarios remplies + tally bloc rempli + verdict global documenté"
      contains: "AUDIT-02 gate"
    - path: "Souffleuse/Sources/Souffleuse/PredictorViewModel.swift"
      provides: "(si verdict positif) feature flag retiré, legacy path supprimé"
  key_links:
    - from: ".planning/phases/01-foundation-hypothesis-validation/REPLAY-RESULTS.md"
      to: "verdict AUDIT-02"
      via: "tally count ≥ 6/12 = proceed to Phase 2"
      pattern: "AUDIT-02 gate"
---

<objective>
Phase 1 verdict + cleanup. Trois étapes séquentielles : (1) bench TTFT pour confirmer non-régression PERF avec le nouveau path activé, (2) exécution `--replay` + verdict humain dans REPLAY-RESULTS.md (AUDIT-02), (3) selon le verdict — si positif (≥ 6/12 ✓), cleanup du feature flag + suppression du path legacy ; si négatif, ESCALATION au user (pas de cleanup, milestone revisité avant Phase 2).

Purpose: Verrouiller AUDIT-02 (verdict explicite sur l'hypothèse fondatrice) et TEST-01 (94 tests verts + nouveaux tests PromptBuilder, post-cleanup). Ce plan est `autonomous: false` à cause de l'eyeball verdict (Task 2) qui requiert l'œil humain.

Output: REPLAY-RESULTS.md rempli ET (selon verdict) PredictorViewModel.swift sans feature flag.

**Décision Q9 (legacy removal)** : Per D-12 + RESEARCH Q9, le legacy path est retiré à la fin de Phase 1 si verdict positif. Revert via git si besoin.
**Décision Q1 (per-slot budgets)** : Valeurs RESEARCH §4 conservées (`system=80, customInstructions=40, contextPrefix=150, fewShot=80, beforeCursor=200, global=512`) — ajustement explicite uniquement si le bench Task 1 remonte une régression > 30ms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation-hypothesis-validation/01-CONTEXT.md
@.planning/phases/01-foundation-hypothesis-validation/01-RESEARCH.md
@.planning/phases/01-foundation-hypothesis-validation/01-03-SUMMARY.md
@.planning/phases/01-foundation-hypothesis-validation/01-04-SUMMARY.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bench TTFT (legacy vs builder path) + confirm budget non-régression</name>
  <files>(read-only / measurement)</files>
  <read_first>
    - .planning/STATE.md §"Performance Metrics" (baseline TTFT ~80ms commit 6ad70df)
    - .planning/phases/01-foundation-hypothesis-validation/01-RESEARCH.md §4 (TTFT consideration) + §11 R9 (perf risk)
    - Souffleuse/Sources/SouffleuseBench/Bench.swift (le bench existant — comprendre sa shape)
  </read_first>
  <action>
**Étape 0 — Inspection préalable du bench (W9)** :
Lire `Souffleuse/Sources/SouffleuseBench/Bench.swift` et confirmer s'il appelle `PredictorViewModel.predict()` ou s'il a son propre prompt path indépendant.
- **Si le bench appelle `PredictorViewModel.predict()`** : l'env var `SOUFFLEUSE_PROMPT_BUILDER=1` exercera le nouveau path comme attendu. Continuer Étapes A+B normalement.
- **Si le bench a son propre path** (le plus probable, vu sa nature TTFT-focused) : `SOUFFLEUSE_PROMPT_BUILDER=1` ne fera AUCUNE différence. Documenter ce fait dans le summary + sauter Étapes B/C, fallback : "TTFT du nouveau path sera mesuré subjectivement via daily-use post-cleanup. Bench formal reporté à PERF-01 Phase 2." (cohérent avec note existante ci-dessous).
- Le but de l'inspection est d'éviter de prétendre mesurer ce qui n'est pas mesuré (W9 du checker).

**Étape A — Bench legacy (baseline)** :
```bash
cd /Users/gabrielwaltio/cocotypist/Souffleuse
swift run SouffleuseBench 2>&1 | tee /tmp/souffleuse-bench-legacy.log
```
Extraire la valeur TTFT médian (look for "ttft" or "TTFT" lines in output). Noter dans summary.

**Étape B — Bench builder path activé** :
```bash
cd /Users/gabrielwaltio/cocotypist/Souffleuse
SOUFFLEUSE_PROMPT_BUILDER=1 swift run SouffleuseBench 2>&1 | tee /tmp/souffleuse-bench-builder.log
```
Extraire la valeur TTFT médian.

**Étape C — Comparaison** :
- Calculer `delta = ttft_builder - ttft_legacy`
- **Seuil acceptable** : delta ≤ +30 ms (per RESEARCH §4 estimation "~30-50 ms when prompt doubles", acceptable car milestone privilège qualité). 
- **Seuil escalation** : delta > +50 ms — STOP, créer un checkpoint:decision pour ajuster les budgets per-slot AVANT de continuer Task 2/3.
- **Seuil silent acceptable** : delta ≤ +30 ms — continuer Task 2 sans intervention.

**Note** : SouffleuseBench peut ne pas être instrumenté pour lire `SOUFFLEUSE_PROMPT_BUILDER` directement. Si la flag ne traverse pas (le bench utilise son propre prompt), documenter ce gap dans le summary et fall back sur : "TTFT du nouveau path sera mesuré subjectivement via daily-use post-cleanup. Bench formal reporté à PERF-01 Phase 2." Cette dégradation est acceptable per `<planner_authority_limits>` (manque d'information runtime — pas une raison de bloquer le verdict AUDIT-02).

**Action concrète** :
- Run les deux benches
- Capture le numérique TTFT
- Decision : si delta > +50ms, créer immédiatement un checkpoint:decision (voir checkpoint Task 1b ci-dessous) ; sinon continuer Task 2
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/cocotypist/Souffleuse && ls /tmp/souffleuse-bench-legacy.log /tmp/souffleuse-bench-builder.log 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Les deux logs `/tmp/souffleuse-bench-legacy.log` et `/tmp/souffleuse-bench-builder.log` existent et contiennent du contenu (≥ 100 bytes chacun)
    - Le TTFT médian a été extrait des deux logs OU une explication écrite documente pourquoi non-extractable (ex : bench n'utilise pas le predict path)
    - Décision documentée : delta ≤ +30ms = continue, delta > +50ms = escalation, entre les deux = continue avec note dans summary
  </acceptance_criteria>
  <done>
    Bench effectué, delta TTFT documenté, décision sur ajustement budget prise (par défaut : pas d'ajustement, budgets RESEARCH §4 conservés).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Exécuter --replay + eyeball verdict (12 scénarios)</name>
  <what-built>
    Le harness `--replay` (plan 01-04) + le PromptBuilder pipeline complet (plans 01-01 → 01-03) sont déjà en place. La task consiste à :
    1. Lancer `SOUFFLEUSE_PROMPT_BUILDER=1 swift run SouffleuseCoherence --replay .planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json` (le flag active le PromptBuilder côté prod aussi, mais le harness l'utilise directement de toute façon)
    2. Attendre la génération des 12 × 2 = 24 ghosts (~30-90s selon modèle)
    3. Ouvrir le `REPLAY-RESULTS.md` généré
    4. **Eyeball verdict** : pour CHAQUE scénario, lire les deux ghosts (WITHOUT context vs WITH context) et cocher la case appropriée (✓ ✗ ou =)
    5. Remplir le tally bloc final (counts ✓ / = / ✗)
    6. Documenter la décision globale en bas du fichier : `≥ 6/12 ✓` = proceed, sinon ESCALATE
  </what-built>
  <how-to-verify>
    **Étapes exactes** :
    
    1. **Lancer le replay** :
       ```bash
       cd /Users/gabrielwaltio/cocotypist/Souffleuse
       SOUFFLEUSE_PROMPT_BUILDER=1 swift run SouffleuseCoherence \
         --replay ../.planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json
       ```
       (Note path relatif depuis Souffleuse/ dir.)
    
    2. **Attendre completion** : stdout affiche `[N/12] <scenario-id>` au fur et à mesure, puis `→ écrit: .../REPLAY-RESULTS.md`.
    
    3. **Ouvrir** `.planning/phases/01-foundation-hypothesis-validation/REPLAY-RESULTS.md` dans ton éditeur.
    
    4. **Pour chaque scénario** (12 sections) :
       - Lire `userTail` (ce qui était en cours de saisie)
       - Lire `notes` (l'intention attendue du scénario)
       - Lire les deux ghosts : `WITHOUT context` vs `WITH context`
       - **Critère de vote** :
         - `✓ with-context better` : le ghost AVEC contexte est plus pertinent ou plus naturel que SANS
         - `= neutral` : les deux sont équivalents (même mauvais, même générique, même bon)
         - `✗ with-context worse` : le ghost AVEC contexte est moins pertinent (régression introduite par le contexte)
       - Cocher la case `[x]` dans la ligne **Verdict:** (remplacer le `[ ]` approprié par `[x]`)
       - Optionnel : ajouter une note dans **Human note:** (ex : "ghost avec contexte cite Carrefour comme attendu", ou "les deux sont vides")
    
    5. **Remplir le bloc Tally** :
       ```
       - ✓ with-context better: N / 12
       - = neutral:              M / 12
       - ✗ with-context worse:   P / 12
       ```
       (N + M + P = 12)
    
    6. **Documenter la décision** sous le bloc Tally :
       - Si `N ≥ 6` : ajouter `**Verdict global: HYPOTHÈSE CONFIRMÉE. Proceed to Task 3 (cleanup).**`
       - Si `N < 6` : ajouter `**Verdict global: HYPOTHÈSE NON CONFIRMÉE. STOP. Milestone à revisiter avant Phase 2.**` + brève note des raisons (ex : "modèle saturé peu importe le contexte", "contexte mal formaté", etc.)
    
    7. **Réponds avec "approved" + verdict count** (ex : "approved 8/12 ✓ — proceed cleanup") ou "blocked N/12 ✓ — escalation needed" + raison.
  </how-to-verify>
  <resume-signal>Type "approved N/M ✓" (with N being the ✓ count) ou "blocked N/M ✓ — <reason>"</resume-signal>
  <files>.planning/phases/01-foundation-hypothesis-validation/REPLAY-RESULTS.md</files>
  <read_first>
    - .planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json (les 12 scénarios à juger)
    - .planning/phases/01-foundation-hypothesis-validation/REPLAY-RESULTS.md (généré par le replay, à remplir)
    - .planning/phases/01-foundation-hypothesis-validation/01-RESEARCH.md §6 (format markdown attendu) + §12 Q2 (seuil 6/12)
  </read_first>
  <action>
Voir <how-to-verify> ci-dessus pour les 7 étapes exactes. Synthèse :

1. Lancer `SOUFFLEUSE_PROMPT_BUILDER=1 swift run SouffleuseCoherence --replay ../.planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json` depuis `Souffleuse/`.
2. Attendre les 24 ghosts (12 scénarios × 2 variantes).
3. Ouvrir `REPLAY-RESULTS.md` et eyeball chaque scénario.
4. Cocher `[x]` ✓ / = / ✗ par scénario selon le critère (avec-contexte plus pertinent qu'avec-contexte vide).
5. Remplir le tally bloc (N ✓ / M = / P ✗, N+M+P=12).
6. Documenter le verdict global : "HYPOTHÈSE CONFIRMÉE" (N≥6) ou "HYPOTHÈSE NON CONFIRMÉE" (N<6).
7. Répondre `approved N/12 ✓` ou `blocked N/12 ✓ — <raison>`.

Critère de vote :
- ✓ : ghost AVEC contexte plus pertinent que SANS
- = : équivalents (les deux bons / les deux mauvais / les deux génériques)
- ✗ : ghost AVEC contexte moins pertinent (régression introduite par le contexte)
  </action>
  <verify>
    <automated>grep -c '\[x\]' /Users/gabrielwaltio/cocotypist/.planning/phases/01-foundation-hypothesis-validation/REPLAY-RESULTS.md 2>/dev/null && grep -q 'HYPOTHÈSE' /Users/gabrielwaltio/cocotypist/.planning/phases/01-foundation-hypothesis-validation/REPLAY-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REPLAY-RESULTS.md existe et a été regénéré (timestamp Generated: récent)
    - Au moins 12 cases `[x]` cochées dans le fichier (une par scénario)
    - Le bloc Tally contient des nombres réels (pas `___ / 12`)
    - Le verdict global est écrit en clair (`HYPOTHÈSE CONFIRMÉE` ou `HYPOTHÈSE NON CONFIRMÉE`)
    - Le user a répondu avec `approved N/M ✓` ou `blocked N/M ✓ — <reason>`
  </acceptance_criteria>
  <done>
    Eyeball verdict effectué, REPLAY-RESULTS.md rempli, décision passe/escalate documentée et signalée à l'agent.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cleanup (si verdict positif) — retirer feature flag + legacy path</name>
  <files>Souffleuse/Sources/Souffleuse/PredictorViewModel.swift</files>
  <read_first>
    - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift (état post-plan 01-03 : PromptBuilderFlag enum + branche dispatch + legacy verbatim)
    - .planning/phases/01-foundation-hypothesis-validation/REPLAY-RESULTS.md (vérifier que le verdict positif est documenté)
    - .planning/phases/01-foundation-hypothesis-validation/01-RESEARCH.md §5 "Migration removal"
  </read_first>
  <action>
**Pré-condition critique** : Cette task n'est exécutée QUE si le user a répondu `approved N/12 ✓` avec `N ≥ 6` à la Task 2. Si Task 2 a renvoyé `blocked`, SKIP cette task entièrement, marquer la phase comme "Hypothèse non confirmée — milestone à revisiter" dans le summary, et STOP.

**Étapes du cleanup** (per RESEARCH §5 "Migration removal") :

**1. Supprimer le `private enum PromptBuilderFlag`** (déclaré au top du fichier près de `PredictDebug`).

**2. Supprimer la branche legacy dans le `container.perform` closure**. Le bloc actuel ressemble à :
```swift
if PromptBuilderFlag.enabled {
    // ── NEW PATH ── ...
} else {
    // ── LEGACY PATH ── ...
}
```
Remplacer par seulement le NEW PATH (sans le `if PromptBuilderFlag.enabled` wrapper). **CRITIQUE** : préserver l'intégralité du per-slot routing introduit au plan 01-03 (BUILDER-02). NE PAS revenir à la version pre-fix avec `customInstructions: ""` / `contextPrefix: ""` — sinon BUILDER-02 est cassé et Phase 2 hérite d'un PromptBuilder qui n'exerce jamais ses per-slot budgets en prod.

```swift
// PromptBuilder path (now sole path post-Phase-1 verdict).
// Per-slot routing (BUILDER-02) : customInstructions / contextPrefix /
// fewShot / beforeCursor flow chacun dans leur slot dédié avec leur
// budget tokens.
let counter = MLXTokenCounter(tokenizer: context.tokenizer)
let builder = PromptBuilder(counter: counter, budget: .phase1Default)
let baseSystem = Self.buildSystemPrompt(detectedLanguage: detectedLanguage)
let customInstr = customInstructions.trimmingCharacters(in: .whitespacesAndNewlines)
let ctxPrefix = contextPrefix.trimmingCharacters(in: .whitespacesAndNewlines)
let built = builder.build(
    system: baseSystem,
    customInstructions: customInstr,
    contextPrefix: ctxPrefix,
    fewShot: examplesBlock,
    beforeCursor: userTail
)
Log.info(.predictor, "prompt_built", count: built.totalTokens)

let promptTokens: [Int]
if isInstructModel {
    let userContent = built.slotTexts[.beforeCursor] ?? ""
    let systemContent = [
        built.slotTexts[.system],
        built.slotTexts[.customInstructions],
        built.slotTexts[.contextPrefix],
        built.slotTexts[.fewShot],
    ].compactMap { $0 }.filter { !$0.isEmpty }.joined(separator: "\n\n")
    let messages: [[String: String]] = [
        ["role": "system", "content": systemContent],
        ["role": "user", "content": userContent],
    ]
    if let templated = try? context.tokenizer.applyChatTemplate(messages: messages) {
        promptTokens = templated
    } else {
        promptTokens = context.tokenizer.encode(text: built.text)
    }
} else {
    promptTokens = context.tokenizer.encode(text: built.text)
}
```

**3. Supprimer le code legacy `basePreamble` assembly** (lignes ~506-513 originelles) :
```swift
// SUPPRIMER :
var parts: [String] = []
if !customInstructions.isEmpty { parts.append(...) }
if !contextPrefix.isEmpty { parts.append(...) }
let basePreamble = parts.isEmpty ? "" : parts.joined(separator: "\n\n") + "\n\n"
```

**3.bis. Supprimer aussi `systemMessage` legacy assembly** (lignes ~486-493 originelles, `systemParts` + `systemMessage = systemParts.joined(...)`). Le nouveau path utilise `baseSystem = Self.buildSystemPrompt(...)` directement et route customInstructions/contextPrefix dans leurs propres slots. Conserver UNIQUEMENT la ligne `let detectedLanguage = Self.detectLanguage(in: userTail)` qui sert au `baseSystem`.

**4. Supprimer `let llmTail = String(userTail.suffix(512))`** (ligne ~658 originelle).

**5. Supprimer `basePromptText` construction** (lignes ~659-664 originelles) :
```swift
// SUPPRIMER :
let basePromptText: String
if examplesBlock.isEmpty {
    basePromptText = basePreamble + llmTail
} else {
    basePromptText = basePreamble + examplesBlock + "\n\n" + llmTail
}
```

**6. Préserver** :
- `let detectedLanguage = Self.detectLanguage(in: userTail)` (utilisé par `baseSystem = Self.buildSystemPrompt(detectedLanguage:)`) — c'est la SEULE ligne à conserver de l'ancien bloc d'assemblage `systemMessage` (lignes 478-493 originelles). Tout le reste de ce bloc (`systemParts.append(...)`, `systemMessage = systemParts.joined(...)`) est supprimé en step 3.bis car `baseSystem` consomme directement le résultat de `buildSystemPrompt` et les pieces customInstructions/contextPrefix sont routées vers leurs propres slots du builder.
- `examplesBlock` retrieval (lignes 642-651) — encore utilisé comme `fewShot:` input du builder
- Toute la suite du Task body (params, stream, generation loop, onChunk handling, cache storage)
- `PredictDebug` env-gate (hors scope du cleanup, c'est un autre dev flag indépendant)

**7. Validation séquentielle (verify command)** :
```bash
swift build && bash ./audit.sh && swift test
```
Tous doivent exit 0. Si l'un échoue, REVERT cette task (git checkout) et marquer la phase comme "verdict positif mais cleanup échoué — debug nécessaire" dans summary.

**Privacy invariants** :
- `Log.info(.predictor, "prompt_built", count:)` est conservé (StaticString event, count-only — pas de regression du check 6 audit.sh)
- Aucun nouveau log call ajouté

**Si verdict négatif (skipped)** :
- Ne modifier AUCUN fichier
- Documenter dans summary: "Cleanup skipped — verdict was N/12 < 6 threshold. Feature flag and legacy path preserved for re-evaluation. ROADMAP Phase 2 doit être suspendue jusqu'à pivot du milestone."
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/cocotypist/Souffleuse && RESULTS=/Users/gabrielwaltio/cocotypist/.planning/phases/01-foundation-hypothesis-validation/REPLAY-RESULTS.md && if grep -q "HYPOTHÈSE CONFIRMÉE" "$RESULTS"; then echo "Verdict positive — cleanup ran — verifying clean state" && swift build 2>&1 | tail -5 && bash ./audit.sh && swift test 2>&1 | tail -10 && (grep -q "SOUFFLEUSE_PROMPT_BUILDER" Sources/Souffleuse/PredictorViewModel.swift && echo "FAIL: feature flag still present after positive verdict" && exit 1) || echo "Clean state OK"; elif grep -q "HYPOTHÈSE NON CONFIRMÉE" "$RESULTS"; then echo "Verdict negative — cleanup did NOT run — verifying legacy preserved" && grep -q "SOUFFLEUSE_PROMPT_BUILDER" Sources/Souffleuse/PredictorViewModel.swift || (echo "FAIL: feature flag should be preserved after negative verdict" && exit 1); echo "Legacy path preserved as expected"; else echo "FAIL: REPLAY-RESULTS.md verdict marker missing (neither CONFIRMÉE nor NON CONFIRMÉE found)" && exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - **Si Task 2 a retourné approved ≥ 6/12** :
      - `grep -q "private enum PromptBuilderFlag" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift ; [ $? -ne 0 ]` (flag retiré)
      - `grep -q "SOUFFLEUSE_PROMPT_BUILDER" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift ; [ $? -ne 0 ]` (env var ref retirée)
      - `grep -q "let basePromptText" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift ; [ $? -ne 0 ]` (legacy assembly retiré)
      - `grep -q "let llmTail = String(userTail.suffix(512))" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift ; [ $? -ne 0 ]` (legacy llmTail retiré)
      - `grep -q "let basePreamble" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift ; [ $? -ne 0 ]` (legacy basePreamble retiré)
      - `grep -q "PromptBuilder(counter:" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift` (PromptBuilder est toujours là)
      - `grep -q "MLXTokenCounter(tokenizer:" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
      - `grep -q 'Log\.info(\.predictor, "prompt_built"' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
      - **Per-slot routing préservé (BUILDER-02)** : `grep -q "customInstructions: customInstr" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
      - **Per-slot routing préservé (BUILDER-02)** : `grep -q "contextPrefix: ctxPrefix" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
      - **Per-slot routing préservé (BUILDER-02)** : NE PAS avoir `customInstructions: ""` ni `contextPrefix: ""` dans le NEW path : `! grep -E "(customInstructions|contextPrefix):\s*\"\"" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
      - `cd Souffleuse && swift build` exit code 0
      - `cd Souffleuse && bash ./audit.sh` exit code 0 (6 checks verts — particulièrement check 6 critique)
      - `cd Souffleuse && swift test` exit code 0 (94+ tests + PromptBuilderTests verts — TEST-01)
    - **Si Task 2 a retourné blocked < 6/12** :
      - Aucun changement de fichier source (Souffleuse/ inchangé git diff vide sauf pour REPLAY-RESULTS.md filled-in)
      - Summary documente l'escalation
  </acceptance_criteria>
  <done>
    Si verdict positif : feature flag retiré, legacy path supprimé, PromptBuilder est l'unique path, audit/test verts.
    Si verdict négatif : aucun changement code, escalation documentée dans summary, milestone à revisiter.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REPLAY-RESULTS.md (eyeball verdict file) | Human input via cochage `[x]`. Versionné git. |
| PredictorViewModel cleanup | Suppression de code legacy ; impacte directement production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Cleanup de PredictorViewModel | mitigate | Le cleanup est gated derrière le verdict humain (Task 2 checkpoint:human-verify). Si verdict < 6/12, aucun changement effectué. Si verdict ≥ 6/12, validation par `swift build + audit.sh + swift test` (tous doivent exit 0) avant de marquer done. Git revert disponible si problème post-cleanup. |
| T-05-02 | Information Disclosure | Verdict humain dans REPLAY-RESULTS.md | accept | Les ghosts loggés proviennent de scénarios fictifs (T2 du gate, satisfait au plan 01-04). Le verdict humain n'ajoute que des marqueurs ✓/=/✗ et notes optionnelles écrites par le développeur. |
| T-05-03 | Repudiation | Decision audit-02 | mitigate | REPLAY-RESULTS.md est versionné git ; le commit qui l'remplit + le commit qui retire/préserve le flag forment l'audit trail. Le summary du plan 01-05 documente le decision rationale. |
</threat_model>

<verification>
- Bench TTFT documenté (delta legacy vs builder, ou note d'impossibilité avec fallback documenté)
- REPLAY-RESULTS.md rempli (12 sections cochées + tally + verdict global)
- **Si verdict positif** : feature flag retiré, legacy path supprimé, `swift build` + `audit.sh` + `swift test` exit 0
- **Si verdict négatif** : code unchanged, escalation documentée, ROADMAP Phase 2 à suspendre
- TEST-01 préservé : `swift test` exit 0 avec ≥ 94 tests + PromptBuilderTests (~7+) verts
</verification>

<success_criteria>
- AUDIT-02 satisfait : verdict explicite documenté dans REPLAY-RESULTS.md (HYPOTHÈSE CONFIRMÉE ou NON)
- TEST-01 préservé : 94 tests + nouveaux PromptBuilderTests verts à la fin (qu'il y ait eu cleanup ou non)
- audit.sh reste vert sur les 6 checks
- Le path PromptBuilder est validé empiriquement avant Phase 2 (ou explicitement réfuté, ce qui est aussi un succès du milestone — l'objectif est de DÉCIDER, pas de réussir à tout prix)
- Phase 1 close : ROADMAP peut passer Phase 1 à `[x]` complete (ou bloquer Phase 2 si verdict négatif)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-hypothesis-validation/01-05-SUMMARY.md` documenting:
- TTFT bench results : legacy ms, builder ms, delta ms (ou note d'impossibilité de mesure)
- Décision sur ajustement budget per-slot (par défaut: aucun ajustement, valeurs RESEARCH §4 conservées)
- Verdict count brut : ✓ N/12, = M/12, ✗ P/12
- Verdict global : HYPOTHÈSE CONFIRMÉE ou NON CONFIRMÉE
- Si CONFIRMÉE : confirmation du cleanup effectué (flag retiré, legacy retiré, audit + test verts post-cleanup), recommandation : ROADMAP Phase 1 → `[x]`, Phase 2 peut démarrer
- Si NON CONFIRMÉE : raisons documentées, recommandation d'action (re-research, pivot milestone, ajuster scénarios) ; ROADMAP Phase 2 doit rester bloquée jusqu'à pivot
- Liste des artefacts produits durant la phase complete (5 plans, 5 fichiers SouffleusePrompt, MLXTokenCounter, PromptBuilderTests, replay-scenarios.json, REPLAY-RESULTS.md, audit.sh update, Package.swift update, PredictorViewModel integration)
</output>

---
phase: 01-foundation-hypothesis-validation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift
  - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift
autonomous: true
requirements: [BUILDER-04]
tags: [swift, mlx, integration, feature-flag, predictor]

must_haves:
  truths:
    - "L'adaptateur production `MLXTokenCounter` wrappe le tokenizer MLX (`any Tokenizer` de MLXLMCommon) derrière le protocol `TokenCounting`."
    - "Le PromptBuilder est appelé depuis `PredictorViewModel.predict()` quand l'env var `SOUFFLEUSE_PROMPT_BUILDER=1` est set, dans le `container.perform` closure où le tokenizer est reachable."
    - "Quand l'env var n'est pas set, le path legacy flat-string (lignes 478-513 + 632-664 verbatim) continue de fonctionner sans changement observable."
    - "Les DEUX paths du predict (instruct via `applyChatTemplate` et base/PT via raw `encode`) sont wired derrière le feature flag."
    - "L'env var `SOUFFLEUSE_PROMPT_BUILDER` est detectée via `ProcessInfo.processInfo.environment`, jamais loggée (T3)."
    - "`Log.info(.predictor, \"prompt_built\", count: built.totalTokens)` est le SEUL événement émis (count-only, StaticString event)."
    - "Aucun appel `Log.*` interpole `built.text`, `built.slotTexts[.beforeCursor]`, ou tout autre slot output (T1)."
  artifacts:
    - path: "Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift"
      provides: "struct MLXTokenCounter: TokenCounting wrappant `any Tokenizer` MLX"
      contains: "struct MLXTokenCounter"
    - path: "Souffleuse/Sources/Souffleuse/PredictorViewModel.swift"
      provides: "private enum PromptBuilderFlag + branche `if PromptBuilderFlag.enabled` dans predict()'s container.perform"
      contains: "PromptBuilderFlag"
  key_links:
    - from: "Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift"
      to: "Souffleuse/Sources/SouffleusePrompt/TokenCounting.swift"
      via: "conformance `MLXTokenCounter: TokenCounting`"
      pattern: "MLXTokenCounter.*:.*TokenCounting"
    - from: "Souffleuse/Sources/Souffleuse/PredictorViewModel.swift"
      to: "Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift"
      via: "instanciation `PromptBuilder(counter:budget:)` dans container.perform"
      pattern: "PromptBuilder\\(counter:"
    - from: "Souffleuse/Sources/Souffleuse/PredictorViewModel.swift"
      to: "ProcessInfo env SOUFFLEUSE_PROMPT_BUILDER"
      via: "feature flag detection"
      pattern: "SOUFFLEUSE_PROMPT_BUILDER"
---

<objective>
Wirer la production : créer l'adaptateur `MLXTokenCounter` (file Souffleuse app target) qui conforme `TokenCounting` au tokenizer MLX, et intégrer le `PromptBuilder` dans `PredictorViewModel.predict()` derrière le feature flag env var `SOUFFLEUSE_PROMPT_BUILDER=1` (D-12). Le path legacy flat-string reste VERBATIM en place — coexistence parallèle pendant la construction (BUILDER-04).

Purpose: Satisfaire BUILDER-04 (pipeline existante continue de fonctionner sans régression pendant la construction). Le flag par défaut OFF garantit que les 94 tests existants restent verts (TEST-01) sans modification. Quand l'utilisateur lance avec `SOUFFLEUSE_PROMPT_BUILDER=1`, le nouveau path est exercé en daily-use et est exerçable par le replay harness (plan 01-04).

Output: Predict path dual (legacy + builder), feature-flag gate au call site, MLXTokenCounter compilable et utilisé dans container.perform.

**Décision migration strategy (per D-12, D-13)** : Feature flag dev-only (default). Justification : le PromptBuilder est un nouveau capability (target SPM SouffleusePrompt avec API surface neuve), pas un refactor cosmétique du code existant. Le flag permet revert instantané si bug pernicieux apparaît + permet au replay harness de basculer entre les deux paths. Le flag sera retiré au plan 01-05 après verdict replay positif.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation-hypothesis-validation/01-CONTEXT.md
@.planning/phases/01-foundation-hypothesis-validation/01-RESEARCH.md
@.planning/phases/01-foundation-hypothesis-validation/01-PATTERNS.md
@.planning/phases/01-foundation-hypothesis-validation/01-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md

<interfaces>
<!-- Interfaces from plan 01-01 + existing PredictorViewModel + MLXLMCommon -->

From Souffleuse/Sources/SouffleusePrompt/* (built by plan 01-01):
```swift
public protocol TokenCounting: Sendable {
    func countTokens(_ text: String) -> Int
    func truncateHead(_ text: String, toBudget budget: Int) -> String
}

public struct PromptBuilder: Sendable {
    public init(counter: TokenCounting, budget: PromptBudget = .phase1Default)
    public func build(
        system: String,
        customInstructions: String,
        contextPrefix: String,
        fewShot: String,
        beforeCursor: String
    ) -> BuiltPrompt
}

public struct BuiltPrompt: Sendable, Equatable {
    public let text: String
    public let slotTexts: [PromptSlot: String]
    public let slotTokenCounts: [PromptSlot: Int]
    public let truncatedSlots: Set<PromptSlot>
    public let totalTokens: Int
}
```

From MLXLMCommon (via `context.tokenizer` inside `container.perform`):
```swift
// Already used in PredictorViewModel.swift:693, 701, 835, 851
context.tokenizer.encode(text: String) -> [Int]
context.tokenizer.applyChatTemplate(messages: [[String:String]]) -> [Int]?
// context.tokenizer is `any Tokenizer` (re-exported from swift-transformers 1.0.0)
```

From Souffleuse/Sources/Souffleuse/PredictorViewModel.swift (existing env-var precedent):
```swift
private enum PredictDebug {
    static let enabled: Bool = ProcessInfo.processInfo.environment["SOUFFLEUSE_PREDICT_LOG"]?.isEmpty == false
    // ...
}
```

From Souffleuse/Sources/SouffleuseLog/Log.swift:
```swift
public static func info(_ module: LogModule, _ event: StaticString, count: Int? = nil)
// event is StaticString = compile-time literal ONLY. Cannot interpolate user text.
```

Existing call site shape (PredictorViewModel.swift lines 668-702):
```swift
let metrics = try await container.perform { context -> StreamMetrics in
    let promptTokens: [Int]
    if isInstructModel {
        let messages: [[String: String]] = [
            ["role": "system", "content": systemMessage],
            ["role": "user", "content": llmTail],
        ]
        if let templated = try? context.tokenizer.applyChatTemplate(messages: messages) {
            promptTokens = templated
        } else {
            promptTokens = context.tokenizer.encode(text: basePromptText)
        }
    } else {
        promptTokens = context.tokenizer.encode(text: basePromptText)
    }
    // ... generation loop uses promptTokens
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Créer Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift</name>
  <files>Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift</files>
  <read_first>
    - Souffleuse/Sources/SouffleusePrompt/TokenCounting.swift (le protocol à conformer)
    - Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift (pour comprendre comment truncateHead est appelé)
    - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift lignes 690-702 (pattern existant `context.tokenizer.encode(text:)`)
    - Souffleuse/Sources/SouffleuseContext/OCRCaretLocator.swift (analog concrete protocol impl)
    - .planning/phases/01-foundation-hypothesis-validation/01-RESEARCH.md §1 (MLXTokenCounter snippet), §3 (truncateHead algorithm)
    - .planning/phases/01-foundation-hypothesis-validation/01-PATTERNS.md §MLXTokenCounter.swift
  </read_first>
  <action>
Créer `Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift`. Adaptateur stateless `struct: Sendable` (PAS `actor` — le tokenizer est sync, et SouffleuseContext/OCRCaretLocator n'est `actor` que parce qu'il sérialise dispatch Vision/capture, pas un précédent applicable ici).

**Décision Q4 du RESEARCH §12** : on place `MLXTokenCounter` dans le target app `Souffleuse` (pas dans `SouffleusePrompt`). Justification : permet à `SouffleusePrompt` de rester importable par les tests sans pulling MLX (validé par plan 01-02 qui n'importe rien de MLX). Place près de `PredictorViewModel.swift` qui est l'unique consumer.

```swift
import Foundation
import MLXLMCommon
import SouffleusePrompt
import Tokenizers   // re-exported via MLXLMCommon's dep on swift-transformers

/// Production adapter wrapping an MLX `Tokenizer` reference behind the
/// `TokenCounting` protocol. Constructed inside the `container.perform`
/// closure where `context.tokenizer` is reachable (per R2 in RESEARCH §11:
/// the tokenizer is `Sendable` but capturing it outside the actor-isolated
/// closure is undocumented — fresh allocation per predict avoids the issue).
///
/// Stateless `struct: Sendable` — same precedent as `EnrichedContext` /
/// `AXSnapshot`. Not an `actor` (unlike `OCRCaretLocator`) because tokenizer
/// calls are sync and the struct holds no mutable state.
///
/// TODO Phase 2: dedupe via shared SouffleusePrompt.MLXTokenCounter
/// (see RESEARCH §11 R4) — currently duplicated in SouffleuseCoherence
/// as CoherenceTokenCounter for Phase 1 simplicity.
struct MLXTokenCounter: TokenCounting {
    /// Reference to the loaded MLX tokenizer. Held as `any Tokenizer` to match
    /// the `MLXLMCommon` API exposed at `context.tokenizer`.
    let tokenizer: any Tokenizer

    func countTokens(_ text: String) -> Int {
        guard !text.isEmpty else { return 0 }
        return tokenizer.encode(text: text).count
    }

    /// Head-truncate `text` to ≤ budget tokens, preferring sentence boundary
    /// then word boundary. D-11 invariant: NEVER cut mid-word. Returns "" if
    /// budget < 1 or no boundary fits (defensive — caller logs a counter-only
    /// warn event).
    ///
    /// Algorithm: split into words (whitespace boundary), then walk from the
    /// head dropping words until the remaining suffix tokenizes to ≤ budget.
    /// Among candidate cut points, prefer those that land RIGHT AFTER a
    /// sentence terminator (`. `, `? `, `! `, `… `, `\n\n`). If none fit,
    /// fall back to the smallest word-boundary cut that fits.
    func truncateHead(_ text: String, toBudget budget: Int) -> String {
        guard budget >= 1 else { return "" }
        let trimmed = text.trimmingCharacters(in: .whitespaces)
        guard !trimmed.isEmpty else { return "" }

        // Quick path: if it already fits, return unchanged.
        let totalTokens = countTokens(trimmed)
        if totalTokens <= budget { return trimmed }

        // Split on whitespace, keep word strings (no leading/trailing spaces).
        let words = trimmed.split(whereSeparator: { $0.isWhitespace }).map(String.init)
        guard !words.isEmpty else { return "" }

        // Build a list of candidate cut points: cutWordIdx = the index of the
        // FIRST word kept. cutWordIdx ranges from 1 to words.count-1 (must
        // keep at least 1 word).
        //
        // For sentence-boundary preference, mark which cutWordIdx values
        // immediately follow a sentence terminator at words[cutWordIdx - 1].
        // Sentence terminators (reusing the list from PredictorViewModel.swift:569):
        // ". ", "? ", "! ", "… " — adapted to per-word last-char check
        // since we lose the trailing space context after split.
        let sentenceEnds: Set<Character> = [".", "?", "!", "…"]

        // Helper: try a specific cutWordIdx, return the suffix string and its
        // token count.
        func tryCut(_ cutWordIdx: Int) -> (text: String, tokens: Int) {
            let suffix = words[cutWordIdx...].joined(separator: " ")
            return (suffix, countTokens(suffix))
        }

        // First pass: walk cutWordIdx from SMALLEST (most retained) to largest.
        // Prefer the smallest cut that lands on a sentence boundary AND fits.
        for cutWordIdx in 1..<words.count {
            // Sentence boundary: the previous word's last non-whitespace char
            // is a terminator.
            let prevWord = words[cutWordIdx - 1]
            guard let last = prevWord.last, sentenceEnds.contains(last) else { continue }
            let (txt, count) = tryCut(cutWordIdx)
            if count <= budget {
                return txt
            }
        }

        // Second pass: fallback to word boundary. Walk smallest-cut first.
        for cutWordIdx in 1..<words.count {
            let (txt, count) = tryCut(cutWordIdx)
            if count <= budget {
                return txt
            }
        }

        // Defensive: even keeping the last word alone doesn't fit (single
        // giant token blob). Per RESEARCH §3 edge case: return "" — caller
        // (PromptBuilder) flags the slot as truncated.
        return ""
    }
}
```

**Notes** :
- `import Tokenizers` : `swift-transformers` `Tokenizer` protocol est re-exported via `MLXLMCommon`. Si Swift compile bloque sur l'import direct, retirer `import Tokenizers` (le type est déjà accessible via `MLXLMCommon`).
- `any Tokenizer` : la syntaxe Swift 6 pour utiliser un existential. `Tokenizer` est un protocol de `swift-transformers` 1.0.0 (HIGH confidence per RESEARCH §2).
- Aucun `print`, `Log.*`, `os_log` — l'adapter est dans `SHIPPING_DIRS=("Sources/Souffleuse" ...)` historiquement, audit.sh continue de scanner ce fichier.

**Privacy (T1)** : `MLXTokenCounter` reçoit des `String` slot inputs via les méthodes du protocol. Il N'EN LOGUE AUCUN. Pas de paramètre logger.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/cocotypist/Souffleuse && test -f Sources/Souffleuse/MLXTokenCounter.swift && grep -q "struct MLXTokenCounter: TokenCounting" Sources/Souffleuse/MLXTokenCounter.swift && grep -q "import SouffleusePrompt" Sources/Souffleuse/MLXTokenCounter.swift && swift build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift`
    - `grep -q "struct MLXTokenCounter: TokenCounting" Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift`
    - `grep -q "import SouffleusePrompt" Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift`
    - `grep -q "import MLXLMCommon" Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift`
    - `grep -q "func countTokens" Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift`
    - `grep -q "func truncateHead" Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift`
    - Aucun `print(`, `NSLog(`, `os_log(`, `Log\\.` dans le fichier (hors commentaires) : `grep -E "(print\\(|NSLog\\(|os_log\\(|Log\\.)" Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift | grep -vE "^[^:]*///|^[^:]*//" ; [ $? -ne 0 ]`
    - `cd Souffleuse && swift build` exit code 0
    - `cd Souffleuse && bash ./audit.sh` exit code 0 (le fichier est sous `Sources/Souffleuse` déjà dans SHIPPING_DIRS)
  </acceptance_criteria>
  <done>
    MLXTokenCounter compile et conforme TokenCounting. Prêt à être instancié dans PredictorViewModel container.perform closure (Task 2).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Intégrer PromptBuilder dans PredictorViewModel.predict() derrière feature flag</name>
  <files>Souffleuse/Sources/Souffleuse/PredictorViewModel.swift</files>
  <read_first>
    - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift lignes 1-50 (imports + PredictDebug env-gate pattern à imiter)
    - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift lignes 460-700 (predict path complet — systemMessage assembly + container.perform + isInstructModel branche)
    - Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift (Task 1, l'adapter à instancier)
    - Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift (API à appeler)
    - Souffleuse/Sources/SouffleusePrompt/BuiltPrompt.swift (pour extraire slotTexts[.beforeCursor])
    - Souffleuse/Sources/SouffleuseLog/Log.swift (Log API surface — StaticString event)
    - .planning/phases/01-foundation-hypothesis-validation/01-RESEARCH.md §5 (integration plan détaillé avec snippet code)
    - .planning/phases/01-foundation-hypothesis-validation/01-PATTERNS.md §PredictorViewModel.swift
  </read_first>
  <action>
Modification ciblée de `Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`. Préserver tout le code existant — le flag dev-only sélectionne entre legacy verbatim et nouveau path.

**1. Imports en haut du fichier (après les imports existants)** :
Ajouter `import SouffleusePrompt` à la liste d'imports si pas déjà présent. Ne pas modifier d'autres imports.

**2. File-scope private enum (placer immédiatement après l'enum `PredictDebug` au début du fichier, lignes ~15-36)** :
```swift
private enum PromptBuilderFlag {
    /// Dev-only: route predict() through the new SouffleusePrompt PromptBuilder
    /// instead of the legacy flat-string concat. Per D-12: removed at end of
    /// Phase 1 once the replay verdict is positive and snapshot tests are green.
    /// NEVER document this flag in user-facing materials.
    static let enabled: Bool =
        ProcessInfo.processInfo.environment["SOUFFLEUSE_PROMPT_BUILDER"]?.isEmpty == false
}
```
NB : pattern miroir EXACT de `PredictDebug` lignes 15-16. PRIVATE file-scope enum. Le static let est évalué une fois au load, comme `PredictDebug.enabled`.

**3. Site d'intégration : à l'intérieur du `container.perform { context -> StreamMetrics in` closure (PredictorViewModel.swift lignes 669-702)**.

Remplacer le bloc actuel (lignes 681-702) :
```swift
let promptTokens: [Int]
if isInstructModel {
    // ... legacy ...
} else {
    promptTokens = context.tokenizer.encode(text: basePromptText)
}
```

Par :
```swift
let promptTokens: [Int]
if PromptBuilderFlag.enabled {
    // ── NEW PATH (Phase 1 PromptBuilder) ──────────────────────────
    // BUILDER-02 + ROADMAP Success Criterion #2 : chaque piece de contexte
    // flows dans SON propre slot (avec son budget en tokens). On NE PASSE
    // PAS customInstructions/contextPrefix vides — sinon les budgets
    // per-slot (customInstructions: 40, contextPrefix: 150) ne sont jamais
    // exercés en production et l'hypothèse Phase 1 est invalidée.
    //
    // Le système slot reçoit UNIQUEMENT le base framing (langue + rôle),
    // SANS le append legacy de customInstructions ni contextPrefix qui
    // étaient inlinés dans `systemMessage` (lignes ~487-492). Le legacy
    // path en bas reste verbatim et continue d'utiliser `systemMessage`
    // tel quel.
    //
    // Construit le PromptBuilder ici parce que `context.tokenizer` n'est
    // reachable QUE dans ce closure (per R2 RESEARCH §11). Cost: une struct
    // allocation par predict, négligeable.
    let counter = MLXTokenCounter(tokenizer: context.tokenizer)
    let builder = PromptBuilder(counter: counter, budget: .phase1Default)
    // Re-extract base system framing without the customInstructions /
    // contextPrefix inline append (cf. ligne ~486 du legacy assembly).
    let baseSystem = Self.buildSystemPrompt(detectedLanguage: detectedLanguage)
    // Per-slot routing (BUILDER-02). Variables nommées pour que la grep
    // gate acceptance criterion les détecte (pas de string-literal "" dans
    // les slot args du new path).
    let customInstr = customInstructions.trimmingCharacters(in: .whitespacesAndNewlines)
    let ctxPrefix = contextPrefix.trimmingCharacters(in: .whitespacesAndNewlines)
    let built = builder.build(
        system: baseSystem,
        customInstructions: customInstr,
        contextPrefix: ctxPrefix,
        fewShot: examplesBlock,
        beforeCursor: userTail      // FULL userTail; builder applique head-truncation
    )
    // T1 mitigation: count-only event, StaticString event literal. NEVER log
    // built.text or built.slotTexts[.beforeCursor].
    Log.info(.predictor, "prompt_built", count: built.totalTokens)

    if isInstructModel {
        // Instruct path: user message = beforeCursor slot (post-truncation).
        // System role = built.text MINUS the beforeCursor slot — i.e. the
        // assembled head (system + customInstructions + contextPrefix +
        // fewShot). On reconstruct par concatenation des slotTexts dans
        // l'ordre canonique (system, customInstructions, contextPrefix,
        // fewShot) pour préserver la routing per-slot dans le chat template.
        let userContent = built.slotTexts[.beforeCursor] ?? ""
        let systemContent = [
            built.slotTexts[.system],
            built.slotTexts[.customInstructions],
            built.slotTexts[.contextPrefix],
            built.slotTexts[.fewShot],
        ].compactMap { $0 }.filter { !$0.isEmpty }.joined(separator: "\n\n")
        let messages: [[String: String]] = [
            ["role": "system", "content": systemContent],
            ["role": "user", "content": userContent],
        ]
        if let templated = try? context.tokenizer.applyChatTemplate(messages: messages) {
            promptTokens = templated
        } else {
            promptTokens = context.tokenizer.encode(text: built.text)
        }
    } else {
        // Base/PT path: full built.text as raw continuation. Les slots
        // sont déjà concaténés dans l'ordre canonique par le builder.
        promptTokens = context.tokenizer.encode(text: built.text)
    }
} else {
    // ── LEGACY PATH (verbatim from pre-Phase-1 code, unchanged) ──────
    if isInstructModel {
        let messages: [[String: String]] = [
            ["role": "system", "content": systemMessage],
            ["role": "user", "content": llmTail],
        ]
        if let templated = try? context.tokenizer.applyChatTemplate(messages: messages) {
            promptTokens = templated
        } else {
            promptTokens = context.tokenizer.encode(text: basePromptText)
        }
    } else {
        promptTokens = context.tokenizer.encode(text: basePromptText)
    }
}
let input = LMInput(tokens: MLXArray(promptTokens))
// ... le reste (params, generation, stream) reste verbatim
```

**Préserver verbatim** :
- Tout le code legacy : lignes 478-513 (systemMessage assembly + basePreamble), lignes 632-664 (Task body avec examplesBlock + llmTail + basePromptText)
- Le comportement quand `SOUFFLEUSE_PROMPT_BUILDER` n'est pas set est rigoureusement identique au commit `6ad70df` baseline.
- `PredictDebug.log("...")` calls (env-gated debug, hors audit scope per CONCERNS.md §"Security Considerations") — ne pas en ajouter ni retirer.

**Privacy invariants (T1 + T3 du security gate)** :
- L'event `"prompt_built"` est une StaticString (compile-time literal). Le tuple `count: built.totalTokens` est un Int — pas de user text.
- AUCUN `Log.*` ne mentionne `built.text`, `built.slotTexts`, `userTail`, `examplesBlock`, `systemMessage`. La grep gate `audit.sh` check 6 le verifie : `Log\\.(info|warn|error)\\([^"]*\\\\(.*(accepted|contextBefore|entry\\.|prefix)` — notre integration ne match jamais ce pattern.
- L'env var `SOUFFLEUSE_PROMPT_BUILDER` est lue via `ProcessInfo` et N'EST JAMAIS loggée.

**Validation séquentielle (verify command)** :
1. `swift build` (exit 0) — toute la pipeline compile
2. `bash audit.sh` (exit 0) — pas de regression privacy. CHECK 6 critique.
3. `swift test` (exit 0) — 94 tests existants + nouveaux tests builder, tous verts (le flag défaut OFF → legacy path exercé par tests existants)
4. Smoke check: `SOUFFLEUSE_PROMPT_BUILDER=1 swift run SouffleuseCoherence 2>&1 | head -10` réussit le model load (pas de smoke test runtime requis pour ce plan — le replay harness du plan 01-04 validera le path en bout-à-bout)
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/cocotypist/Souffleuse && grep -q "private enum PromptBuilderFlag" Sources/Souffleuse/PredictorViewModel.swift && grep -q 'SOUFFLEUSE_PROMPT_BUILDER' Sources/Souffleuse/PredictorViewModel.swift && grep -q "PromptBuilder(counter:" Sources/Souffleuse/PredictorViewModel.swift && grep -q 'Log\.info(\.predictor, "prompt_built"' Sources/Souffleuse/PredictorViewModel.swift && swift build 2>&1 | tail -10 && bash ./audit.sh && swift test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "private enum PromptBuilderFlag" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
    - `grep -q "SOUFFLEUSE_PROMPT_BUILDER" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
    - `grep -q "PromptBuilder(counter:" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
    - `grep -q "MLXTokenCounter(tokenizer:" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
    - **Per-slot routing (BUILDER-02)** : `grep -q "customInstructions: customInstr" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift` (variable, not "")
    - **Per-slot routing (BUILDER-02)** : `grep -q "contextPrefix: ctxPrefix" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift` (variable, not "")
    - **Per-slot routing (BUILDER-02)** : `grep -q "let baseSystem = Self.buildSystemPrompt" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift` (system slot receives base framing only)
    - **Per-slot routing (BUILDER-02)** : le NEW path NE passe PAS de literal `""` aux slots customInstructions/contextPrefix : `! grep -E "(customInstructions|contextPrefix):\s*\"\"" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
    - `grep -q "Log\.info(\.predictor, \"prompt_built\"" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
    - `grep -q "import SouffleusePrompt" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
    - Le mot `PromptBuilderFlag.enabled` apparaît dans le predict path : `grep -q "PromptBuilderFlag.enabled" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
    - Le code legacy `basePromptText = basePreamble` est PRESERVÉ : `grep -q "basePromptText = basePreamble" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
    - Le code legacy `let llmTail = String(userTail.suffix(512))` est PRESERVÉ : `grep -q "let llmTail = String(userTail.suffix(512))" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`
    - `cd Souffleuse && swift build` exit code 0
    - `cd Souffleuse && bash ./audit.sh` exit code 0 (les 6 checks verts, particulièrement check 6 qui scrute Log.* interpolating user fields)
    - `cd Souffleuse && swift test` exit code 0 ET nombre de tests passés ≥ 94 + PromptBuilderTests (plan 01-02 est wave 1, déjà green)
    - AUCUN appel `Log.*` dans PredictorViewModel.swift ne référence `built\.text`, `built\.slotTexts`, `userTail`, `examplesBlock` : `grep -E "Log\.(info|warn|error)\([^)]*\\\\\(.*(built|userTail|examplesBlock)" Souffleuse/Sources/Souffleuse/PredictorViewModel.swift | grep -vE "^[^:]*///|^[^:]*//" ; [ $? -ne 0 ]`
  </acceptance_criteria>
  <done>
    Pipeline existante (flag OFF) intacte, nouveau path (flag ON) compile et execute. Le replay harness du plan 01-04 va exercer le nouveau path en mode --replay. Audit.sh vert. 94+ tests verts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `predict()` `container.perform` closure | Reçoit `userTail`, `contextPrefix`, `customInstructions` (user-derived). Passe à PromptBuilder. |
| `ProcessInfo` env var lookup | `SOUFFLEUSE_PROMPT_BUILDER` flag detection au load time. |
| `Log.info` call site dans le nouveau path | Émet `"prompt_built"` event StaticString + count Int. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `Log.info(.predictor, "prompt_built", count:)` | mitigate | T1 du security gate : event est StaticString (compile-time literal), payload est `count: Int` uniquement. Aucune interpolation de `built.text`/`slotTexts`. Audit.sh check 6 grep gate verrouille au commit time. |
| T-03-02 | Information Disclosure | `ProcessInfo.processInfo.environment["SOUFFLEUSE_PROMPT_BUILDER"]` | mitigate | T3 du security gate : env var lue une fois au static load (pattern miroir de `PredictDebug`), JAMAIS écrite dans un log. Mitigation passive (par construction). |
| T-03-03 | Tampering | Legacy path préservé | mitigate | BUILDER-04 : conservation verbatim du code basePreamble + basePromptText. Quand flag OFF, comportement identique au commit `6ad70df`. 94 tests existants exercent ce path et restent verts. |
| T-03-04 | Repudiation | `prompt_built` event | accept | Single counter-only event ; pas de répudiation surface (cohérent avec `Log.info(.predictor, "fewshot_injected", count:)` existant). |
| T-03-05 | Elevation of Privilege | env var as dev-only | accept | Le flag est dev-only par convention (D-12, Q11). Si un user curieux le set, il accède au nouveau path (snapshot-tested + replay-validated). Pas de privilège escaladable. |
</threat_model>

<verification>
- `swift build` exit code 0 (compilation complète)
- `bash audit.sh` exit code 0 (6 checks verts, check 6 critique)
- `swift test` exit code 0 (94 + PromptBuilderTests, tous verts)
- Le legacy path est exercé par défaut (flag OFF) — préservation BUILDER-04
- Le nouveau path est gated par `PromptBuilderFlag.enabled` derrière env var `SOUFFLEUSE_PROMPT_BUILDER`
- `MLXTokenCounter` instancié à l'intérieur de `container.perform` (per R2 RESEARCH §11, jamais hoisté)
- Un seul `Log.info(.predictor, "prompt_built", count:)` ajouté ; aucune interpolation de user text
</verification>

<success_criteria>
- BUILDER-04 satisfait : pipeline existante continue de fonctionner sans régression (flag OFF par défaut = legacy verbatim)
- Migration strategy tranchée (feature flag dev-only, justifié par scope du nouveau capability)
- Adapter `MLXTokenCounter` conforme `TokenCounting` + utilisé via `PromptBuilder(counter:budget:)` dans le bon scope d'isolation
- DEUX paths (instruct + base/PT) wirés derrière le flag
- Privacy invariants préservés (audit.sh vert, aucun user text loggué)
- Prêt pour le replay harness du plan 01-04 qui activera le flag pour comparer A/B
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-hypothesis-validation/01-03-SUMMARY.md` documenting:
- Fichier `MLXTokenCounter.swift` créé (path, taille, conforme `TokenCounting`)
- `PromptBuilderFlag` enum ajouté au top de PredictorViewModel.swift (lignes near PredictDebug)
- Bloc d'intégration ajouté dans `container.perform` (lignes ~681-702 originelles, désormais branche `if PromptBuilderFlag.enabled { ... } else { ... }`)
- Confirmation `swift build` + `audit.sh` + `swift test` verts
- `Log.info(.predictor, "prompt_built", count:)` placé exactement une fois
- Aucune modification du code legacy en dehors du dispatch ; basePreamble/basePromptText/llmTail intacts
- (Si applicable) Décision de retrait de `import Tokenizers` si MLXLMCommon le re-exporte déjà — documenter
</output>

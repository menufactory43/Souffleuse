---
phase: 01-foundation-hypothesis-validation
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-03]
files_modified:
  - Souffleuse/Sources/SouffleuseCoherence/main.swift
  - .planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json
autonomous: true
requirements: [AUDIT-01]
tags: [swift, mlx, replay-harness, audit, cli]

must_haves:
  truths:
    - "L'executable `SouffleuseCoherence` accepte un sub-command `--replay <scenarios.json>` qui prime sur le comportement par défaut."
    - "Le fichier `.planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json` existe avec 12 scénarios versionnés (version: 1) couvrant champ vide, Slack, Mail, VSCode, Notes, Intercom, Brave, Discord, et le stress head-truncation."
    - "Pour chaque scénario, le harness exécute le predict DEUX fois (avec contextPrefix, sans contextPrefix) via le même `PromptBuilder` que la production (BUILDER-01 path)."
    - "Le harness écrit `.planning/phases/01-foundation-hypothesis-validation/REPLAY-RESULTS.md` en atomic write avec une section par scénario : ghost-WITHOUT vs ghost-WITH + ligne verdict humain ✓/=/✗."
    - "Le replay ne lit AUCUN fichier réel d'utilisateur : pas de history.aes, pas de AX, pas de clipboard, pas de screen capture (T2 mitigation)."
    - "Le default behaviour (run sans `--replay`) reste verbatim (existing `Coherence.main()` loop préservée)."
  artifacts:
    - path: ".planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json"
      provides: "ScenarioFile JSON (version: 1) avec 12 scénarios curated"
      contains: "\"version\": 1"
    - path: "Souffleuse/Sources/SouffleuseCoherence/main.swift"
      provides: "Sub-command --replay + Scenario/ScenarioFile Codable + REPLAY-RESULTS.md writer atomic"
      contains: "--replay"
  key_links:
    - from: "Souffleuse/Sources/SouffleuseCoherence/main.swift"
      to: "Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift"
      via: "import SouffleusePrompt + PromptBuilder instantiation in replay loop"
      pattern: "PromptBuilder\\(counter:"
    - from: "Souffleuse/Sources/SouffleuseCoherence/main.swift"
      to: ".planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json"
      via: "JSONDecoder().decode(ScenarioFile.self, from: data)"
      pattern: "ScenarioFile.self"
    - from: "Souffleuse/Sources/SouffleuseCoherence/main.swift"
      to: ".planning/phases/01-foundation-hypothesis-validation/REPLAY-RESULTS.md"
      via: "data.write(to: ..., options: .atomic)"
      pattern: "options: \\.atomic"
---

<objective>
Étendre `SouffleuseCoherence/main.swift` avec un sub-command `--replay <scenarios.json>` (D-05) qui charge le scenario file, exécute chaque scénario en DEUX variantes (with/without contextPrefix) via le même PromptBuilder qu'en production, et écrit `REPLAY-RESULTS.md` côte-à-côte en markdown atomic-written. Créer le fichier `replay-scenarios.json` avec les 12 scénarios seed du RESEARCH §7.

Purpose: Satisfaire AUDIT-01 (PromptBuilder expose un mode test/replay qui rejoue 10-20 scénarios reproductibles avec et sans contexte enrichi et logue le ghost produit pour chaque variante). Le verdict humain (lecture du REPLAY-RESULTS.md + cochage ✓/=/✗) est fait au plan 01-05.

Output: `swift run SouffleuseCoherence --replay .planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json` charge le modèle, génère deux ghosts par scénario, et écrit REPLAY-RESULTS.md.

**Décision Q2 (seuil AUDIT-02)** : 6/12 ✓ verdicts (50% strict majority). Documenté au tally bloc du REPLAY-RESULTS.md, vérouillé au plan 01-05.
**Décision Q6 (nombre de scénarios)** : 12 (per seed RESEARCH §7).
**Décision Q8 (stdout + markdown)** : Les deux. Markdown = artifact ; stdout = condensed line par scénario (id + 2 ghosts) pour feedback temps réel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation-hypothesis-validation/01-CONTEXT.md
@.planning/phases/01-foundation-hypothesis-validation/01-RESEARCH.md
@.planning/phases/01-foundation-hypothesis-validation/01-PATTERNS.md
@.planning/phases/01-foundation-hypothesis-validation/01-01-SUMMARY.md
@.planning/phases/01-foundation-hypothesis-validation/01-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Interfaces from plans 01-01, 01-03 + existing SouffleuseCoherence main + MLXLMCommon -->

From Souffleuse/Sources/SouffleusePrompt/* (built by plan 01-01):
```swift
public struct PromptBuilder: Sendable {
    public init(counter: TokenCounting, budget: PromptBudget = .phase1Default)
    public func build(system:customInstructions:contextPrefix:fewShot:beforeCursor:) -> BuiltPrompt
}
public struct BuiltPrompt: Sendable, Equatable {
    public let text: String
    public let slotTexts: [PromptSlot: String]
    // ...
}
```

From Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift (built by plan 01-03):
```swift
struct MLXTokenCounter: TokenCounting {
    let tokenizer: any Tokenizer
    // ...
}
```
NB: `MLXTokenCounter` is `internal` in `Souffleuse` app target. To use it from `SouffleuseCoherence`, either: (a) re-implement a thin equivalent inside SouffleuseCoherence/main.swift (the simpler Phase 1 path since the file is already standalone and self-contained); OR (b) move MLXTokenCounter to `SouffleusePrompt` and make it `public`. **Recommendation:** option (a) — Phase 1 simplicity, mirror the duplication-acceptance posture from RESEARCH §6 ("Phase 1: leave duplicated, mark with `// TODO Phase 2: dedupe`").

From Souffleuse/Sources/SouffleuseCoherence/main.swift (existing harness):
```swift
@main
struct Coherence {
    static func main() async {
        setbuf(stdout, nil); setbuf(stderr, nil)
        let modelId = ProcessInfo.processInfo.environment["SOUFFLEUSE_MODEL"]
            ?? "mlx-community/gemma-3-1b-pt-8bit"
        // ... model load ...
        MLX.GPU.set(cacheLimit: 20 * 1024 * 1024)
        let cfg = ModelConfiguration(id: modelId, defaultPrompt: "")
        let container = try await LLMModelFactory.shared.loadContainer(configuration: cfg) { _ in }
        // ... default coherence loop ...
    }
}

@Sendable func emit(_ s: String) {
    FileHandle.standardOutput.write(Data((s + "\n").utf8))
}

enum Prod {
    static let maxWords = 4
    static let maxTokens = 12
    static func displayGhost(rawSnapshot: String, prefix: String) -> String { ... }
    // ... hasCompletedFirstWord, stripPrefixOverlap, ghostIsRepeatingPrefix ...
}
```

From Souffleuse/Sources/Souffleuse/AllowlistConfig.swift (versioned JSON precedent):
```swift
struct AllowlistFile: Codable {
    let version: Int
    // ...
}
// data.write(to: fileURL, options: .atomic)  // pattern at line 76-78
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Créer replay-scenarios.json (12 scénarios seed)</name>
  <files>.planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json</files>
  <read_first>
    - .planning/phases/01-foundation-hypothesis-validation/01-RESEARCH.md §7 (schema + initial seed exact des 12 scénarios)
    - .planning/phases/01-foundation-hypothesis-validation/01-CONTEXT.md (D-07 schema minimal requis)
    - Souffleuse/Sources/Souffleuse/AllowlistConfig.swift (analog versioned JSON `version: Int = 1`)
  </read_first>
  <action>
Écrire `.planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json` avec le contenu EXACT du seed de 12 scénarios documenté dans RESEARCH §7 (lignes 832-944 du fichier 01-RESEARCH.md). 

Structure du fichier :
```json
{
  "version": 1,
  "scenarios": [
    { "id": "slack-empty-channel", "label": "...", "bundleID": "com.tinyspeck.slackmacgap", ... },
    { "id": "slack-reply-mid", ... },
    { "id": "mail-new-subject", ... },
    { "id": "mail-reply-body", ... },
    { "id": "vscode-comment-fn", ... },
    { "id": "vscode-impl-line", ... },
    { "id": "notes-blank", ... },
    { "id": "intercom-cs-reply", ... },
    { "id": "browser-form-name", ... },
    { "id": "discord-reply", ... },
    { "id": "mid-edit-rewrite", ... },
    { "id": "long-tail-truncation", ... }
  ]
}
```

Chaque scénario contient AU MINIMUM (per D-07) : `id`, `label`, `bundleID`, `contextPrefix`, `userTail`. Optionnels: `windowTitle`, `notes`, `customInstructions`.

**Contenu intégral** : copier verbatim depuis RESEARCH §7 lignes 832-944, sans modification ni reformulation. Les bundle IDs sont validés (per VERIFIED note ligne 949 du RESEARCH : aucun n'est dans le `personalizationBundleBlocklist`/`bundleBlocklist`).

**Privacy invariants (T2 du security gate)** :
- Les 12 scénarios sont des STRINGS FICTIVES inventées (composé par l'auteur du research). Aucune extraction de logs / history réels.
- Pas de PII : tous les noms (Marie, Paul, Sophie, Alex) sont génériques, les emails sont `*@example.com` ou `*@cocotypist.io`.
- Le fichier est en clair, versionné git, diffable. C'est sa raison d'être (reproductibilité).
  </action>
  <verify>
    <automated>test -f /Users/gabrielwaltio/cocotypist/.planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json && python3 -c "import json; d = json.load(open('/Users/gabrielwaltio/cocotypist/.planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json')); assert d['version'] == 1, 'version'; assert len(d['scenarios']) == 12, f\"want 12 scenarios got {len(d['scenarios'])}\"; ids = [s['id'] for s in d['scenarios']]; required = ['slack-empty-channel','slack-reply-mid','mail-new-subject','mail-reply-body','vscode-comment-fn','vscode-impl-line','notes-blank','intercom-cs-reply','browser-form-name','discord-reply','mid-edit-rewrite','long-tail-truncation']; missing = [r for r in required if r not in ids]; assert not missing, f'missing: {missing}'; assert all('contextPrefix' in s and 'userTail' in s and 'bundleID' in s and 'id' in s and 'label' in s for s in d['scenarios']), 'missing required fields'; print('OK 12 scenarios validated')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json`
    - Le JSON parse sans erreur (`python3 -c "import json; json.load(open(...))"` exit 0)
    - `version == 1`
    - `len(scenarios) == 12`
    - IDs présents : `slack-empty-channel`, `slack-reply-mid`, `mail-new-subject`, `mail-reply-body`, `vscode-comment-fn`, `vscode-impl-line`, `notes-blank`, `intercom-cs-reply`, `browser-form-name`, `discord-reply`, `mid-edit-rewrite`, `long-tail-truncation`
    - Chaque scénario a au minimum : `id`, `label`, `bundleID`, `contextPrefix`, `userTail`
    - Le scénario `long-tail-truncation` a un `userTail` de plus de 600 caractères (stress test head-truncation)
  </acceptance_criteria>
  <done>
    Fichier scenarios checked-in et conforme au schema D-07. Prêt à être lu par le `--replay` sub-command (Task 2).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Étendre SouffleuseCoherence/main.swift avec sub-command --replay</name>
  <files>Souffleuse/Sources/SouffleuseCoherence/main.swift</files>
  <read_first>
    - Souffleuse/Sources/SouffleuseCoherence/main.swift (le fichier complet — pattern `@main struct Coherence`, `Prod` enum, `emit`, `rawGhost`, default loop)
    - Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift (API à appeler)
    - Souffleuse/Sources/SouffleusePrompt/BuiltPrompt.swift (pour extraire .text)
    - Souffleuse/Sources/Souffleuse/MLXTokenCounter.swift (pattern à dupliquer en local — Phase 1 acceptable duplication per RESEARCH §6)
    - .planning/phases/01-foundation-hypothesis-validation/01-RESEARCH.md §6 (replay harness extension détaillé)
    - .planning/phases/01-foundation-hypothesis-validation/01-PATTERNS.md §SouffleuseCoherence/main.swift
    - Souffleuse/Sources/Souffleuse/AllowlistConfig.swift (atomic write pattern `data.write(to: fileURL, options: .atomic)`)
  </read_first>
  <action>
Modifier `Souffleuse/Sources/SouffleuseCoherence/main.swift`. Préserver TOUT le code existant (default coherence loop, `Prod` enum, `emit`, `rawGhost`) et ajouter les sections suivantes.

**1. Imports en haut du fichier** : ajouter `import SouffleusePrompt` après les imports MLX existants. Le module est déjà dépendance via Package.swift plan 01-01.

**2. Section avant `@main struct Coherence`** : déclarer les types Codable + helpers replay :

```swift
// MARK: - Replay harness (--replay sub-command)

/// One curated scenario. Schema per D-07 (CONTEXT.md) and §7 (RESEARCH.md).
struct Scenario: Codable, Sendable {
    let id: String
    let label: String
    let bundleID: String
    let windowTitle: String?
    let contextPrefix: String
    let userTail: String
    let notes: String?
    let customInstructions: String?
}

/// Top-level scenario file (versioned per project's persisted-config convention).
struct ScenarioFile: Codable, Sendable {
    let version: Int
    let scenarios: [Scenario]
}

/// Local TokenCounting impl mirroring `MLXTokenCounter` from the Souffleuse
/// app target. Duplicated here per Phase 1 simplicity (RESEARCH §6). Phase 2
/// candidate: extract to a shared `SouffleusePrompt`-public adapter.
/// TODO Phase 2: dedupe with Sources/Souffleuse/MLXTokenCounter.swift.
struct CoherenceTokenCounter: TokenCounting {
    let tokenizer: any Tokenizer

    func countTokens(_ text: String) -> Int {
        guard !text.isEmpty else { return 0 }
        return tokenizer.encode(text: text).count
    }

    func truncateHead(_ text: String, toBudget budget: Int) -> String {
        guard budget >= 1 else { return "" }
        let trimmed = text.trimmingCharacters(in: .whitespaces)
        guard !trimmed.isEmpty else { return "" }
        if countTokens(trimmed) <= budget { return trimmed }
        let words = trimmed.split(whereSeparator: { $0.isWhitespace }).map(String.init)
        guard !words.isEmpty else { return "" }
        let sentenceEnds: Set<Character> = [".", "?", "!", "…"]
        for cutWordIdx in 1..<words.count {
            let prev = words[cutWordIdx - 1]
            guard let last = prev.last, sentenceEnds.contains(last) else { continue }
            let suffix = words[cutWordIdx...].joined(separator: " ")
            if countTokens(suffix) <= budget { return suffix }
        }
        for cutWordIdx in 1..<words.count {
            let suffix = words[cutWordIdx...].joined(separator: " ")
            if countTokens(suffix) <= budget { return suffix }
        }
        return ""
    }
}

/// Run a single scenario through the PromptBuilder pipeline, returning the
/// displayed ghost. `contextPrefix` parameter lets the caller disable it for
/// the WITHOUT-context variant without mutating the scenario.
func replayScenario(
    _ s: Scenario,
    contextPrefix: String,
    container: ModelContainer
) async -> String {
    let result = try? await container.perform { ctx -> String in
        let counter = CoherenceTokenCounter(tokenizer: ctx.tokenizer)
        let builder = PromptBuilder(counter: counter, budget: .phase1Default)
        // System prompt: minimal framing for the replay (cohérent avec ce que
        // PredictorViewModel passe; simplifié ici pour ne pas dépendre du code
        // app target).
        let system = "You are an inline autocomplete. Continue the user's text naturally."
        let built = builder.build(
            system: system,
            customInstructions: s.customInstructions ?? "",
            contextPrefix: contextPrefix,
            fewShot: "",                    // Phase 1: few-shot pas exercé dans le replay
            beforeCursor: s.userTail
        )
        let toks = ctx.tokenizer.encode(text: built.text)
        let input = LMInput(tokens: MLXArray(toks))
        let params = GenerateParameters(
            maxTokens: Prod.maxTokens,
            temperature: 0,
            topP: 0.9,
            repetitionPenalty: 1.0,
            repetitionContextSize: 32
        )
        let stream = try MLXLMCommon.generate(
            input: input, parameters: params, context: ctx
        )
        var raw = ""
        for await ev in stream {
            if case .chunk(let t) = ev { raw += t }
        }
        return raw
    }
    let rawSnapshot = result ?? ""
    return Prod.displayGhost(rawSnapshot: rawSnapshot, prefix: s.userTail)
}

/// Load scenarios from JSON file.
func loadScenarios(from path: String) throws -> ScenarioFile {
    let url = URL(fileURLWithPath: path)
    let data = try Data(contentsOf: url)
    return try JSONDecoder().decode(ScenarioFile.self, from: data)
}

/// Generate the REPLAY-RESULTS.md markdown.
func renderReplayResults(
    modelId: String,
    results: [(scenario: Scenario, withoutCtx: String, withCtx: String)]
) -> String {
    let iso = ISO8601DateFormatter().string(from: Date())
    var out = """
    # Replay Results — Phase 1 Hypothesis Validation

    **Generated:** \(iso)
    **Model:** \(modelId)
    **Scenarios:** \(results.count)
    **Founding hypothesis under test:** « le ghost junk vient du prompt pauvre, pas du modèle ».

    Pour chaque scénario : le ghost produit SANS contexte (`contextPrefix=""`)
    vs AVEC contexte (`contextPrefix` du scénario). Eyeball verdict : ✓ si
    avec-contexte est plus pertinent, = si neutre, ✗ si moins pertinent.

    > **Caveat — système prompt** (W5) : le system prompt utilisé en replay
    > est une version simplifiée (`"You are an inline autocomplete. Continue
    > the user's text naturally."`) — la version production passe par
    > `PredictorViewModel.buildSystemPrompt(detectedLanguage:)` qui injecte
    > la langue détectée et un framing plus riche. Le verdict porte donc sur
    > l'EFFET DU `contextPrefix` (with vs without), pas sur la parité prompt
    > complète production.

    > **Caveat — paramètres génération** (W6) : `temperature=0`, `topP=0.9`,
    > `repetitionPenalty=1.0`, `maxTokens=12`. Peuvent différer des prefs
    > runtime utilisateur (qui modulent `maxTokens` via la pref "Court /
    > Moyen / Long").

    ---

    """
    for (i, r) in results.enumerated() {
        let safeUserTail = r.scenario.userTail
            .replacingOccurrences(of: "`", with: "\\`")
            .replacingOccurrences(of: "\n", with: "⏎")
        let safeWithout = r.withoutCtx
            .replacingOccurrences(of: "`", with: "\\`")
            .replacingOccurrences(of: "\n", with: "⏎")
        let safeWith = r.withCtx
            .replacingOccurrences(of: "`", with: "\\`")
            .replacingOccurrences(of: "\n", with: "⏎")
        out += """

        ## \(i+1). [\(r.scenario.id)] \(r.scenario.label)

        - **bundleID:** `\(r.scenario.bundleID)`
        - **windowTitle:** \(r.scenario.windowTitle.map { "`\($0)`" } ?? "—")
        - **userTail:** `\(safeUserTail)`
        - **notes:** \(r.scenario.notes ?? "—")

        | Variant | Ghost |
        |---------|-------|
        | **WITHOUT context** | `\(safeWithout)` |
        | **WITH context**    | `\(safeWith)` |

        **Verdict:** [ ] ✓ with-context better  [ ] = neutral  [ ] ✗ with-context worse

        **Human note:** _(fill in)_

        ---

        """
    }
    out += """

    ## Tally (fill in after eyeball pass)

    - ✓ with-context better: ___ / \(results.count)
    - = neutral:              ___ / \(results.count)
    - ✗ with-context worse:   ___ / \(results.count)

    **AUDIT-02 gate (planner-set):** ≥ 6 / \(results.count) ✓ verdicts to proceed to Phase 2.

    """
    return out
}

/// Replay sub-command entry point.
func runReplay(scenariosPath: String, modelId: String, container: ModelContainer) async {
    let file: ScenarioFile
    do {
        file = try loadScenarios(from: scenariosPath)
    } catch {
        emit("ERREUR chargement scenarios: \(error)")
        return
    }
    emit("Replay: \(file.scenarios.count) scénarios — modèle \(modelId)")
    var results: [(scenario: Scenario, withoutCtx: String, withCtx: String)] = []
    for (i, s) in file.scenarios.enumerated() {
        emit("[\(i+1)/\(file.scenarios.count)] \(s.id)")
        // Run BOTH variants sequentially (MLX is GPU-bound, parallel doesn't help).
        let withoutCtx = await replayScenario(s, contextPrefix: "", container: container)
        let withCtx    = await replayScenario(s, contextPrefix: s.contextPrefix, container: container)
        emit("    WITHOUT: \"\(withoutCtx.replacingOccurrences(of: "\n", with: "⏎"))\"")
        emit("    WITH:    \"\(withCtx.replacingOccurrences(of: "\n", with: "⏎"))\"")
        results.append((s, withoutCtx, withCtx))
    }
    let md = renderReplayResults(modelId: modelId, results: results)
    let outURL = URL(fileURLWithPath: scenariosPath)
        .deletingLastPathComponent()
        .appendingPathComponent("REPLAY-RESULTS.md")
    do {
        try Data(md.utf8).write(to: outURL, options: .atomic)
        emit("→ écrit: \(outURL.path)")
    } catch {
        emit("ERREUR écriture REPLAY-RESULTS.md: \(error)")
    }
}
```

**3. Modifier `@main struct Coherence`** : préfixer `main()` par une détection de sub-command. Le code par défaut reste INTACT (chaîne d'historique, ne pas casser le replay de cohérence existant).

Remplacer :
```swift
@main
struct Coherence {
    static func main() async {
        setbuf(stdout, nil); setbuf(stderr, nil)
        let modelId = ProcessInfo.processInfo.environment["SOUFFLEUSE_MODEL"]
            ?? "mlx-community/gemma-3-1b-pt-8bit"
        emit("──────────────────────────────────────────────")
        emit(" Coherence — typing simulé (config PROD)       ")
        // ... existing default body ...
    }
}
```

Par :
```swift
@main
struct Coherence {
    static func main() async {
        setbuf(stdout, nil); setbuf(stderr, nil)
        let args = CommandLine.arguments
        let modelId = ProcessInfo.processInfo.environment["SOUFFLEUSE_MODEL"]
            ?? "mlx-community/gemma-3-1b-pt-8bit"

        // Load model once — shared by both default and replay paths.
        MLX.GPU.set(cacheLimit: 20 * 1024 * 1024)
        let cfg = ModelConfiguration(id: modelId, defaultPrompt: "")
        emit("Chargement (\(modelId))…")
        let container: ModelContainer
        do {
            container = try await LLMModelFactory.shared.loadContainer(configuration: cfg) { _ in }
        } catch {
            emit("ERREUR chargement: \(error)")
            return
        }
        emit("  prêt.\n")

        // ── Sub-command dispatch ─────────────────────────────────────
        if args.count >= 3, args[1] == "--replay" {
            await runReplay(scenariosPath: args[2], modelId: modelId, container: container)
            return
        }

        // ── Default coherence loop (verbatim — typage simulé) ────────
        emit("──────────────────────────────────────────────")
        emit(" Coherence — typing simulé (config PROD)       ")
        emit(" modèle: \(modelId)")
        emit(" greedy temp=0 · penalty=\(penalty) · maxWords=\(Prod.maxWords)")
        emit("──────────────────────────────────────────────")

        var totals: [Verdict: Int] = [:]

        for t in targets {
            emit("[\(t.label)] « \(t.text) »")
            let chars = Array(t.text)
            for k in breakpoints(t.text) {
                let typed = String(chars[0..<k])
                let prefix = t.lead + typed
                let remaining = String(chars[k...])
                let trimmed = prefix.trimmingCharacters(in: .whitespaces)
                let fired = trimmed.count >= 3 && Prod.hasCompletedFirstWord(prefix)
                var ghost = ""
                if fired {
                    let raw = await rawGhost(prefix: prefix, on: container)
                    ghost = Prod.displayGhost(rawSnapshot: raw, prefix: prefix)
                }
                let v = classify(prefix: prefix, ghost: ghost, remaining: remaining, fired: fired)
                totals[v, default: 0] += 1
                let mark: String
                switch v {
                case .match: mark = "✓ MATCH"
                case .alt:   mark = "~ ALT  "
                case .empty: mark = "∅ EMPTY"
                case .gated: mark = "· GATED"
                }
                let shownPrefix = typed.replacingOccurrences(of: "\n", with: "⏎")
                let shownRem = remaining.replacingOccurrences(of: "\n", with: "⏎")
                emit("  \(mark)  tapé:\"\(shownPrefix)\"  ghost:\"\(ghost)\"  (cible→\"\(shownRem.prefix(28))\")")
            }
            emit("")
        }

        let fired = (totals[.match] ?? 0) + (totals[.alt] ?? 0) + (totals[.empty] ?? 0)
        let total = fired + (totals[.gated] ?? 0)
        emit("──────────────────────────────────────────────")
        emit(" Bilan global (\(total) étapes)")
        emit("──────────────────────────────────────────────")
        emit(" ✓ MATCH : \(totals[.match] ?? 0)")
        emit(" ~ ALT   : \(totals[.alt] ?? 0)")
        emit(" ∅ EMPTY : \(totals[.empty] ?? 0)   (ghost vide → user voit rien)")
        emit(" · GATED : \(totals[.gated] ?? 0)   (prod ne déclenche pas)")
        if fired > 0 {
            let mr = Double(totals[.match] ?? 0) / Double(fired) * 100
            let er = Double(totals[.empty] ?? 0) / Double(fired) * 100
            emit(String(format: " → match rate (sur déclenchés): %.0f%%", mr))
            emit(String(format: " → empty rate (sur déclenchés): %.0f%%", er))
        }
        emit("──────────────────────────────────────────────")
    }
}
```

**Notes** :
- Le model load a été factorisé en haut de `main()` (partagé entre default et replay), ne pas dupliquer.
- L'output `Chargement…` change légèrement (inclut modelId) — accepté car ce target est dev-only, pas dans SHIPPING_DIRS.
- `print(...)` et toutes les écritures stdout sont OK ici : SouffleuseCoherence N'EST PAS dans `audit.sh SHIPPING_DIRS` (per RESEARCH §10 final note).
- `import Tokenizers` n'est nécessaire que si Swift bloque sur `any Tokenizer` ; MLXLMCommon le re-exporte normalement.

**Privacy invariants (T2)** : `runReplay` ne lit QUE le fichier `scenarios.json` checked-in. Aucun read de `history.aes`, AX, clipboard, screen capture. Pas d'import de `SouffleusePersonalization`, `SouffleuseAX`, `SouffleuseContext`. Le check 5 d'audit.sh continue de passer (history.aes inchangé), bien que SouffleuseCoherence soit hors scope du scan.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/cocotypist/Souffleuse && grep -q '"--replay"' Sources/SouffleuseCoherence/main.swift && grep -q "import SouffleusePrompt" Sources/SouffleuseCoherence/main.swift && grep -q "struct ScenarioFile: Codable" Sources/SouffleuseCoherence/main.swift && grep -q "PromptBuilder(counter:" Sources/SouffleuseCoherence/main.swift && grep -q "options: .atomic" Sources/SouffleuseCoherence/main.swift && grep -q "REPLAY-RESULTS.md" Sources/SouffleuseCoherence/main.swift && swift build --target SouffleuseCoherence 2>&1 | tail -5 && bash ./audit.sh && swift test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import SouffleusePrompt' Souffleuse/Sources/SouffleuseCoherence/main.swift`
    - `grep -q 'struct Scenario: Codable, Sendable' Souffleuse/Sources/SouffleuseCoherence/main.swift`
    - `grep -q 'struct ScenarioFile: Codable, Sendable' Souffleuse/Sources/SouffleuseCoherence/main.swift`
    - `grep -q 'struct CoherenceTokenCounter: TokenCounting' Souffleuse/Sources/SouffleuseCoherence/main.swift`
    - `grep -q 'func runReplay(' Souffleuse/Sources/SouffleuseCoherence/main.swift`
    - `grep -q 'func renderReplayResults(' Souffleuse/Sources/SouffleuseCoherence/main.swift`
    - `grep -q '"--replay"' Souffleuse/Sources/SouffleuseCoherence/main.swift`
    - `grep -q 'PromptBuilder(counter:' Souffleuse/Sources/SouffleuseCoherence/main.swift`
    - `grep -q 'REPLAY-RESULTS.md' Souffleuse/Sources/SouffleuseCoherence/main.swift`
    - `grep -q 'options: .atomic' Souffleuse/Sources/SouffleuseCoherence/main.swift`
    - Le default loop est PRESERVÉ : `grep -q "Coherence — typing simulé" Souffleuse/Sources/SouffleuseCoherence/main.swift`
    - `cd Souffleuse && swift build --target SouffleuseCoherence` exit code 0
    - `cd Souffleuse && swift build` exit code 0 (full package, pas de casse cross-target)
    - `cd Souffleuse && bash ./audit.sh` exit code 0 (les 6 checks verts ; SouffleuseCoherence hors scope mais audit doit rester vert globalement)
    - `cd Souffleuse && swift test` exit code 0 (94+ tests, aucune régression)
    - Aucun import de `SouffleuseAX`, `SouffleuseContext`, `SouffleusePersonalization` (T2 mitigation) : `grep -E "import (SouffleuseAX|SouffleuseContext|SouffleusePersonalization)" Souffleuse/Sources/SouffleuseCoherence/main.swift ; [ $? -ne 0 ]`
  </acceptance_criteria>
  <done>
    `swift run SouffleuseCoherence --replay .planning/phases/01-foundation-hypothesis-validation/replay-scenarios.json` peut être lancé. Le markdown sortie est généré atomiquement. Le default loop reste intact. Verdict humain au plan 01-05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `replay-scenarios.json` (in-repo) | Fichier checked-in, contient des strings fictives. Lecture only par SouffleuseCoherence en mode replay. |
| `REPLAY-RESULTS.md` (in-repo, generated) | Markdown généré contenant les ghosts MLX. Strings issues du modèle + scenarios checked-in (pas de user text réel). |
| `CommandLine.arguments` parsing | Parsing minimal de `--replay <path>`. Pas de shell escape, pas de IO arbitraire. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `REPLAY-RESULTS.md` | mitigate | T2 du security gate : le harness ne lit QUE `replay-scenarios.json` (checked-in fictif). Aucun import de SouffleuseAX/Context/Personalization (acceptance criterion grep-gate). Aucun read de history.aes/clipboard/screen. Le markdown ne contient donc QUE des ghosts générés depuis des inputs fictifs. |
| T-04-02 | Tampering | Path traversal via `--replay <arg>` | accept | Phase 1 dev-only ; aucune validation de path requise (utilisateur unique = développeur). Si un path arbitraire est passé, le worst case est un crash au JSONDecoder. |
| T-04-03 | Information Disclosure | stdout emit per scenario | accept | SouffleuseCoherence hors `SHIPPING_DIRS` (CLI dev-only, RESEARCH §10 final). Print de ghosts en stdout = comportement intentionnel du dev harness. |
| T-04-04 | Repudiation | Atomic write REPLAY-RESULTS.md | mitigate | `data.write(to:, options: .atomic)` garantit qu'un kill mid-render ne laisse pas un fichier corrompu. Le markdown est versionné git. |
</threat_model>

<verification>
- `swift build --target SouffleuseCoherence` exit code 0
- `swift build` (full package) exit code 0
- `bash audit.sh` exit code 0 (les 6 checks verts)
- `swift test` exit code 0 (94+ tests verts)
- `replay-scenarios.json` parse en JSON valide + 12 scénarios validés
- Le harness n'importe AUCUN module qui touche au contexte réel (AX/Context/Personalization)
- Le default loop SouffleuseCoherence (typing simulé) reste exerçable via `swift run SouffleuseCoherence` (sans arg)
- L'exécution end-to-end du replay sera validée au plan 01-05 (model load = ~30s, génération 24 ghosts = ~30s)
</verification>

<success_criteria>
- AUDIT-01 satisfait : mode replay exposé, 12 scénarios reproductibles, log ghost par variante
- Sub-command `--replay <path>` opérationnel
- 12 scénarios curated (champ vide ×4, prefix-avec-contexte ×4, code ×2, mid-edit ×1, head-truncation stress ×1)
- REPLAY-RESULTS.md généré en atomic write au bon path
- Privacy invariants préservés (T2 mitigated par construction)
- Prêt pour le verdict humain + cleanup du flag au plan 01-05
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-hypothesis-validation/01-04-SUMMARY.md` documenting:
- Path et taille du `replay-scenarios.json` (validé : 12 scénarios)
- Nouveaux symboles ajoutés à `SouffleuseCoherence/main.swift` (`Scenario`, `ScenarioFile`, `CoherenceTokenCounter`, `replayScenario`, `runReplay`, `renderReplayResults`, `loadScenarios`)
- Confirmation : default loop intact, `swift run SouffleuseCoherence` (sans arg) produit toujours le typing-simulé output
- Confirmation : swift build/audit/test verts
- Documentation de la duplication acceptée de `MLXTokenCounter` → `CoherenceTokenCounter` (RESEARCH §6 posture)
- (Si applicable) Notes sur le runtime time du model load + 24 ghost generations (sera validé au plan 01-05 lors de l'exécution réelle)
</output>

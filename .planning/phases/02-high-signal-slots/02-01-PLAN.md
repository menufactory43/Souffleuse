---
phase: 02-high-signal-slots
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Souffleuse/Sources/SouffleusePrompt/PromptSlot.swift
  - Souffleuse/Sources/SouffleusePrompt/PromptBudget.swift
  - Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift
  - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift
  - Souffleuse/Sources/SouffleuseCoherence/main.swift
  - Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift
autonomous: true
requirements: [SLOT-04]
must_haves:
  truths:
    - "PromptSlot.fewShot no longer exists; PromptSlot.previousUserInputs is in the active block"
    - "PromptBuilder.build(...) accepts previousUserInputs: (not fewShot:) and the rename is exhaustive"
    - "All 104 tests remain green after the rename"
    - "audit.sh 6 checks remain green after the rename"
  artifacts:
    - path: Souffleuse/Sources/SouffleusePrompt/PromptSlot.swift
      provides: "Enum with previousUserInputs in active block, fewShot removed"
      contains: "case previousUserInputs"
    - path: Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift
      provides: "build(...) signature with previousUserInputs: parameter"
      contains: "previousUserInputs: String"
  key_links:
    - from: Souffleuse/Sources/Souffleuse/PredictorViewModel.swift
      to: Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift
      via: "builder.build(... previousUserInputs: examplesBlock ...)"
      pattern: "previousUserInputs:"
    - from: Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift
      to: Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift
      via: "test call sites use previousUserInputs:"
      pattern: "previousUserInputs:"
---

<objective>
Atomic mechanical rename of slot `fewShot` → `previousUserInputs` across PromptSlot enum, PromptBudget, PromptBuilder signature + internal references, PredictorViewModel integration site, SouffleuseCoherence replay harness, and PromptBuilderTests. NO behavior change. NO new slot wiring (Plan 02-03 do that).

Purpose: Per D-16, isolate the rename in a single atomic commit so the codebase never sits half-migrated. Phase 1 already declared `previousUserInputs` in the reserved block — this plan flips it to active and removes `fewShot`. Decoupling the rename from new-slot wiring keeps git history readable and gates Wave 2 cleanly.

Output: A single atomic commit where every occurrence of `fewShot` (case, parameter, dictionary key, doc-comment) becomes `previousUserInputs`, with 104/104 tests green and audit.sh 6/6 green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-high-signal-slots/02-CONTEXT.md
@.planning/phases/02-high-signal-slots/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md

<interfaces>
<!-- Phase 1 contract that this plan renames. The case `previousUserInputs` ALREADY EXISTS in the reserved block (PromptSlot.swift:20). This plan moves it up to active and deletes `fewShot`. -->

From Souffleuse/Sources/SouffleusePrompt/PromptSlot.swift (current):
```swift
public enum PromptSlot: String, Sendable, CaseIterable, Hashable {
    // ── Active in Phase 1 ────────────────────────────────────
    case system
    case customInstructions
    case contextPrefix
    case fewShot                          // ← REMOVE
    case beforeCursor

    // ── Reserved for Phase 2/3 (declared, never filled at Phase 1) ────
    case afterCursor                      // ← stays reserved (Plan 02-03 activates)
    case fieldContext                     // ← stays reserved (Plan 02-03 activates)
    case previousUserInputs               // ← MOVE TO ACTIVE BLOCK
    case clipboardContext                 // ← stays reserved (Phase 3)
    case screenContext                    // ← stays reserved (Phase 3)
}
```

From Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift (current):
```swift
public static let evictionPriority: [PromptSlot] = [
    .fewShot,                             // ← rename to .previousUserInputs
    .customInstructions,
    .contextPrefix,
    .beforeCursor,
    .system,
]

public func build(
    system: String,
    customInstructions: String,
    contextPrefix: String,
    fewShot: String,                      // ← rename to previousUserInputs: String
    beforeCursor: String
) -> BuiltPrompt
```

From Souffleuse/Sources/SouffleusePrompt/PromptBudget.swift (current):
```swift
public static let phase1Default = PromptBudget(
    global: 512,
    perSlot: [
        .system: 80,
        .customInstructions: 40,
        .contextPrefix: 150,
        .fewShot: 80,                     // ← rename key to .previousUserInputs
        .beforeCursor: 200,
    ]
)
```

From Souffleuse/Sources/Souffleuse/PredictorViewModel.swift (line 742, 764):
```swift
let built = builder.build(
    ...
    fewShot: examplesBlock,               // ← rename to previousUserInputs:
    beforeCursor: userTail
)
// And line 764:
built.slotTexts[.fewShot],                // ← rename to .previousUserInputs
```

From Souffleuse/Sources/SouffleuseCoherence/main.swift (line 288):
```swift
let built = builder.build(
    ...
    fewShot: "",                          // ← rename to previousUserInputs:
    beforeCursor: s.userTail
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic rename fewShot → previousUserInputs across all 6 files</name>
  <files>
    Souffleuse/Sources/SouffleusePrompt/PromptSlot.swift,
    Souffleuse/Sources/SouffleusePrompt/PromptBudget.swift,
    Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift,
    Souffleuse/Sources/Souffleuse/PredictorViewModel.swift,
    Souffleuse/Sources/SouffleuseCoherence/main.swift,
    Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift
  </files>
  <read_first>
    Souffleuse/Sources/SouffleusePrompt/PromptSlot.swift,
    Souffleuse/Sources/SouffleusePrompt/PromptBudget.swift,
    Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift,
    Souffleuse/Sources/SouffleusePrompt/BuiltPrompt.swift,
    Souffleuse/Sources/Souffleuse/PredictorViewModel.swift (lines 700-780),
    Souffleuse/Sources/SouffleuseCoherence/main.swift (lines 270-320),
    Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift,
    .planning/phases/02-high-signal-slots/02-CONTEXT.md (D-16),
    .planning/phases/02-high-signal-slots/02-PATTERNS.md (PromptSlot section)
  </read_first>
  <action>
**Per D-16, perform the rename as a single sweep — do NOT split into per-file commits.** Order of file edits below; all changes land in one git commit.

1. **`Souffleuse/Sources/SouffleusePrompt/PromptSlot.swift`:**
   - Delete the line `    case fewShot` from the active block.
   - Move `    case previousUserInputs` from the reserved block UP to the active block (place it where `fewShot` was — between `contextPrefix` and `beforeCursor` so source diff stays small).
   - Update divider comments: change `// ── Active in Phase 1 ────` to `// ── Active in Phase 2 ────` and `// ── Reserved for Phase 2/3 ────` to `// ── Reserved for Phase 3 ────`.
   - Update the leading doc-comment (lines 3-8): replace the phrase "Active in Phase 1: system, customInstructions, contextPrefix, fewShot, beforeCursor" with "Active in Phase 2: system, customInstructions, contextPrefix, previousUserInputs, beforeCursor" and remove `previousUserInputs` from the reserved list mentioned in the doc-comment.

2. **`Souffleuse/Sources/SouffleusePrompt/PromptBudget.swift`:**
   - In `phase1Default.perSlot`, rename the key `.fewShot: 80` to `.previousUserInputs: 80`. Keep budget at 80 per D-16b.
   - Update the doc-comment (lines 19-21): replace "fewShot=80" with "previousUserInputs=80" in the rationale text. Leave the rest of the comment untouched (`global: 512` stays — Plan 02-03 revisits the global cap).

3. **`Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift`:**
   - In `evictionPriority` (lines 28-34), rename `.fewShot,` to `.previousUserInputs,`.
   - In `build(...)` signature (lines 44-50), rename the parameter label and internal name from `fewShot:` to `previousUserInputs:`.
   - In the `inputs` tuple array (line 52-58), rename `(.fewShot, fewShot)` to `(.previousUserInputs, previousUserInputs)`.
   - In the `assemblyOrder` array (line 118-120), rename `.fewShot,` to `.previousUserInputs,`.
   - Update the doc-comment at the top of the type (lines 3-20): replace `4. fewShot` with `4. previousUserInputs` and replace the sentence "Eviction order when global cap fires: drop fewShot first" with "Eviction order when global cap fires: drop previousUserInputs first".

4. **`Souffleuse/Sources/Souffleuse/PredictorViewModel.swift`:**
   - Locate the call site around line 738-744. Rename the keyword arg `fewShot: examplesBlock` to `previousUserInputs: examplesBlock`. Do NOT add new slots yet — Plan 02-04 does that.
   - Locate the slotTexts subscript around line 764. Rename `built.slotTexts[.fewShot]` to `built.slotTexts[.previousUserInputs]`. Do NOT add new slots to the array yet.
   - Search the file for any other occurrence of `fewShot` (e.g. doc-comments around the integration site). Update each. Do NOT rename the local Swift variable `examplesBlock` or `fewShotK` — those are separate identifiers (see #5 below).
   - Specifically: line ~647 (`let fewShotK = Self.fewShotK`) refers to `SimilarHistoryRetrieval.defaultK` and is NOT the slot name — leave it untouched. Per D-16c, `SimilarHistoryRetrieval` itself is not refactored.

5. **`Souffleuse/Sources/SouffleuseCoherence/main.swift`:**
   - Locate the call site around line 284-290. Rename `fewShot: ""` to `previousUserInputs: ""`. Phase 2 still leaves this slot empty in the replay harness (Plan 02-05 may revisit).
   - Search the file for any other `fewShot` occurrence and update.

6. **`Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift`:**
   - For EVERY `@Test func ...` that invokes `builder.build(...)`, rename the keyword arg `fewShot:` to `previousUserInputs:`. Do not change any test body otherwise — assertions remain valid because the rename is purely lexical.
   - Search the file for any `.fewShot` dictionary subscript (e.g. `built.slotTexts[.fewShot]` or `built.slotTokenCounts[.fewShot]`) and update to `.previousUserInputs`.

7. **Verify no stragglers across the whole repo:**
   - Run `grep -rn 'fewShot' Souffleuse/Sources Souffleuse/Tests 2>/dev/null | grep -v '^#'` — output must be empty EXCEPT for the legitimate `fewShotK`/`SimilarHistoryRetrieval.defaultK` references in `PredictorViewModel.swift` (these are `let fewShotK` local Swift constants tied to the K-nearest-neighbour retrieval count, NOT slot names). Any other hit = miss; fix it.

8. **Commit message (single atomic commit):**
   ```
   refactor(02-01): rename PromptSlot.fewShot → previousUserInputs (D-16)

   Atomic mechanical rename per Phase 2 D-16. No behavior change.
   Phase 1 declared `previousUserInputs` in the reserved block —
   this commit flips it active and removes `fewShot`. The
   SimilarHistoryRetrieval source (Jaccard few-shot) is unchanged
   (D-16c); only the slot label moves.

   - PromptSlot enum: case moved from reserved → active
   - PromptBudget.phase1Default: key renamed (budget 80 preserved)
   - PromptBuilder: evictionPriority, build() signature, assemblyOrder
   - PredictorViewModel: integration call site + slotTexts subscript
   - SouffleuseCoherence: replay harness call site
   - PromptBuilderTests: all 10 @Test call sites updated

   No new slots. No new wiring. Plan 02-02/02-03 will add fieldContext
   and afterCursor.
   ```
  </action>
  <verify>
    <automated>cd Souffleuse && swift build 2>&1 | tail -5 && swift test 2>&1 | tail -5 && bash audit.sh 2>&1 | tail -5 && grep -rn 'fewShot[^K]' Sources Tests 2>/dev/null | grep -v 'fewShotK\|SimilarHistoryRetrieval.defaultK\|^#' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `cd Souffleuse && swift build` exit 0
    - `cd Souffleuse && swift test` reports 104/104 tests passed (no regression; same count as Phase 1 end)
    - `cd Souffleuse && bash audit.sh` exit 0 (6/6 checks)
    - `grep -rn 'case fewShot' Souffleuse/Sources` returns 0 matches
    - `grep -rn 'case previousUserInputs' Souffleuse/Sources/SouffleusePrompt/PromptSlot.swift` returns 1 match in the active block (above the divider for reserved slots)
    - `grep -n 'previousUserInputs:' Souffleuse/Sources/SouffleusePrompt/PromptBuilder.swift` returns ≥3 matches (signature, inputs tuple, doc-comment)
    - `grep -n 'previousUserInputs:' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift` returns ≥1 match (call site)
    - `grep -n 'previousUserInputs:' Souffleuse/Tests/SouffleuseTests/PromptBuilderTests.swift` returns ≥5 matches (every test invoking build(...))
    - `grep -rn 'fewShot[^K]' Souffleuse/Sources Souffleuse/Tests 2>/dev/null | grep -v 'fewShotK\|SimilarHistoryRetrieval' | grep -v '^#'` returns 0 (no stragglers)
    - git log shows a single commit with message starting `refactor(02-01): rename PromptSlot.fewShot`
  </acceptance_criteria>
  <done>
    Single atomic commit on main containing the exhaustive rename. Build green, 104 tests green, audit.sh 6/6 green. No new slots wired (Plan 02-03 does that). No regression in any test assertion. SimilarHistoryRetrieval source unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan is a pure lexical rename — no new trust boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | audit.sh log-field whitelist | mitigate | `audit.sh` check #5 (whitelisted log fields) MUST stay green; this plan adds NO new log statement, so the rename cannot regress the invariant. Verify command runs audit.sh as gate. |
| T-02-02 | Tampering | Test suite count | accept | 104 tests must remain at 104. The rename cannot legitimately remove or add tests. Verify command checks test count via `swift test` output. |
</threat_model>

<verification>
- `cd Souffleuse && swift build` exit 0
- `cd Souffleuse && swift test` reports 104 tests passed
- `cd Souffleuse && bash audit.sh` exit 0
- No `case fewShot` or `fewShot:` parameter survives in source
- Single git commit covers the entire rename (no split commits, no partial state)
</verification>

<success_criteria>
- 100% of `fewShot` slot references (case, key, parameter, doc) renamed to `previousUserInputs` in a single atomic commit
- 104/104 tests green
- audit.sh 6/6 green
- No `SimilarHistoryRetrieval` code touched (D-16c)
- Wave 2 (Plans 02-02 + 02-03) can start against a clean compiling codebase
</success_criteria>

<output>
After completion, create `.planning/phases/02-high-signal-slots/02-01-SUMMARY.md` documenting: the rename scope (6 files, exhaustive), test count before/after (104 → 104), audit.sh status, the single commit SHA, and a 1-line callout that `fewShotK`/`SimilarHistoryRetrieval` were intentionally NOT renamed (these are the retrieval K constant, not the slot name).
</output>

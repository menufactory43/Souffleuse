---
phase: 04-cascade-quality-architecture
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - Souffleuse/Sources/Souffleuse/CompletionCache.swift
  - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift
  - Souffleuse/Tests/SouffleuseTests/CompletionCacheTests.swift
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
tags:
  - phase-04
  - split-pvm
  - completion-cache
  - kv-cache

must_haves:
  truths:
    - "CompletionCache owns predictCache FIFO(32), tokenCountCache, KVCacheHolder reference, lastContextFingerprint."
    - "KVCacheBypassFlag is moved into CompletionCache.swift — env var `SOUFFLEUSE_DISABLE_KV_CACHE` literal STAYS byte-identical."
    - "decideExtendTrimInvalidate(...) returns a KVDecision enum — pure decision tree extracted from PVM:1163-1311."
    - "storeInCache / clearPredictCache / longestExtendingKey API exposed for SuggestionPolicy + ModelRuntime."
    - "Context fingerprint invalidation moves verbatim from PVM:492-503."
  artifacts:
    - path: "Souffleuse/Sources/Souffleuse/CompletionCache.swift"
      provides: "@MainActor final class CompletionCache + KVDecision enum + KVCacheBypassFlag (moved)"
      min_lines: 200
    - path: "Souffleuse/Tests/SouffleuseTests/CompletionCacheTests.swift"
      provides: "FIFO eviction + fingerprint invalidation + decideExtendTrim correctness tests"
      min_lines: 100
  key_links:
    - from: "Sources/Souffleuse/PredictorViewModel.swift"
      to: "CompletionCache"
      via: "cache.storeInCache / cache.predictCache[...] / cache.kvCacheHolder / cache.decideExtendTrim"
      pattern: "cache\\.(lookup|store|clearPredictCache|kvCacheHolder|decideExtendTrim)"
---

<objective>
Troisième boundary du split D-03 : extraire les caches (predictCache FIFO, tokenCountCache, KVCacheHolder reference, lastContextFingerprint) hors PVM dans `@MainActor final class CompletionCache`. Inclure le KV cache decision tree (PVM:1163-1311) sous forme d'un `KVDecision` enum + `decideExtendTrimInvalidate(...)` pure function.

**Critique** : le literal env var `SOUFFLEUSE_DISABLE_KV_CACHE` DOIT rester byte-identique (rollback safety net Phase 3). Le `KVCacheBypassFlag` migre de PVM vers CompletionCache.swift.

Output : `CompletionCache.swift` (~250 LOC) consolidant tout le state cache cross-keystroke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cascade-quality-architecture/04-RESEARCH.md
@.planning/phases/04-cascade-quality-architecture/04-PATTERNS.md
@.planning/phases/04-cascade-quality-architecture/04-02-SUMMARY.md
@.planning/phases/04-cascade-quality-architecture/04-03-SUMMARY.md

<interfaces>
```swift
enum KVDecision: Sendable, Equatable {
    case bypass
    case cold
    case fingerprintChanged
    case extend(addedTokens: Int)
    case trim(removedTokens: Int)
    case diverged
    case identical
}

@MainActor
final class CompletionCache {
    let kvCacheHolder: KVCacheHolder
    var tokenCountCache: TokenCountCache { get }

    init()

    func lookup(userTail: String) -> String?
    func store(prefix: String, suggestion: String)
    func clearPredictCache()
    func longestExtendingKey(userTail: String) -> (key: String, suggestion: String)?

    @discardableResult
    func updateContextFingerprint(_ fp: String) -> Bool

    func decideExtendTrimInvalidate(
        invariance: InvariancePrefix,
        userTailTokenCount: Int,
        promptTokens: Int
    ) -> KVDecision

    func storeCaches(_ caches: [Any], fingerprint: String, beforeCursorTokens: Int)
    func invalidate(reason: KVCacheHolder.InvalidationReason)
    func invalidateAll()
}
```

Env var literal MUST stay byte-identical : `SOUFFLEUSE_DISABLE_KV_CACHE`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer CompletionCache.swift (FIFO + tokenCountCache + fingerprint + KV bypass flag + decideExtendTrim)</name>
  <files>Souffleuse/Sources/Souffleuse/CompletionCache.swift</files>
  <read_first>
    - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift (lignes 31-34, 64-72, 126-131, 139, 149-158, 220-240, 492-503, 666-692, 1163-1311)
    - Souffleuse/Sources/Souffleuse/KVCacheHolder.swift (intégral)
    - Souffleuse/Sources/SouffleusePrompt/MemoizingTokenCounter.swift (TokenCountCache definition)
    - .planning/phases/04-cascade-quality-architecture/04-PATTERNS.md (§"CompletionCache.swift" lignes 288-385)
    - .planning/phases/04-cascade-quality-architecture/04-RESEARCH.md (§"Concrete PVM-region mapping" cache rows)
  </read_first>
  <action>
    Créer `Souffleuse/Sources/Souffleuse/CompletionCache.swift`.

    Imports :
    ```swift
    import Foundation
    import MLXLMCommon       // KVCache
    import SouffleusePrompt  // TokenCountCache
    import SouffleuseLog
    ```

    1. **`private enum KVCacheBypassFlag`** — migration verbatim de PVM:31-34. Env var literal `SOUFFLEUSE_DISABLE_KV_CACHE` EXACTEMENT préservé (rollback safety net).

    2. **`enum KVDecision: Sendable, Equatable`** — 7 cas : bypass, cold, fingerprintChanged, extend(addedTokens: Int), trim(removedTokens: Int), diverged, identical. Equatable conformance pour tests.

    3. **`@MainActor final class CompletionCache`** :

       Propriétés :
       - `private var predictCache: [String: String] = [:]`
       - `private var predictCacheOrder: [String] = []`
       - `private static let predictCacheCapacity = 32`
       - `let tokenCountCache = TokenCountCache(cap: 64)`
       - `let kvCacheHolder = KVCacheHolder()`
       - `private var lastContextFingerprint: String?`

       API :
       - `init() {}`
       - `func lookup(userTail: String) -> String? { predictCache[userTail] }`
       - `func store(prefix: String, suggestion: String)` — FIFO eviction verbatim PVM:220-240 (idempotent : si clé existe déjà, no-op)
       - `func clearPredictCache()` — vide dict + order
       - `func longestExtendingKey(userTail: String) -> (key: String, suggestion: String)?` — migré de PVM:666-692. Réplique la logique du undo-as-ghost : itère l'order, trouve la clé qui a `userTail` comme préfixe et qui est la plus longue. Retourne `(key, predictCache[key]!)`.
       - `@discardableResult func updateContextFingerprint(_ fp: String) -> Bool` — verbatim PVM:492-503. Si changement : `clearPredictCache()` + `Log.info(.predictor, "cache_invalidate_context")` + return true. Sinon update + return false.
       - `func storeCaches(_ caches: [Any], fingerprint: String, beforeCursorTokens: Int)` — délègue au `kvCacheHolder.storeCaches(...)`. **Vérifier l'API exacte du holder** (lire KVCacheHolder.swift) et adapter — si holder expose `storeCaches(_:fingerprint:beforeCursorTokens:)` directement, l'appeler ; sinon adapter.
       - `func invalidate(reason: KVCacheHolder.InvalidationReason)` — délègue.
       - `func invalidateAll()` — `clearPredictCache()` + `invalidate(reason: .explicit)` + `Log.info(.predictor, "kv_cache_invalidate", count: 3)`. Compose les 3 actions de PVM swapModel L201-209.

    4. **`func decideExtendTrimInvalidate(invariance: InvariancePrefix, userTailTokenCount: Int, promptTokens: Int) -> KVDecision`** — migré de PVM:1163-1311.

       Ordre EXACT (Pitfall 4 RESEARCH §"Common Pitfalls") :
       ```
       1. if KVCacheBypassFlag.enabled → .bypass
       2. if kvCacheHolder.caches == nil → .cold
       3. if kvCacheHolder.fingerprint != invariance.fingerprint → .fingerprintChanged
       4. let prior = kvCacheHolder.beforeCursorTokens
          let delta = userTailTokenCount - prior
          if delta == 0 → .identical
          if delta > 0 → .extend(addedTokens: delta)
          if delta < 0:
              if abs(delta) <= MAX_TRIM_TOKENS → .trim(removedTokens: abs(delta))
              else → .diverged
       ```

       Lire PVM:1163-1311 pour la valeur exacte de `MAX_TRIM_TOKENS` (ou nom équivalent). Si la constante a un autre nom (par ex. `maxTrimWindow`), la migrer verbatim et garder le même nom + valeur dans CompletionCache.swift via `private static let`.

       AUCUN log dans `decideExtendTrimInvalidate` — les logs `kv_cache_extend/trim/invalidate` sont émis CÔTÉ caller (PVM, puis ModelRuntime au plan 04-05) qui applique la décision.

    Le fichier final ~250 LOC.
  </action>
  <verify>
    <automated>swift build --package-path Souffleuse 2>&amp;1 | tail -10 ; test -f Souffleuse/Sources/Souffleuse/CompletionCache.swift</automated>
  </verify>
  <acceptance_criteria>
    - "Souffleuse/Sources/Souffleuse/CompletionCache.swift exists"
    - "grep -q 'private enum KVCacheBypassFlag' Souffleuse/Sources/Souffleuse/CompletionCache.swift"
    - "grep -q 'SOUFFLEUSE_DISABLE_KV_CACHE' Souffleuse/Sources/Souffleuse/CompletionCache.swift"
    - "grep -q 'enum KVDecision: Sendable, Equatable' Souffleuse/Sources/Souffleuse/CompletionCache.swift"
    - "grep -q 'final class CompletionCache' Souffleuse/Sources/Souffleuse/CompletionCache.swift"
    - "grep -q 'func lookup(userTail' Souffleuse/Sources/Souffleuse/CompletionCache.swift"
    - "grep -q 'func store(prefix' Souffleuse/Sources/Souffleuse/CompletionCache.swift"
    - "grep -q 'func clearPredictCache' Souffleuse/Sources/Souffleuse/CompletionCache.swift"
    - "grep -q 'func longestExtendingKey' Souffleuse/Sources/Souffleuse/CompletionCache.swift"
    - "grep -q 'func updateContextFingerprint' Souffleuse/Sources/Souffleuse/CompletionCache.swift"
    - "grep -q 'func decideExtendTrimInvalidate' Souffleuse/Sources/Souffleuse/CompletionCache.swift"
    - "grep -q 'predictCacheCapacity = 32' Souffleuse/Sources/Souffleuse/CompletionCache.swift"
    - "swift build --package-path Souffleuse exits 0"
  </acceptance_criteria>
  <done>CompletionCache compile, KVCacheBypassFlag migré avec env var byte-identique, decideExtendTrim implémenté.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CompletionCache dans PredictorViewModel — supprimer caches inlined</name>
  <files>Souffleuse/Sources/Souffleuse/PredictorViewModel.swift</files>
  <read_first>
    - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift (sections 31-34, 64-72, 126-158, 195-214, 220-240, 492-503, 666-692, 1163-1311)
    - Souffleuse/Sources/Souffleuse/CompletionCache.swift (post Task 1)
  </read_first>
  <action>
    Modifier PVM pour déléguer au `CompletionCache` :

    1. Ajouter `private let cache = CompletionCache()` près des autres deps (post-04-02 : à côté de policy + planner).

    2. **Supprimer** :
       - `private enum KVCacheBypassFlag` (PVM:31-34) — fully removed (unique source = CompletionCache.swift).
       - `private var predictCache`, `predictCacheOrder`, `predictCacheCapacity` (PVM:126-131).
       - `private let tokenCountCache` (PVM:139).
       - `private var lastContextFingerprint` (PVM:149).
       - `private let sessionCacheHolder = KVCacheHolder()` (PVM:158).

    3. **Refactor call-sites** :
       - `storeInCache(prefix:suggestion:)` → `cache.store(prefix:suggestion:)`
       - Lookups `predictCache[userTail]` → `cache.lookup(userTail: userTail)`
       - `clearPredictCache()` interne → `cache.clearPredictCache()`
       - Undo-as-ghost (PVM:666-692) → `cache.longestExtendingKey(userTail: userTail)`
       - Context fingerprint (PVM:492-503) → `cache.updateContextFingerprint(contextFingerprint)`. ATTENTION : NE PAS dupliquer le `Log.info(.predictor, "cache_invalidate_context")` (déjà émis par updateContextFingerprint).
       - KV decision tree (PVM:1163-1311) → `let decision = cache.decideExtendTrimInvalidate(...)` + switch sur `decision`. Les `Log.info(.predictor, "kv_cache_extend/trim/invalidate", count: N)` restent côté PVM pour l'instant (migrent en ModelRuntime au plan 04-05).
       - `swapModel(to:)` (PVM:195-214) → remplacer le triplet `sessionCacheHolder.invalidate(.explicit) + clearPredictCache() + Log.info(.predictor, "kv_cache_invalidate", count: 3)` par `cache.invalidateAll()` (qui fait les 3).
       - `sessionCacheHolder` references → `cache.kvCacheHolder`. Cela inclut le `container.perform` closure (PVM:1009-1412) qui lit `sessionCacheHolder.caches` → `cache.kvCacheHolder.caches`.
       - `tokenCountCache` references → `cache.tokenCountCache`.

    4. Build + test full suite.
  </action>
  <verify>
    <automated>swift build --package-path Souffleuse 2>&amp;1 | tail -10 ; swift test --package-path Souffleuse 2>&amp;1 | grep -E 'Test run with' | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - "grep -q 'private let cache = CompletionCache' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift"
    - "grep -c 'private enum KVCacheBypassFlag' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift returns 0"
    - "grep -c 'private var predictCache:' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift returns 0"
    - "grep -c 'private let sessionCacheHolder' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift returns 0"
    - "grep -c 'SOUFFLEUSE_DISABLE_KV_CACHE' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift returns 0"
    - "grep -c 'SOUFFLEUSE_DISABLE_KV_CACHE' Souffleuse/Sources/Souffleuse/CompletionCache.swift returns 1"
    - "grep -cE 'cache\\.(lookup|store|kvCacheHolder|decideExtendTrim|updateContextFingerprint|invalidateAll)' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift returns >=5"
    - "swift build --package-path Souffleuse exits 0"
    - "swift test --package-path Souffleuse exits 0"
  </acceptance_criteria>
  <done>PVM ne possède plus aucun cache inlined. Env var literal byte-identique préservé exactly once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CompletionCacheTests.swift — FIFO + fingerprint + decideExtendTrim</name>
  <files>Souffleuse/Tests/SouffleuseTests/CompletionCacheTests.swift</files>
  <read_first>
    - Souffleuse/Tests/SouffleuseTests/KVCacheHolderTests.swift
    - Souffleuse/Tests/SouffleuseTests/KVCacheBypassTests.swift
    - Souffleuse/Sources/Souffleuse/CompletionCache.swift (post Tasks 1-2)
    - Souffleuse/Sources/Souffleuse/KVCacheHolder.swift (pour construire fixtures InvariancePrefix)
  </read_first>
  <behavior>
    - fifoEvictionAtCapacity32 : insère 33 items, l'item le plus ancien est evicted
    - storeIsIdempotent : store(p, s1) puis store(p, s2) → cache toujours s1 (per PVM logic)
    - clearPredictCacheEmptiesAll : clearPredictCache vide
    - longestExtendingKeyFindsLongest : cache "abc"→x, "abcdef"→y → longestExtendingKey("ab") returns ("abcdef", y)
    - longestExtendingKeyReturnsNilIfNoExtension : userTail trop long ou pas de match → nil
    - updateContextFingerprintNoChangeReturnsFalse : fp identique → preserve cache + return false
    - updateContextFingerprintChangeClearsAndReturnsTrue : fp différent → clear cache + return true
    - decideColdWhenHolderEmpty : cache fresh → decideExtendTrim returns .cold
    - decideFingerprintChangedWhenMismatch : populate holder via storeCaches, query avec fingerprint différent → .fingerprintChanged
    - decideIdenticalWhenZeroDelta : populate holder beforeCursorTokens=N, query userTailTokenCount=N → .identical
    - decideExtendWhenPositiveDelta : query userTailTokenCount=N+5 → .extend(5)
    - decideTrimWhenSmallNegativeDelta : query userTailTokenCount=N-3 → .trim(3) (selon MAX_TRIM_TOKENS)
  </behavior>
  <action>
    Créer `Souffleuse/Tests/SouffleuseTests/CompletionCacheTests.swift`.

    Header :
    ```swift
    import Testing
    import Foundation
    import MLXLMCommon
    @testable import Souffleuse

    @MainActor
    @Suite("Phase 4 — CompletionCache FIFO + fingerprint + KV decision")
    struct CompletionCacheTests {
        // ...
    }
    ```

    Implémenter tous les tests du `<behavior>` block. ≥10 tests.

    **Fixture helper** pour construire un `InvariancePrefix` : lire `KVCacheHolder.swift` pour la signature exacte de `InvariancePrefix.init`. Si l'init accepte des paramètres comme `(systemPrompt: String, contextPrefix: String, ...)`, créer une closure `static func makeInvariance(fingerprint: String? = nil) -> InvariancePrefix` qui retourne une instance avec un fingerprint déterministe — modifiable via le param.

    Pour les tests `decideFingerprintChangedWhenMismatch`, `decideIdenticalWhenZeroDelta`, `decideExtendWhenPositiveDelta`, `decideTrimWhenSmallNegativeDelta` : populer le holder via `cache.storeCaches([], fingerprint: "fp-A", beforeCursorTokens: 50)` (adapter selon l'API réelle du KVCacheHolder), puis appeler `decideExtendTrimInvalidate(...)` avec différents inputs.

    Note pour bypass : ne PAS écrire de test `decideBypassWhenEnvVarSet` ici — le test KVCacheBypassTests existant couvre déjà ce path. Documenter dans le file header : `// NOTE: bypass behavior covered by existing KVCacheBypassTests.swift`.
  </action>
  <verify>
    <automated>swift test --package-path Souffleuse --filter CompletionCacheTests 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - "Souffleuse/Tests/SouffleuseTests/CompletionCacheTests.swift exists"
    - "grep -c '@Test' Souffleuse/Tests/SouffleuseTests/CompletionCacheTests.swift returns >=10"
    - "grep -q '@MainActor' Souffleuse/Tests/SouffleuseTests/CompletionCacheTests.swift"
    - "grep -q 'decideExtendTrimInvalidate' Souffleuse/Tests/SouffleuseTests/CompletionCacheTests.swift"
    - "grep -q 'updateContextFingerprint' Souffleuse/Tests/SouffleuseTests/CompletionCacheTests.swift"
    - "swift test --package-path Souffleuse --filter CompletionCacheTests exits 0"
  </acceptance_criteria>
  <done>10+ tests cache verts.</done>
</task>

<task type="auto">
  <name>Task 4: Audit + replay equivalence + full tests</name>
  <files>.planning/phases/04-cascade-quality-architecture/04-04-REPLAY-DIFF.md</files>
  <read_first>
    - .planning/phases/04-cascade-quality-architecture/04-02-BASELINE-REPLAY.md
    - Souffleuse/audit.sh
  </read_first>
  <action>
    Gates :

    1. `bash Souffleuse/audit.sh` — exit 0. CompletionCache émet `cache_invalidate_context` et `kv_cache_invalidate count:3`, tous deux StaticString count-only.

    2. `swift test --package-path Souffleuse` — exit 0. ≥192 tests (182 post-04-03 + ≥10 CompletionCache).

    3. Replay equivalence — Pitfall 4 critique : un changement subtil dans l'ordre des invalidations ou la canonicalisation du fingerprint casserait le KV decision verdict. Re-run replay, diff vs baseline (`04-02-BASELINE-REPLAY.md`), capturer dans `04-04-REPLAY-DIFF.md` avec verdict. Si REGRESSION : vérifier l'ordre exact bypass→cold→fingerprintChanged→delta dans `decideExtendTrimInvalidate`.
  </action>
  <verify>
    <automated>bash Souffleuse/audit.sh && swift test --package-path Souffleuse 2>&amp;1 | grep -E 'Test run with' && test -f .planning/phases/04-cascade-quality-architecture/04-04-REPLAY-DIFF.md</automated>
  </verify>
  <acceptance_criteria>
    - "bash Souffleuse/audit.sh exits 0"
    - "swift test --package-path Souffleuse exits 0"
    - "swift test output contains 'Test run with' followed by integer >= 192"
    - "04-04-REPLAY-DIFF.md exists"
    - "grep 'REGRESSION DETECTED' .planning/phases/04-cascade-quality-architecture/04-04-REPLAY-DIFF.md returns 0"
  </acceptance_criteria>
  <done>Plan 04-04 complet. CompletionCache extrait, env var byte-identique, decideExtendTrim ε-équivalent, tests verts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @MainActor (cache state) | Tout sur MainActor — pas de cross-actor transfer dans ce module |
| KVCache references → container.perform | CompletionCache fournit kvCacheHolder ; le caller (PVM puis ModelRuntime) crée le CacheBox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | Env var literal changed by accident | mitigate | Acceptance criteria : grep -c `SOUFFLEUSE_DISABLE_KV_CACHE` Sources/Souffleuse/CompletionCache.swift returns 1 ; PVM returns 0. |
| T-04-04-02 | Tampering | Replay equivalence broken by reordered invalidation (Pitfall 4) | mitigate | Order verbatim PVM:1163-1311. Task 4 diff vs baseline. |
| T-04-04-03 | Information Disclosure | Aucun nouveau log avec user fields | accept | `cache_invalidate_context` et `kv_cache_invalidate` sont StaticString existants. |

**audit.sh invariants préservés :** aucun nouveau Log call user-data, aucune lecture history.aes, aucun print/NSLog. Check 6 OK by construction.
</threat_model>

<verification>
- CompletionCache + KVDecision créés
- PVM ne possède plus aucun cache inlined ni KVCacheBypassFlag
- Env var `SOUFFLEUSE_DISABLE_KV_CACHE` byte-identique dans CompletionCache.swift exactly once
- ≥10 tests cache verts
- Audit 6/6, ≥192 tests total, replay équivalent
</verification>

<success_criteria>
1. `swift build` exit 0
2. `swift test` exit 0, ≥192 tests
3. `bash Souffleuse/audit.sh` exit 0
4. CompletionCache + KVDecision en place ; PVM allégé
5. Env var literal `SOUFFLEUSE_DISABLE_KV_CACHE` apparaît exactement 1 fois (CompletionCache.swift)
6. 04-04-REPLAY-DIFF.md : verdict EQUIVALENT
</success_criteria>

<output>
After completion, create `.planning/phases/04-cascade-quality-architecture/04-04-SUMMARY.md` listing :
- CompletionCache LOC
- PVM LOC before/after
- Tests added (≥10)
- KVCacheBypassFlag migration confirmation (env var grep counts)
- Replay verdict
</output>

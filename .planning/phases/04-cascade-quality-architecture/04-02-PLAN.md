---
phase: 04-cascade-quality-architecture
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift
  - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift
  - Souffleuse/Tests/SouffleuseTests/SuggestionPolicyTests.swift
  - Souffleuse/Tests/SouffleuseTests/ClassificationGridTests.swift
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
tags:
  - phase-04
  - split-pvm
  - suggestion-policy
  - classification-grid

must_haves:
  truths:
    - "SuggestionPolicyEngine owns currentGhost/currentSource/currentScore/shownAt state — extracted from PVM cascade region."
    - "routeInstant(userTail:historySnapshot:wordCompleter:) returns Optional<GhostUpdate> per cascade matrix D-08."
    - "onLLMChunk(_:userTail:) applies Relevance Gate replacement bar AND emits ghost_classified_parasite when replacement within parasiteWindow."
    - "endLifecycle(reason:) is the SINGLE call-site emitting the 5 ghost_classified_* events — at most one event per ghost lifetime."
    - "History exact-match L1 is gated by afterSpaceL1Bar (0.4) — re-enabled per D-08."
    - "Le replay SouffleuseCoherence (sans --replay extension, juste la baseline command) reste exécutable et produit des outputs ε-identiques à HEAD pré-plan."
  artifacts:
    - path: "Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
      provides: "@MainActor final class SuggestionPolicyEngine + routeInstant + onLLMChunk + endLifecycle + LifecycleEndReason enum + GhostUpdate struct"
      min_lines: 350
    - path: "Souffleuse/Sources/Souffleuse/PredictorViewModel.swift"
      provides: "Cascade region L525-913 délégué à SuggestionPolicyEngine — PVM rétrécit"
    - path: "Souffleuse/Tests/SouffleuseTests/SuggestionPolicyTests.swift"
      provides: "Tests routeInstant truth-table (9 rows D-08) + onLLMChunk Relevance Gate replacement bar + L1 re-enable derrière le Gate"
      min_lines: 180
    - path: "Souffleuse/Tests/SouffleuseTests/ClassificationGridTests.swift"
      provides: "Tests endLifecycle — 1 event par lifecycle, mutually exclusive categories"
      min_lines: 80
  key_links:
    - from: "Sources/Souffleuse/PredictorViewModel.swift"
      to: "SuggestionPolicyEngine"
      via: "Owned by PVM, called from predict() / onChunk"
      pattern: "policy\\.route|policy\\.onLLMChunk|policy\\.endLifecycle"
    - from: "Sources/Souffleuse/SuggestionPolicy.swift"
      to: "Log.info(.predictor, \"ghost_classified_*\", count:)"
      via: "endLifecycle(reason:)"
      pattern: "ghost_classified_(correct|acceptable|useless|bad|parasite)"
---

<objective>
Extraire la cascade routing + Relevance Gate + classification grid de `PredictorViewModel` (PVM:504-913, ~410 LOC) dans une nouvelle classe `@MainActor final class SuggestionPolicyEngine` dans `SuggestionPolicy.swift` (qui contient déjà l'enum namespace + le scorer pur).

Cette extraction est le **PREMIER MODULE** du split D-03 (per recommandation RESEARCH §"Open Questions" Q3 + planning_directives wave 2). Une fois Policy en place, le Gate + classification + L1 re-enable sont déjà actifs ; les 3 autres extractions (Planner, Cache, Runtime) deviennent mécaniques.

**Migration in-place atomique (D-02)** : PVM délègue à Policy au lieu d'inliner. Pas de feature flag. Filet de sécurité : suite de tests doit rester verte + replay equivalence à la fin.

Purpose : faire émerger la frontière où vit le Ghost Relevance Gate avant tout autre déplacement. Délivre directement D-05..D-11 (Gate + classification grid) en plus de la première moitié de D-03.

Output : `SuggestionPolicyEngine` class + nouvelle classification grid émission (5 events) + L1 history re-enabled derrière le Gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-cascade-quality-architecture/04-CONTEXT.md
@.planning/phases/04-cascade-quality-architecture/04-RESEARCH.md
@.planning/phases/04-cascade-quality-architecture/04-PATTERNS.md
@.planning/phases/04-cascade-quality-architecture/04-01-SUMMARY.md

<interfaces>
<!-- Types created here, consumed by PVM façade and downstream plans (04-03, 04-04, 04-05). -->

From Sources/Souffleuse/SuggestionPolicy.swift (NEW types in this plan):
```swift
struct GhostUpdate: Sendable, Equatable {
    let text: String
    let source: SuggestionSource
    let score: Score
}

enum LifecycleEndReason: Sendable {
    case acceptedFull
    case acceptedPartial(chunks: Int)
    case dismissedByEsc
    case typedPastWithoutOverlap
    case typedDiverged
    case replacedByOther
    case replacedByOtherStable
    case modelSwap
    case focusChange
    case blocklist
}

@MainActor
final class SuggestionPolicyEngine {
    var currentGhost: String { get }
    var currentSource: SuggestionSource { get }
    var currentScore: Score { get }

    init(maxWords: Int)
    func updateMaxWords(_ n: Int)

    func routeInstant(
        userTail: String,
        historySnapshot: [TypingHistoryEntry],
        wordCompleter: WordCompleter
    ) -> GhostUpdate?

    func onLLMChunk(_ chunk: String, userTail: String) -> GhostUpdate?

    func applyGhost(_ text: String, source: SuggestionSource, score: Score)

    func endLifecycle(reason: LifecycleEndReason)

    func reset()
}
```

From existing (referenced):
- `WordCompleter` in `SouffleuseTyping`
- `TypingHistoryEntry` in `SouffleusePersonalization`
- `Log.info(_:_:count:)` in `SouffleuseLog`
- `Score`, `SuggestionSource`, `SuggestionPolicy.Tuning`, `SuggestionPolicy.score(...)` from Plan 01
- `historyExactSubstringMatch` migrating verbatim from PVM:1524-1549
- `capToWords` migrating verbatim from PVM:415-433 (note: stays accessible — PVM:415-433 stays for now ; copy/reference par static)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline replay equivalence capture (pré-extraction)</name>
  <files>.planning/phases/04-cascade-quality-architecture/04-02-BASELINE-REPLAY.md</files>
  <read_first>
    - Souffleuse/Sources/SouffleuseCoherence/main.swift (lignes 1-100 — comprendre la commande --replay et son output)
    - .planning/phases/03-perf-kv-cache/03-04-PLAN.md (si existe — protocole replay equivalence Phase 3)
    - .planning/phases/04-cascade-quality-architecture/04-RESEARCH.md (§"Pattern 4: Atomic-commit per boundary + replay equivalence" lignes 320-326)
  </read_first>
  <action>
    Établir la baseline de replay equivalence AVANT toute modification de la cascade.

    1. Build le binaire `SouffleuseCoherence` : `swift build --package-path Souffleuse --product SouffleuseCoherence`. Doit exit 0.

    2. Lister les scenarios de replay actuels :
       ```
       ls Souffleuse/Sources/SouffleuseCoherence/replay-scenarios.json 2>/dev/null || find Souffleuse -name 'replay-scenarios*.json' | head -3
       ```
       Identifier le path canonique (probablement `Souffleuse/Sources/SouffleuseCoherence/replay-scenarios.json`).

    3. Lancer le replay et capturer l'output dans le baseline file :
       ```
       swift run --package-path Souffleuse SouffleuseCoherence --replay > .planning/phases/04-cascade-quality-architecture/04-02-BASELINE-REPLAY.txt 2>&1
       ```

       Si le binaire prend un argument différent (vérifier `main.swift` pour la signature exacte), adapter la commande. **Si `--replay` n'existe pas ou ne tourne pas hors-MLX**, marquer la baseline comme TEXTUAL_BUILD_ONLY : capturer juste l'output du `swift build` + un hash sha256 du fichier `replay-scenarios.json` (si présent) comme baseline minimale.

    4. Créer `.planning/phases/04-cascade-quality-architecture/04-02-BASELINE-REPLAY.md` avec :
       - Date + commit hash (`git rev-parse HEAD`)
       - Commande utilisée
       - Hash sha256 du fichier scenarios (s'il existe)
       - Nombre de scenarios processés
       - Pour CHAQUE scenario : id + ghost output produit (premier 80 chars suffisent — c'est l'equivalence target)
       - Si replay binaire non fonctionnel hors-MLX live : noter explicitement `MODE: BUILD-ONLY BASELINE` + lister les commandes alternatives utilisées pour valider l'extraction (suite de tests + audit).

    Ce fichier sert de point de comparaison à la Task 6 (post-extraction replay).
  </action>
  <verify>
    <automated>test -f .planning/phases/04-cascade-quality-architecture/04-02-BASELINE-REPLAY.md && wc -l .planning/phases/04-cascade-quality-architecture/04-02-BASELINE-REPLAY.md | awk '{print $1}' | xargs -I{} test {} -ge 5</automated>
  </verify>
  <acceptance_criteria>
    - "04-02-BASELINE-REPLAY.md exists with at least 5 lines"
    - "grep -q 'commit' .planning/phases/04-cascade-quality-architecture/04-02-BASELINE-REPLAY.md"
    - "swift build --package-path Souffleuse exits 0 (recorded in baseline)"
  </acceptance_criteria>
  <done>Baseline pré-extraction commité. Task 6 (post-extraction) pourra comparer.</done>
</task>

<task type="auto">
  <name>Task 2: Étendre SuggestionPolicy.swift — class SuggestionPolicyEngine + GhostUpdate + LifecycleEndReason + routeInstant</name>
  <files>Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift</files>
  <read_first>
    - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift (lignes 95-109 SuggestionSource ; 504-609 cascade L0/L1 instant ghost ; 666-715 undo-as-ghost + hasFieldHint gate ; 1524-1549 historyExactSubstringMatch ; 415-433 capToWords)
    - Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift (le fichier actuel — Score + scorer + Tuning extension déjà présents)
    - Souffleuse/Sources/SouffleuseTyping/WordCompleter.swift (API : `wordCompleter.completion(for: userTail) -> String?`)
    - Souffleuse/Sources/SouffleusePersonalization/TypingHistoryStore.swift (TypingHistoryEntry shape)
    - .planning/phases/04-cascade-quality-architecture/04-RESEARCH.md (§"Cascade routing decision matrix" lignes 552-666, §"Classification grid emission points" lignes 745-816)
    - .planning/phases/04-cascade-quality-architecture/04-PATTERNS.md (§"SuggestionPolicy.swift" lignes 33-162)
  </read_first>
  <action>
    Étendre `Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift` (créé en Plan 01) avec une nouvelle classe `@MainActor final class SuggestionPolicyEngine` + types associés. NE PAS supprimer l'enum namespace `SuggestionPolicy` (il porte les pure functions et le Tuning extension).

    Structure du fichier après cette task :

    1. Imports (préserver Plan 01 + ajouter ce qui manque) :
       ```swift
       import Foundation
       import SouffleuseLog
       import SouffleusePersonalization
       import SouffleuseTyping
       ```

    2. **Garder verbatim** : `enum SuggestionSource`, `struct Score`, `enum SuggestionPolicy { static func score, prefixFit, lengthFit }`, extension Tuning (déjà fichier séparé).

    3. **Ajouter** `struct GhostUpdate: Sendable, Equatable` avec `let text: String`, `let source: SuggestionSource`, `let score: Score`.

    4. **Ajouter** `enum LifecycleEndReason: Sendable` avec exactement les 10 cas listés dans `<interfaces>` ci-dessus (acceptedFull, acceptedPartial(chunks: Int), dismissedByEsc, typedPastWithoutOverlap, typedDiverged, replacedByOther, replacedByOtherStable, modelSwap, focusChange, blocklist).

    5. **Ajouter** `@MainActor final class SuggestionPolicyEngine` avec :

       Propriétés state (private(set) pour read externe) :
       - `private(set) var currentGhost: String = ""`
       - `private(set) var currentSource: SuggestionSource = .none`
       - `private(set) var currentScore: Score = Score(sourcePrior: 0, prefixFit: 0, lengthFit: 0)`
       - `private(set) var shownAt: Date? = nil`
       - `private var lastReplacedSource: SuggestionSource = .none`
       - `private var maxWords: Int`

       Init :
       - `init(maxWords: Int)` initialise `self.maxWords = maxWords`.

       API publique :

       a) `func updateMaxWords(_ n: Int) { self.maxWords = n }` — appelé quand PreferencesStore change.

       b) `func routeInstant(userTail: String, historySnapshot: [TypingHistoryEntry], wordCompleter: WordCompleter) -> GhostUpdate?`
          — Reproduit la cascade L0/L1 instant de PVM:539-609 + cascade matrix D-08 :
          - Cas mid-word (`userTail.last?.isLetter == true`) : L0 exclusif. Appel `wordCompleter.completion(for: userTail)`. Si complétion ≥ 3 chars : compute `score = SuggestionPolicy.score(source: .wordComplete, ghost: completion, userTail: userTail)`. Si `score.passesGate` → retourne `GhostUpdate(text: completion, source: .wordComplete, score: score)` + `Log.info(.predictor, "ghost_word_complete", count: completion.count)`. Sinon nil.
          - Cas after-space (`userTail.last?.isWhitespace == true` OU `userTail.isEmpty`) : L1 first.
            - Appel `Self.historyExactSubstringMatch(userTail: userTail, snapshot: historySnapshot)`.
            - Si match : `let capped = Self.capToWords(match, max: maxWords)`. Compute `score = SuggestionPolicy.score(source: .history, ghost: capped, userTail: userTail)`. Si `score.value >= SuggestionPolicy.Tuning.afterSpaceL1Bar` (D-08 0.4) → retourne `GhostUpdate(text: capped, source: .history, score: score)` + `Log.info(.predictor, "ghost_history_match", count: capped.count)`.
            - Sinon nil (L2 fillera via le stream).
          - Tout autre cas (ex. caret juste après un terminator non-whitespace) : nil.

       c) `func onLLMChunk(_ chunk: String, userTail: String) -> GhostUpdate?`
          — Reproduit la décision PVM:853-913 mais REMPLACE l'ancienne anti-churn high/low par le Relevance Gate (D-07) :
          - Si mid-word : log `Log.info(.predictor, "ghost_gate_block_midword", count: chunk.count)` + return nil (D-08 mid-word = L0 only).
          - Compute `score = SuggestionPolicy.score(source: .llm, ghost: chunk, userTail: userTail)`.
          - Si `!score.passesGate` : log `Log.info(.predictor, "ghost_gate_block", count: Int(score.value * 100))` + return nil.
          - Si `!currentGhost.isEmpty` : test replacement bar.
            - `let isHistoryFirst = (currentSource == .history)`
            - `let beatsBar = score.beats(currentScore)`
            - `let l2Upgrades = isHistoryFirst && (score.value >= currentScore.value + SuggestionPolicy.Tuning.l2UpgradeDelta)`
            - Si `!(beatsBar || l2Upgrades)` : log `Log.info(.predictor, "ghost_keep_under_bar", count: currentGhost.count)` + return nil.
            - Si `let shown = shownAt, Date().timeIntervalSince(shown) < SuggestionPolicy.Tuning.parasiteWindow` : émettre `ghost_classified_parasite` (D-09/D-10 — la classification émise au moment du remplacement) :
              `Log.info(.predictor, "ghost_classified_parasite", count: Int(Date().timeIntervalSince(shown) * 1000))`
              **Important** : ce log est l'UNIQUE classification émise pour le ghost remplacé. Setter `currentGhost = ""` + reset state via `applyGhost(...)` (qui se charge de mettre à jour `lastReplacedSource = currentSource` AVANT d'écraser).
          - Retourner `GhostUpdate(text: chunk, source: .llm, score: score)`. Le caller (PVM façade ou GenerationPlanner futur) appellera `applyGhost(...)` pour effectivement set le state.

       d) `func applyGhost(_ text: String, source: SuggestionSource, score: Score)`
          — Met à jour atomiquement le state :
          - `lastReplacedSource = currentSource`
          - `currentGhost = text`
          - `currentSource = source`
          - `currentScore = score`
          - `shownAt = Date()`

       e) `func endLifecycle(reason: LifecycleEndReason)`
          — SINGLE CALL-SITE pour les 5 events `ghost_classified_*`. Logique RESEARCH §"Mécanisme proposé" lignes 772-799 verbatim :
          ```swift
          guard !currentGhost.isEmpty, let shownAt else { return }
          let visibleMs = Int(Date().timeIntervalSince(shownAt) * 1000)
          switch reason {
          case .acceptedFull:
              Log.info(.predictor, "ghost_classified_correct", count: visibleMs)
          case .acceptedPartial(let chunks):
              Log.info(.predictor, "ghost_classified_acceptable", count: chunks)
          case .dismissedByEsc, .typedPastWithoutOverlap:
              if visibleMs >= SuggestionPolicy.Tuning.uselessMinVisibleMs {
                  Log.info(.predictor, "ghost_classified_useless", count: visibleMs)
              }
          case .typedDiverged:
              if visibleMs <= SuggestionPolicy.Tuning.badMaxDivergeMs {
                  Log.info(.predictor, "ghost_classified_bad", count: visibleMs)
              }
          case .replacedByOther:
              Log.info(.predictor, "ghost_classified_parasite", count: visibleMs)
          case .replacedByOtherStable, .modelSwap, .focusChange, .blocklist:
              break  // silent
          }
          // Reset state — never double-emit (Pitfall 5)
          currentGhost = ""
          currentSource = .none
          currentScore = Score(sourcePrior: 0, prefixFit: 0, lengthFit: 0)
          shownAt = nil
          ```

       f) `func reset()` — réinit complet (currentGhost vide, source none, score zeros, shownAt nil, lastReplacedSource none). Appelé par `cancel()` côté PVM façade.

    6. **Ajouter** au bas du fichier, **nonisolated static helpers** migrés verbatim depuis PVM :

       a) `nonisolated static func historyExactSubstringMatch(userTail: String, snapshot: [TypingHistoryEntry]) -> String?` — copie verbatim de PVM:1524-1549. **Ne PAS supprimer encore l'original dans PVM** — l'utilisation directe par PVM cascade dans la PVM:551 reste momentanément, mais à la Task 3 on remplace les call-sites par `SuggestionPolicyEngine.historyExactSubstringMatch(...)`.

       b) `nonisolated static func capToWords(_ s: String, max: Int) -> String` — copie verbatim de PVM:415-433. Idem : on garde PVM original momentanément, Task 3 fait le remplacement.

    Le fichier final fait ~350 LOC (squelette Plan 01 ~80 LOC + extension Plan 02 ~270 LOC).
  </action>
  <verify>
    <automated>swift build --package-path Souffleuse 2>&amp;1 | tail -10 ; grep -c 'final class SuggestionPolicyEngine' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift</automated>
  </verify>
  <acceptance_criteria>
    - "grep -q '@MainActor' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'final class SuggestionPolicyEngine' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'struct GhostUpdate: Sendable, Equatable' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'enum LifecycleEndReason: Sendable' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'func routeInstant' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'func onLLMChunk' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'func endLifecycle' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'func applyGhost' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'ghost_classified_correct' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'ghost_classified_acceptable' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'ghost_classified_useless' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'ghost_classified_bad' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'ghost_classified_parasite' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'nonisolated static func historyExactSubstringMatch' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'nonisolated static func capToWords' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "swift build --package-path Souffleuse exits 0"
    - "swift test --package-path Souffleuse exits 0 (tous tests existants restent verts — pas encore de wiring PVM, just compilation)"
  </acceptance_criteria>
  <done>SuggestionPolicyEngine + GhostUpdate + LifecycleEndReason + helpers static créés. Le fichier compile. Les 151+ tests Plan 01 + baseline restent verts (parce que personne n'utilise encore Engine à part les tests — ajoutés Task 4).</done>
</task>

<task type="auto">
  <name>Task 3: Wire SuggestionPolicyEngine dans PredictorViewModel — délégation cascade + lifecycle hooks</name>
  <files>Souffleuse/Sources/Souffleuse/PredictorViewModel.swift</files>
  <read_first>
    - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift (le fichier entier — 1566 LOC ; en pratique lire 1-50, 90-140, 180-220, 474-720, 770-920, 1414-1565)
    - Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift (post Task 2)
    - .planning/phases/04-cascade-quality-architecture/04-RESEARCH.md (§"Concrete PVM-region mapping" lignes 354-405)
    - .planning/phases/04-cascade-quality-architecture/04-PATTERNS.md (§"PredictorViewModel.swift (MODIFIED)" lignes 662-697)
  </read_first>
  <action>
    Modifier `PredictorViewModel.swift` pour déléguer la cascade L0/L1, la décision Relevance Gate sur LLM chunks, et la classification grid à `SuggestionPolicyEngine`. **Migration in-place atomique (D-02)** — pas de feature flag.

    Conservé : l'API publique de PVM (`predict(...)`, `cancel()`, `swapModel(to:)`, `loadModel()`, `ingestAccepted(...)`, etc.). Les observables (`suggestion`, `suggestionSource`, etc.) restent.

    Étapes précises :

    1. **Ajouter la dépendance `SuggestionPolicyEngine`** dans PVM :
       ```swift
       private let policy = SuggestionPolicyEngine(maxWords: 16)  // initial — sync avec self.maxWords plus bas
       ```
       Placer cette déclaration près des autres deps (après `wordCompleter` à PVM:184).

    2. **Sync maxWords entre PVM et policy** :
       Quand `maxWords` change (PVM L162), appeler `policy.updateMaxWords(self.maxWords)`. Si `maxWords` est un `@ObservationIgnored` simple `var`, ajouter `didSet { policy.updateMaxWords(maxWords) }` ou (préférable) exposer `maxWords` via une fonction setter qui appelle aussi `policy.updateMaxWords`. Choisir l'approche la moins invasive — si `maxWords` est seulement set depuis l'AppDelegate, c'est trivial.

    3. **Refactor de la cascade L0/L1 instant** (PVM L525-609) :
       Remplacer le block PVM L539-586 (historyHit + wordCompletionSuffix + priority resolution) par :
       ```swift
       let routeResult = policy.routeInstant(
           userTail: prefix,
           historySnapshot: historySnapshot,
           wordCompleter: wordCompleter
       )
       let instantGhost: String = routeResult?.text ?? ""
       let instantSource: SuggestionSource = routeResult?.source ?? .none
       // Note : les `Log.info(.predictor, "ghost_history_match"/"ghost_word_complete", ...)` sont
       // maintenant émis À L'INTÉRIEUR de policy.routeInstant. Ne PAS dupliquer ici.
       ```

       **Préserver le stability gate** existant à PVM L587-609 ? Selon RESEARCH §"Cascade routing — Pitfall 1/3", le stability gate est REMPLACÉ par le Relevance Gate (D-07) — il vit maintenant dans `policy.onLLMChunk` (replacement bar) + `policy.routeInstant` (passesGate). MAIS : la cascade instant PVM:587-608 fait un "stability check" sur le ghost qui change (comparant à `suggestion`). Ce check devient redondant si on `applyGhost(...)` à la fin de la routine. **Décision** : appeler `policy.applyGhost(routeResult.text, source: routeResult.source, score: routeResult.score)` SI on décide de display ce ghost ; sinon, `policy.reset()` si on veut clear. Le PVM façade lit `self.suggestion = policy.currentGhost` après chaque appel. Préserver le check `instantGhost != suggestion` pour décider DE l'appel `applyGhost`.

       Concrètement, après le bloc routeInstant :
       ```swift
       if !instantGhost.isEmpty, let route = routeResult {
           if instantGhost != suggestion {  // changement → applyGhost (stability est dans le score replacement bar)
               policy.applyGhost(route.text, source: route.source, score: route.score)
           }
           self.suggestion = policy.currentGhost
           self.suggestionSource = policy.currentSource
       }
       ```

    4. **Refactor de l'onChunk closure** (PVM L782-913) :
       Remplacer la branche `highConfidence` (L874-898) par un appel à `policy.onLLMChunk(...)`. Le filtre (overlap strip, markup clean, repeat anti, words cap) reste à sa place pré-Gate. Schéma :
       ```swift
       let oneLine = ... // existing strip/clean/truncate logic — UNCHANGED
       let chunkUpdate = policy.onLLMChunk(oneLine, userTail: capturedPrefix)
       guard let update = chunkUpdate else { return }
       // Apply : remplace l'ancienne branche highConfidence
       policy.applyGhost(update.text, source: .llm, score: update.score)
       self.suggestion = policy.currentGhost
       self.suggestionSource = policy.currentSource
       ```

       **Important** : conserver l'anti-repeat (PVM L841 `ghostIsRepeatingPrefix`) AVANT l'appel à `policy.onLLMChunk` — c'est en amont de la décision de policy.

    5. **Hooks de classification grid** — wiring des end-of-lifecycle events :

       a) **Tab accept (full)** : quand PVM ingère une acceptation full Tab, appeler `policy.endLifecycle(reason: .acceptedFull)`. PVM:1489 `ingestAccepted(...)` est appelé depuis AppDelegate.handleKey full-accept branch. Ajouter à l'entrée de `ingestAccepted` (ou via callback explicite — selon l'archi actuelle) : `policy.endLifecycle(reason: .acceptedFull)`. **Note** : la classification du chunk partial accept est l'affaire de l'AppDelegate (qui possède le ChunkSplitter). Pour ce plan, on traite seulement le full accept ici ; le partial est wired plus tard quand TypingSession est extrait (04-04 / 04-07). Marquer en commentaire `// TODO Phase 4 04-04+ : wire partial accept ghost_classified_acceptable depuis TypingSession`.

       b) **Esc dismiss** : quand `cancel()` est appelé suite à un Esc, appeler `policy.endLifecycle(reason: .dismissedByEsc)` AVANT le reset. Comme `cancel()` est appelé de plusieurs paths (Tab accept, focus change, etc.), discriminer via un paramètre default :
          ```swift
          func cancel(reason: LifecycleEndReason = .focusChange) {
              policy.endLifecycle(reason: reason)
              // ... existing cancel logic ...
              policy.reset()
          }
          ```
          Et adapter les call-sites internes à PVM (`cancel()` interne — `cancel(reason: .focusChange)`). Les call-sites externes (AppDelegate handleKey .esc) appelleront `predictor.cancel(reason: .dismissedByEsc)`.

       c) **Model swap** : `swapModel(to:)` appelle `policy.endLifecycle(reason: .modelSwap)` AVANT le reset.

       d) **Replacement parasite** : déjà émis depuis `policy.onLLMChunk` (per Task 2 step 5c). Aucun wiring supplémentaire.

       e) **typedDiverged / typedPastWithoutOverlap** : ces classifications nécessitent un signal "le user a tapé qqch et son premier mot diverge du ghost dans X ms". Ce signal vient de `TypingSession.tick()` (extraction 04-07). Pour ce plan, **marquer TODO** : `// TODO Phase 4 04-07 : appeler policy.endLifecycle(reason: .typedDiverged / .typedPastWithoutOverlap) depuis TypingSession.tick() quand divergence détectée`.

    6. **Cleanup déduplication** :
       - PVM L539-586 (historyHit + wordCompletionSuffix branche prioritaire) : SUPPRIMER (remplacé par routeInstant).
       - PVM L874-898 (highConfidence branche + ghost_protect_high + ghost_keep_longer) : SUPPRIMER (remplacé par policy.onLLMChunk replacement bar).
       - PVM L1524-1549 (`historyExactSubstringMatch`) : SUPPRIMER (déplacé à SuggestionPolicy comme static helper).
       - PVM L415-433 (`capToWords`) : GARDER ENCORE — utilisé par d'autres call-sites internes à PVM. SuggestionPolicy.capToWords est une copie pour usage interne policy.

       **Vérifier** que les call-sites PVM internes qui appelaient `Self.historyExactSubstringMatch(...)` directement (par ex. dans le undo-as-ghost à PVM:666-692) référencent maintenant `SuggestionPolicyEngine.historyExactSubstringMatch(...)`.

    7. **Préserver le source decay** (PVM L505-521 HIGH → llm) : ce decay s'applique au début de `predict(...)`. **Décision** : MIGRER ce decay dans `policy.beginPredict()` (nouvelle méthode à ajouter en Task 2 ? Si oubliée, l'ajouter ici via un patch léger : `func beginPredict() { if currentSource == .history || currentSource == .cache || currentSource == .undoCache { currentSource = .llm } }`).

       Si Task 2 a oublié `beginPredict()`, **l'ajouter** dans SuggestionPolicy.swift en parallèle (autoriser un mini-amendement à Task 2).

       Call-site dans PVM:504 :
       ```swift
       policy.beginPredict()  // remplace PVM L505-521 verbatim
       ```

       Et SUPPRIMER PVM L505-521.

    Cette task est la plus longue. Procéder par sections, compiler à chaque section, ne pas perdre l'audit.
  </action>
  <verify>
    <automated>swift build --package-path Souffleuse 2>&amp;1 | tail -20 ; swift test --package-path Souffleuse 2>&amp;1 | grep -E 'Test run with [0-9]+ tests' | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - "grep -q 'private let policy = SuggestionPolicyEngine' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift"
    - "grep -q 'policy.routeInstant' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift"
    - "grep -q 'policy.onLLMChunk' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift"
    - "grep -q 'policy.endLifecycle' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift"
    - "grep -q 'policy.applyGhost' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift"
    - "grep -q 'policy.beginPredict' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift"
    - "grep -c 'ghost_protect_high\\|ghost_keep_longer' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift returns 0 (anciens events de l'anti-churn high/low supprimés)"
    - "grep -c 'historyExactSubstringMatch' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift returns 0 (function moved to SuggestionPolicy)"
    - "swift build --package-path Souffleuse exits 0"
    - "swift test --package-path Souffleuse exits 0 (139 baseline + ≥12 Plan 01 tests stay green)"
    - "wc -l Souffleuse/Sources/Souffleuse/PredictorViewModel.swift returns value <= 1450 (some shrinkage from cascade extraction)"
  </acceptance_criteria>
  <done>PVM délègue cascade L0/L1 + LLM Gate à SuggestionPolicyEngine ; classification events câblés sur Tab full / Esc / swapModel ; deux TODOs marqués pour TypingSession extraction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: SuggestionPolicyTests.swift — 9 rows truth-table + Gate replacement bar tests</name>
  <files>Souffleuse/Tests/SouffleuseTests/SuggestionPolicyTests.swift</files>
  <read_first>
    - Souffleuse/Tests/SouffleuseTests/HistoryExactMatchTests.swift (fixture helpers, @Suite pattern)
    - Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift (post Tasks 1-2)
    - .planning/phases/04-cascade-quality-architecture/04-RESEARCH.md (§"Cascade routing decision matrix" lignes 552-573 — la truth table 9 rows)
    - .planning/phases/04-cascade-quality-architecture/04-PATTERNS.md (§"SuggestionPolicyTests.swift" lignes 549-583)
  </read_first>
  <behavior>
    Tests routeInstant (la matrice D-08 — 9 rows) :
    - Row 1 (mid-word, L0 only) : userTail finit par lettre, wordCompleter retourne suffix ≥3 chars → GhostUpdate.source == .wordComplete
    - Row 2 (mid-word, cache hit) : couvert au plan 04-04 (CompletionCache) — skip ici
    - Row 3 (mid-word, L2 chunk arrive) : `policy.onLLMChunk(chunk, userTail: "Bonjou")` retourne nil + log ghost_gate_block_midword
    - Row 4 (after-space, L1 hit qualif) : userTail finit par espace, history matche, score ≥ 0.4 → GhostUpdate.source == .history
    - Row 5 (after-space, L1 + L2 upgrade) : currentSource=.history avec score X, onLLMChunk avec score >= X+0.15 → returns non-nil (upgrade applied)
    - Row 5bis (after-space, L1 + L2 no upgrade) : onLLMChunk avec score < X+0.15 → returns nil (L1 kept)
    - Row 6 (after-space, L2 only) : currentGhost vide, onLLMChunk avec score passesGate → returns non-nil
    - Row 7 (after-space, rien) : userTail empty, no history → routeInstant returns nil
    - Row 8 (under gate) : onLLMChunk avec score < 0.25 → returns nil + ghost_gate_block log
    - Row 9 (replacement parasite) : currentGhost non-vide, shownAt récent (< parasiteWindow), nouveau onLLMChunk passe le Gate → vérifier qu'un event parasite est conceptuellement émis (test via state ou via une observation mockable)

    Tests Gate replacement bar :
    - replacementBarRejectsCloseScore : currentScore=0.6, onLLMChunk score=0.65 → return nil (0.65 < 0.69)
    - replacementBarAcceptsAboveBar : currentScore=0.6, onLLMChunk score=0.75 → return non-nil
    - l2UpgradesL1 : currentSource=.history score=0.5, onLLMChunk score=0.66 → returns non-nil (0.66 >= 0.5+0.15)

    Tests L1 re-enable derrière le Gate (D-08) :
    - afterSpaceHistoryBelowBarReturnsNil : history match donne ghost dont le score < 0.4 → routeInstant returns nil
    - afterSpaceHistoryAboveBarReturnsGhost : score >= 0.4 → returns GhostUpdate.source == .history

    ≥12 tests au total.
  </behavior>
  <action>
    Créer `Souffleuse/Tests/SouffleuseTests/SuggestionPolicyTests.swift` avec ≥12 cas couvrant la truth table D-08 + replacement bar D-07.

    Header :
    ```swift
    import Testing
    import Foundation
    import SouffleusePersonalization
    import SouffleuseTyping
    @testable import Souffleuse

    @MainActor
    @Suite("Phase 4 — Cascade routing (D-08) + Relevance Gate replacement (D-07)")
    struct SuggestionPolicyTests {
        // helper to build a TypingHistoryEntry
        static func entry(_ context: String, _ accepted: String) -> TypingHistoryEntry {
            TypingHistoryEntry(timestamp: Date(), contextBefore: context, accepted: accepted, bundleID: nil)
        }
        // helper to build a fresh engine
        static func engine() -> SuggestionPolicyEngine { SuggestionPolicyEngine(maxWords: 16) }
        // ...
    }
    ```

    Implémenter chaque test du `<behavior>` block en utilisant les fixtures.

    Pour le test parasite (Row 9), simuler la séquence :
    ```swift
    let policy = Self.engine()
    let s1 = Score(sourcePrior: 0.6, prefixFit: 1.0, lengthFit: 1.0)
    policy.applyGhost("first ghost", source: .llm, score: s1)
    // shownAt set automatiquement
    // immédiatement, on lance un onLLMChunk avec un score qui bat le replacement bar
    let result = policy.onLLMChunk("better much longer ghost text", userTail: "Bonjour ")
    #expect(result != nil)
    // L'engine a émis ghost_classified_parasite — on ne le verify pas via Log (qui écrit sur disque) mais via
    // une observation indirecte : currentGhost should now be "" after policy.applyGhost is called externally,
    // OR via le state lastReplacedSource. Test conceptuel : la séquence ne crash pas + currentSource du remplaçant
    // est .llm. Le contenu du log est validé en Task 5 par audit.
    ```

    Convention rappel : tous les seuils via `SuggestionPolicy.Tuning.*`, jamais de literal.

    Au-delà des 9 rows + 3 replacement + 2 L1 re-enable, ajouter quelques tests d'isolation :
    - `routeInstantReturnsNilWhenWordCompleterEmpty()`
    - `endLifecycleResetsCurrentGhost()`
    - `endLifecycleAcceptedFullEmitsCorrect()` — test conceptuel : applyGhost + endLifecycle(.acceptedFull) doit reset l'état (vérifié via `currentGhost == ""` après).
  </action>
  <verify>
    <automated>swift test --package-path Souffleuse --filter SuggestionPolicyTests 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - "Souffleuse/Tests/SouffleuseTests/SuggestionPolicyTests.swift exists"
    - "grep -c '@Test' Souffleuse/Tests/SouffleuseTests/SuggestionPolicyTests.swift returns >=12"
    - "grep -q '@MainActor' Souffleuse/Tests/SouffleuseTests/SuggestionPolicyTests.swift"
    - "grep -q 'routeInstant' Souffleuse/Tests/SouffleuseTests/SuggestionPolicyTests.swift"
    - "grep -q 'onLLMChunk' Souffleuse/Tests/SouffleuseTests/SuggestionPolicyTests.swift"
    - "grep -q 'SuggestionPolicy.Tuning.' Souffleuse/Tests/SouffleuseTests/SuggestionPolicyTests.swift"
    - "swift test --package-path Souffleuse --filter SuggestionPolicyTests exits 0"
  </acceptance_criteria>
  <done>Truth table D-08 verte. Replacement bar D-07 testé. L1 re-enable derrière le Gate testé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: ClassificationGridTests.swift — 1 event par lifecycle, mutually exclusive</name>
  <files>Souffleuse/Tests/SouffleuseTests/ClassificationGridTests.swift</files>
  <read_first>
    - Souffleuse/Tests/SouffleuseTests/KVCacheBypassTests.swift (pattern @MainActor @Test stateful)
    - Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift (post Tasks 1-2)
    - .planning/phases/04-cascade-quality-architecture/04-RESEARCH.md (§"Classification grid emission points" lignes 745-816, §"Pitfall 5" lignes 728-736)
    - .planning/phases/04-cascade-quality-architecture/04-PATTERNS.md (§"ClassificationGridTests.swift" lignes 628-658)
  </read_first>
  <behavior>
    Invariant central (Pitfall 5) : "1 ghost lifecycle = 1 classification event".
    - Test : applyGhost + endLifecycle(.acceptedFull) → state reset (currentGhost == ""), second endLifecycle no-op
    - Test : applyGhost + endLifecycle(.dismissedByEsc) avec shownAt >= 200ms → state reset
    - Test : applyGhost + endLifecycle(.dismissedByEsc) avec shownAt < 200ms → state reset MAIS aucun event useless (pas de check log direct dans test ; vérifié par invariant "state reset")
    - Test : applyGhost + endLifecycle(.typedDiverged) avec shownAt < 500ms → reset, mais visible window valide pour event bad
    - Test : applyGhost + endLifecycle(.typedDiverged) avec shownAt > 500ms → state reset, mais pas d'event bad émis (out of window) — testable via état post-call
    - Test : applyGhost + endLifecycle(.replacedByOther) → reset (parasite event conceptuellement émis)
    - Test : applyGhost + endLifecycle(.modelSwap) → reset (silent, no event)
    - Test : applyGhost + endLifecycle(.focusChange) → reset (silent)
    - Test : endLifecycle(.acceptedFull) sans applyGhost préalable → no-op (guard `!currentGhost.isEmpty` tient)
    - Test : applyGhost + endLifecycle(.acceptedFull) + endLifecycle(.acceptedFull) → second call is no-op (state already reset)
    - Test : applyGhost("X", llm, score1) + applyGhost("Y", history, score2) → lastReplacedSource == .llm (vérifie le tracking pour parasite detection)
  </behavior>
  <action>
    Créer `Souffleuse/Tests/SouffleuseTests/ClassificationGridTests.swift` avec ≥10 cas.

    Header :
    ```swift
    import Testing
    import Foundation
    @testable import Souffleuse

    @MainActor
    @Suite("Phase 4 — Classification grid lifecycle (D-09, D-10, Pitfall 5)")
    struct ClassificationGridTests {

        static func engine() -> SuggestionPolicyEngine { SuggestionPolicyEngine(maxWords: 16) }
        static func sampleScore() -> Score { Score(sourcePrior: 0.6, prefixFit: 1.0, lengthFit: 1.0) }

        // ...
    }
    ```

    Implémenter tous les tests du `<behavior>` block. L'observabilité est limitée (le système log écrit sur disque) ; les invariants se vérifient via le STATE de l'engine après le call (`currentGhost == ""`, `currentSource == .none`, `shownAt == nil`, `lastReplacedSource == X`).

    Test bonus pour l'invariant "1 event par lifecycle" :
    ```swift
    @Test func applyAndEndAreIdempotentReset() {
        let p = Self.engine()
        p.applyGhost("ghost", source: .llm, score: Self.sampleScore())
        #expect(p.currentGhost == "ghost")
        #expect(p.shownAt != nil)
        p.endLifecycle(reason: .acceptedFull)
        #expect(p.currentGhost == "")
        #expect(p.currentSource == .none)
        #expect(p.shownAt == nil)
        // Second call : aucun crash, aucun change
        p.endLifecycle(reason: .acceptedFull)
        #expect(p.currentGhost == "")
    }
    ```

    Test pour le tracking parasite (sans déclencher onLLMChunk — juste applyGhost) :
    ```swift
    @Test func lastReplacedSourceTrackedAcrossApply() {
        let p = Self.engine()
        p.applyGhost("first", source: .history, score: Self.sampleScore())
        p.applyGhost("second", source: .llm, score: Self.sampleScore())
        // Pas d'API publique pour lastReplacedSource directement ; on vérifie indirectement
        // via le fait que currentSource est maintenant .llm.
        #expect(p.currentSource == .llm)
    }
    ```

    ≥10 tests.
  </action>
  <verify>
    <automated>swift test --package-path Souffleuse --filter ClassificationGridTests 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - "Souffleuse/Tests/SouffleuseTests/ClassificationGridTests.swift exists"
    - "grep -c '@Test' Souffleuse/Tests/SouffleuseTests/ClassificationGridTests.swift returns >=10"
    - "grep -q '@MainActor' Souffleuse/Tests/SouffleuseTests/ClassificationGridTests.swift"
    - "grep -q 'endLifecycle' Souffleuse/Tests/SouffleuseTests/ClassificationGridTests.swift"
    - "grep -q 'LifecycleEndReason' Souffleuse/Tests/SouffleuseTests/ClassificationGridTests.swift"
    - "swift test --package-path Souffleuse --filter ClassificationGridTests exits 0"
  </acceptance_criteria>
  <done>10+ tests classification verts. Invariant "1 event par lifecycle" verrouillé par tests.</done>
</task>

<task type="auto">
  <name>Task 6: Post-extraction gates — audit + tests full + replay equivalence vs baseline</name>
  <files></files>
  <read_first>
    - .planning/phases/04-cascade-quality-architecture/04-02-BASELINE-REPLAY.md (créé Task 1)
    - Souffleuse/audit.sh
  </read_first>
  <action>
    Trois gates de validation :

    1. **Audit gate** — `bash Souffleuse/audit.sh` doit exit 0, 6 checks verts. Critique : les 5 nouveaux events `ghost_classified_*` + `ghost_gate_*` + `ghost_keep_under_bar` sont tous `StaticString` → check 6 (no os_log interpolation user fields) passe par construction. Mais le check 4 (log fields whitelisted = ts/level/module/event/count) doit confirmer qu'aucun nouveau field n'est ajouté. Et le check 5 (history.aes scope) reste : aucune lecture nouvelle dans SuggestionPolicy.swift.

    2. **Tests full suite** — `swift test --package-path Souffleuse` exit 0. Compter les tests : doit être ≥151 + ≥12 SuggestionPolicy + ≥10 ClassificationGrid = ≥173. Aucun test baseline (139) ne régresse.

    3. **Replay equivalence vs baseline (D-02 safety net)** —
       a. Re-run le replay : `swift run --package-path Souffleuse SouffleuseCoherence --replay > /tmp/04-02-post-replay.txt 2>&1` (ou la commande utilisée en Task 1).
       b. Si baseline est en mode `LIVE_REPLAY` : `diff .planning/phases/04-cascade-quality-architecture/04-02-BASELINE-REPLAY.txt /tmp/04-02-post-replay.txt`. Tolérance : 0 différence sur le ghost text per scenario (l'extraction est sémantiquement equivalent — la cascade routing PEUT produire des outputs différents si le Gate threshold filtre des ghosts qui passaient avant). **Acceptable divergence** : si le Gate maintenant filtre un ghost low-score qui était précédemment affiché, c'est un *intended behavior change* de Phase 4 — annoter dans `04-02-REPLAY-DIFF.md` chaque divergence avec une justification ("Gate D-07 hard block at score X < 0.25" ou "L1 re-enable per D-08").
       c. Si baseline est en mode `BUILD-ONLY` : seulement vérifier que `swift build` + `swift test` restent verts, plus pas de diff brut.

    Créer `.planning/phases/04-cascade-quality-architecture/04-02-REPLAY-DIFF.md` listant :
       - Mode (LIVE / BUILD-ONLY)
       - Commande
       - Nombre de scenarios
       - Diff summary : N scenarios identical, M scenarios with intended divergence (annotated)
       - Si M > 0 : table avec scenario id + before ghost + after ghost + justification (D-07 / D-08 / autre)
       - Verdict global : `EQUIVALENT (modulo intended Gate changes)` ou `REGRESSION DETECTED`

    Si verdict est `REGRESSION DETECTED` : STOP, NE PAS commit, diagnostiquer. Les régressions inacceptables incluent : ghosts qui devraient passer le Gate et ne passent plus, ghosts différents pour le même scenario *sans* changement de seuil expliquant.

    Si verdict est `EQUIVALENT (modulo intended)` : OK pour committer Plan 04-02.
  </action>
  <verify>
    <automated>bash Souffleuse/audit.sh && swift test --package-path Souffleuse 2>&amp;1 | grep -E 'Test run with [0-9]+ tests' && test -f .planning/phases/04-cascade-quality-architecture/04-02-REPLAY-DIFF.md</automated>
  </verify>
  <acceptance_criteria>
    - "bash Souffleuse/audit.sh exits 0"
    - "swift test --package-path Souffleuse exits 0"
    - "swift test --package-path Souffleuse output contains 'Test run with' followed by integer >= 173"
    - "04-02-REPLAY-DIFF.md exists"
    - "grep -E 'EQUIVALENT|REGRESSION' .planning/phases/04-cascade-quality-architecture/04-02-REPLAY-DIFF.md returns at least one match"
    - "grep 'REGRESSION DETECTED' .planning/phases/04-cascade-quality-architecture/04-02-REPLAY-DIFF.md returns 0 (no unexpected regression)"
  </acceptance_criteria>
  <done>Plan 04-02 complet : SuggestionPolicyEngine câblé, cascade L0/L1 + Gate + classification grid live, 22+ nouveaux tests verts, audit vert, replay équivalent (modulo changements intentionnels du Gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @MainActor → process-internal | SuggestionPolicyEngine state vit sur MainActor ; onLLMChunk est appelé depuis un MainActor.run hop (déjà la convention PVM) |
| Log.info emission (5 nouveaux events) | StaticString event names → privacy-by-typesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | Logs des classification events (count: visibleMs) | mitigate | `count` est un Int (durée ms ou chunk count) — non-identifiant. Audit check 6 forbids interpolation of user fields ; les events sont StaticString. Vérifié par bash Souffleuse/audit.sh Task 6. |
| T-04-02-02 | Tampering | History exact-match L1 derrière le Gate ne lit pas history.aes directement | mitigate | SuggestionPolicy reçoit `historySnapshot: [TypingHistoryEntry]` en paramètre (snapshot in-memory). Aucune lecture nouvelle de history.aes. Audit check 5 vérifié. |
| T-04-02-03 | Tampering | Classification grid double-counting (Pitfall 5) | mitigate | Tests ClassificationGridTests verrouillent l'invariant "1 event per lifecycle". endLifecycle reset state ; second call no-op. |
| T-04-02-04 | Repudiation | Replay equivalence drift non détectée | mitigate | Task 1 capture baseline ; Task 6 diff vs baseline + génère REPLAY-DIFF.md avec verdict. |
| T-04-02-05 | Information Disclosure | Score components individually logged | accept | Le scaled `Int(score.value * 100)` est non-identifiant. Les 3 composants individuels ne sont JAMAIS loggés. |
| T-04-02-06 | DoS | Re-entrance dans la cascade pendant partial-accept (Pitfall 2) | mitigate | PVM préserve le guard `partialRemainder.isEmpty` avant d'appeler routeInstant. Le wiring TypingSession (04-07) prend le relais. |

**audit.sh invariants préservés :**
- 5 nouveaux events StaticString — check 6 OK by construction
- Aucun nouveau lecteur de history.aes — check 5 OK
- Aucun nouveau field log — check 4 OK (count: Int only)
- Aucun print/NSLog/os_log interpolant user fields — check 1-3 OK
- Audit vérifié à Task 6 (`bash Souffleuse/audit.sh` exits 0)
</threat_model>

<verification>
- SuggestionPolicyEngine + GhostUpdate + LifecycleEndReason créés
- PVM délègue cascade + Gate + classification
- 5 events ghost_classified_* émis via single call-site endLifecycle
- L1 history exact-match réactivé derrière afterSpaceL1Bar
- ≥22 nouveaux tests verts (SuggestionPolicyTests + ClassificationGridTests)
- ≥173 tests total
- audit.sh 6/6
- Replay equivalent (modulo intended Gate changes documentés dans REPLAY-DIFF.md)
</verification>

<success_criteria>
1. `swift build --package-path Souffleuse` exit 0
2. `swift test --package-path Souffleuse` exit 0 avec ≥173 tests
3. `bash Souffleuse/audit.sh` exit 0
4. SuggestionPolicyEngine + GhostUpdate + LifecycleEndReason en place
5. PVM ne contient plus la cascade L0/L1 inlined ni l'anti-churn highConfidence (remplacé par delegation)
6. 5 events ghost_classified_* émis EXCLUSIVEMENT depuis endLifecycle (1 call-site)
7. 04-02-REPLAY-DIFF.md verdict `EQUIVALENT (modulo intended)` — pas de REGRESSION
</success_criteria>

<output>
After completion, create `.planning/phases/04-cascade-quality-architecture/04-02-SUMMARY.md` listing :
- New types (SuggestionPolicyEngine, GhostUpdate, LifecycleEndReason)
- PVM LOC before/after (1566 → ?)
- Cascade region migrated (lines 525-913 → policy delegation)
- Classification events wired : Tab full / Esc / swapModel (parasite handled internally)
- TODOs marked for 04-07 (TypingSession) : typedDiverged + typedPastWithoutOverlap + partial-accept acceptable
- Replay equivalence verdict (link to 04-02-REPLAY-DIFF.md)
- Test count before/after
</output>

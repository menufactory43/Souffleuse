---
phase: 04-cascade-quality-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift
  - Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift
  - Souffleuse/Tests/SouffleuseTests/RelevanceGateTests.swift
autonomous: true
requirements:
  - D-06
  - D-07
  - D-13
tags:
  - phase-04
  - foundation
  - scoring
  - relevance-gate

must_haves:
  truths:
    - "Score(sourcePrior, prefixFit, lengthFit) calcule un scalar [0,1] = produit des trois facteurs."
    - "score.passesGate retourne false sous Tuning.gateFloor (0.25)."
    - "score.beats(other) retourne true ssi score.value >= other.value × Tuning.replacementBar (1.15)."
    - "Tous les seuils D-06/D-07/D-08/D-09 vivent dans un seul fichier (SuggestionPolicy+Tuning.swift)."
    - "Les tests unitaires référencent Tuning.* (jamais de literals)."
  artifacts:
    - path: "Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
      provides: "Score struct + SuggestionPolicy.score(source:ghost:userTail:) pure static + prefixFit + lengthFit + SuggestionSource enum"
      min_lines: 80
    - path: "Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift"
      provides: "SuggestionPolicy.Tuning enum with gateFloor / replacementBar / afterSpaceL1Bar / l2UpgradeDelta / parasiteWindow / uselessMinVisibleMs / badMaxDivergeMs / sourcePrior dict / lengthFitByWordCount array"
      min_lines: 30
    - path: "Souffleuse/Tests/SouffleuseTests/RelevanceGateTests.swift"
      provides: "Pure-function tests on Score + passesGate + beats + lengthFit bell curve + prefixFit mid-word/after-space"
      min_lines: 60
  key_links:
    - from: "Tests/SouffleuseTests/RelevanceGateTests.swift"
      to: "SuggestionPolicy.Tuning"
      via: "@testable import Souffleuse"
      pattern: "SuggestionPolicy\\.Tuning\\."
---

<objective>
Créer la fondation pure-function du Ghost Relevance Gate : le type `Score`, le scorer `SuggestionPolicy.score(...)`, l'enum `SuggestionSource` (canonique, à mover depuis PVM:95-109), et le single-file holder `Tuning` regroupant TOUS les seuils tunables D-06/D-07/D-08/D-09/D-13.

Purpose : verrouiller le contrat numérique du Gate avant tout déplacement de logique cascade. Les tests unitaires deviennent immédiatement exécutables sans aucune dépendance MLX.

Output : un nouveau fichier `SuggestionPolicy.swift` (squelette + Score + scorer pur + nonisolated static helpers), un fichier `SuggestionPolicy+Tuning.swift` (constants), un test file `RelevanceGateTests.swift`. La cascade live n'est pas encore branchée — c'est l'étape 04-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-cascade-quality-architecture/04-CONTEXT.md
@.planning/phases/04-cascade-quality-architecture/04-RESEARCH.md
@.planning/phases/04-cascade-quality-architecture/04-PATTERNS.md

<interfaces>
<!-- Canonical types this plan creates. Subsequent plans (04-02..04-04) consume them. -->

From Souffleuse/Sources/Souffleuse/PredictorViewModel.swift:95-109 (to migrate verbatim):
```swift
enum SuggestionSource: Sendable {
    case none
    case wordComplete
    case history
    case cache
    case undoCache
    case llm
}
```

New Score type (Sources/Souffleuse/SuggestionPolicy.swift):
```swift
struct Score: Sendable, Equatable, CustomStringConvertible {
    let sourcePrior: Float
    let prefixFit: Float
    let lengthFit: Float
    var value: Float { sourcePrior * prefixFit * lengthFit }
    var passesGate: Bool { value >= SuggestionPolicy.Tuning.gateFloor }
    func beats(_ other: Score) -> Bool { value >= other.value * SuggestionPolicy.Tuning.replacementBar }
    var description: String { "Score(src=\(sourcePrior) pref=\(prefixFit) len=\(lengthFit) → \(value))" }
}

enum SuggestionPolicy {
    static func score(source: SuggestionSource, ghost: String, userTail: String) -> Score { ... }
    nonisolated static func prefixFit(ghost: String, userTail: String) -> Float { ... }
    nonisolated static func lengthFit(ghost: String) -> Float { ... }
}
```

Note: `SuggestionPolicy` est introduit ici comme un `enum` namespace pour les pure functions du Gate. Le plan 04-02 le transformera en `@MainActor final class` (state-bearing) en gardant les helpers `static` pour la testabilité.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Créer SuggestionPolicy.swift (squelette pur, Score + scorer + SuggestionSource)</name>
  <files>Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift</files>
  <read_first>
    - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift (lignes 1-10, 95-109, 1524-1549 — imports, SuggestionSource verbatim, historyExactSubstringMatch pour référence du style `nonisolated static`)
    - Souffleuse/Sources/Souffleuse/KVCacheHolder.swift (lignes 1-50 — pattern Sendable+Equatable du `InvariancePrefix`)
    - .planning/phases/04-cascade-quality-architecture/04-RESEARCH.md (§"Confidence scoring formula + Swift type sketch" lignes 444-549)
    - .planning/phases/04-cascade-quality-architecture/04-PATTERNS.md (§"SuggestionPolicy.swift" lignes 35-160, §"Pattern E" lignes 904-924)
    - .planning/phases/04-cascade-quality-architecture/04-CONTEXT.md (D-06, D-07, D-08 — formule + seuils)
  </read_first>
  <behavior>
    - Test : Score(0.5, 0.5, 0.5).value == 0.125
    - Test : Score(0.5, 0.5, 0.5).passesGate == false (0.125 < 0.25)
    - Test : Score(0.7, 1.0, 1.0).passesGate == true (0.7 ≥ 0.25)
    - Test : Score B(0.75) beats Score A(0.6) car 0.75 ≥ 0.6×1.15=0.69
    - Test : Score B(0.65) does NOT beat Score A(0.6) car 0.65 < 0.69
    - Test : SuggestionPolicy.score(source:.history, ghost:"voilà la suite", userTail:"écrire ") retourne value > 0.25
    - Test : prefixFit mid-word (userTail finit par lettre) : ghost qui commence par le complement du mot → 1.0, sinon → 0.0
    - Test : prefixFit after-space (userTail.last.isWhitespace) : ghost qui commence par lettre/digit → 1.0, ghost qui commence par espace ou markdown → 0.0
    - Test : lengthFit table : 0 mot → 0.0 ; 1 mot → 0.6 ; 2-5 mots → 1.0 ; 6 → 0.85 ; 7-8 → 0.6 ; 9+ → 0.3
  </behavior>
  <action>
    Créer le fichier `Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift` avec EXACTEMENT le contenu suivant (per D-06, D-07, D-13 — pas de littéraux dans le scoring, seulement des références à `Tuning`) :

    1. Imports : `import Foundation`, `import SouffleuseLog`, `import SouffleusePersonalization` (pour TypingHistoryEntry — utilisé en 04-02), `import SouffleuseTyping` (pour WordCompleter — utilisé en 04-02). Note : à cette étape, ces imports sont déjà nécessaires pour le squelette qui sera enrichi en 04-02.

    2. Documentation file-header (`///`) qui explique : ce fichier est la cible D-03 / D-05..D-13. Le plan 04-01 introduit la fondation pure-function (Score + scorer + Tuning) ; le plan 04-02 introduit le `@MainActor final class SuggestionPolicy` state-bearing.

    3. **Déplacement atomique de `enum SuggestionSource`** (per D-02). Dans `Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift`, créer `enum SuggestionSource` au top-level — migration verbatim depuis `PredictorViewModel.swift:95-109` (cases, raw values, Sendable conformance). Dans le MÊME commit : supprimer la définition originale `PredictorViewModel.swift:95-109`. Le nom reste global → tous les call-sites PVM continuent de compiler sans changement. Ajouter `// MARK: - SuggestionSource (Phase 4 D-03 — moved from PredictorViewModel)` au-dessus de la définition.

    4. Struct `Score` avec exactement : `let sourcePrior: Float`, `let prefixFit: Float`, `let lengthFit: Float`, `var value: Float { sourcePrior * prefixFit * lengthFit }`, `var passesGate: Bool { value >= SuggestionPolicy.Tuning.gateFloor }`, `func beats(_ other: Score) -> Bool { value >= other.value * SuggestionPolicy.Tuning.replacementBar }`, conforme à `Sendable, Equatable, CustomStringConvertible`, et `var description: String { "Score(src=\(sourcePrior) pref=\(prefixFit) len=\(lengthFit) → \(value))" }`.

    5. Enum `SuggestionPolicy` au top-level (sera étendu en class par 04-02 — voir Note ↓) contenant :
       - `static func score(source: SuggestionSource, ghost: String, userTail: String) -> Score`
         retourne `Score(sourcePrior: Tuning.sourcePrior[source] ?? 0.0, prefixFit: Self.prefixFit(ghost: ghost, userTail: userTail), lengthFit: Self.lengthFit(ghost: ghost))`
       - `nonisolated static func prefixFit(ghost: String, userTail: String) -> Float`
         logique D-06 verbatim : si `userTail.last?.isLetter == true` (mid-word) → 1.0 si `ghost.lowercased()` commence par un caractère qui complète le mot (non-vide, premier char est letter), sinon 0.0 ; si `userTail.last?.isWhitespace == true` OU `userTail.isEmpty` (after-space) → 1.0 si `ghost.first` est letter/digit/`'`/`"` (natural continuation), 0.0 si ghost commence par espace/newline/markdown (`#`, `*`, `_`, `~`) ; tout autre cas → 0.0
       - `nonisolated static func lengthFit(ghost: String) -> Float`
         compte les mots via `ghost.split(whereSeparator: { $0.isWhitespace }).count`, clamp à `Tuning.lengthFitByWordCount.count - 1`, retourne `Tuning.lengthFitByWordCount[index]`

    Note transition 04-01 → 04-02 : `SuggestionPolicy` est un `enum` ici (namespace pur, pas d'état). En 04-02, on ajoutera `@MainActor final class SuggestionPolicyEngine` (ou on convertira `enum` en `final class`) qui contient l'état (currentGhost, currentSource, currentScore, shownAt, etc.). Les `static func score/prefixFit/lengthFit` resteront accessibles comme `SuggestionPolicy.score(...)` via un `extension` ou via les noms originaux. **Stratégie retenue** : garder le namespace `enum SuggestionPolicy` pour les pure functions ; le plan 04-02 introduira `@MainActor final class SuggestionPolicyEngine` séparément. Cela évite le rename de `enum` → `class` qui casserait les call-sites du Tuning.

    Pas de `@MainActor`, pas d'état mutable. Pure functions only.
  </action>
  <verify>
    <automated>swift build --package-path Souffleuse 2>&amp;1 | tail -20 ; test -f Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift</automated>
  </verify>
  <acceptance_criteria>
    - "Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift exists"
    - "grep -q 'struct Score: Sendable, Equatable' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'enum SuggestionSource: Sendable' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'enum SuggestionPolicy' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'static func score(source: SuggestionSource' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'nonisolated static func prefixFit' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -q 'nonisolated static func lengthFit' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift"
    - "grep -vE '^[[:space:]]*///|^[[:space:]]*//' Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift | grep -cE '0\\.(25|4|15|55|6|7|75|85)' returns 0 (no inline magic numbers — all via Tuning)"
    - "PredictorViewModel.swift no longer defines `enum SuggestionSource` (grep -n 'enum SuggestionSource' Souffleuse/Sources/Souffleuse/PredictorViewModel.swift returns 0)"
    - "swift build --package-path Souffleuse exits 0"
  </acceptance_criteria>
  <done>SuggestionPolicy.swift compile, contient Score+SuggestionSource+pure scorer ; PVM ne définit plus SuggestionSource (déplacé). Pas encore de state-bearing class — c'est 04-02.</done>
</task>

<task type="auto">
  <name>Task 2: Créer SuggestionPolicy+Tuning.swift (single-file holder de toutes les constantes D-06/D-07/D-08/D-09/D-13)</name>
  <files>Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift</files>
  <read_first>
    - Souffleuse/Sources/Souffleuse/PredictorViewModel.swift (lignes 17-20 PromptBuilderFlag, 31-34 KVCacheBypassFlag — pattern single-file flag holder)
    - .planning/phases/04-cascade-quality-architecture/04-RESEARCH.md (§"Confidence scoring formula" lignes 444-479 ; §"Classification grid emission" lignes 813-816)
    - .planning/phases/04-cascade-quality-architecture/04-PATTERNS.md (§"SuggestionPolicy+Tuning.swift" lignes 164-204)
    - .planning/phases/04-cascade-quality-architecture/04-CONTEXT.md (D-06, D-07, D-08, D-09, D-13)
  </read_first>
  <action>
    Créer `Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift` qui étend `SuggestionPolicy` avec un sous-namespace `enum Tuning` contenant TOUS les seuils numériques de la phase 4. Visibilité `internal` (NOT `private`) pour `@testable` access.

    Imports : `import Foundation`.

    Structure exacte :

    ```swift
    extension SuggestionPolicy {
        /// Constantes tunables centralisées pour le Ghost Relevance Gate (D-06..D-13).
        /// Pitfall 6 (RESEARCH §"Common Pitfalls") : aucun seuil littéral ne doit
        /// apparaître ailleurs dans le code ou les tests — toujours via Tuning.*.
        enum Tuning {
            // MARK: - D-07 Gate floor + replacement bar
            static let gateFloor: Float = 0.25
            static let replacementBar: Float = 1.15

            // MARK: - D-08 Routing thresholds
            static let afterSpaceL1Bar: Float = 0.4
            static let l2UpgradeDelta: Float = 0.15

            // MARK: - D-09 Classification windows (assumptions A2-A4 in RESEARCH)
            static let parasiteWindow: TimeInterval = 0.8
            static let uselessMinVisibleMs: Int = 200
            static let badMaxDivergeMs: Int = 500

            // MARK: - D-06 Source priors
            static let sourcePrior: [SuggestionSource: Float] = [
                .wordComplete: 0.55,
                .history:      0.75,
                .llm:          0.60,
                .cache:        0.70,
                .undoCache:    0.65,
                .none:         0.0,
            ]

            // MARK: - D-06 Bell curve length_fit (index = word count, clamp to last for >=10)
            static let lengthFitByWordCount: [Float] = [
                0.0,  // 0 mots — défensif
                0.6,  // 1 mot
                1.0,  // 2
                1.0,  // 3
                1.0,  // 4
                1.0,  // 5
                0.85, // 6 — bord du sweet spot
                0.6,  // 7
                0.6,  // 8
                0.3,  // 9+ — trop long
            ]
        }
    }
    ```

    Aucune autre constante. Aucune fonction. Aucun import autre que Foundation. Le `extension SuggestionPolicy` cible le `enum SuggestionPolicy` créé en Task 1.
  </action>
  <verify>
    <automated>swift build --package-path Souffleuse 2>&amp;1 | tail -5 ; test -f Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift</automated>
  </verify>
  <acceptance_criteria>
    - "Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift exists"
    - "grep -q 'extension SuggestionPolicy' Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift"
    - "grep -q 'enum Tuning' Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift"
    - "grep -q 'static let gateFloor: Float = 0.25' Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift"
    - "grep -q 'static let replacementBar: Float = 1.15' Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift"
    - "grep -q 'static let parasiteWindow: TimeInterval = 0.8' Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift"
    - "grep -q 'static let sourcePrior' Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift"
    - "grep -q 'static let lengthFitByWordCount' Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift"
    - "swift build --package-path Souffleuse exits 0"
  </acceptance_criteria>
  <done>Tuning.swift compile, contient les 10 constantes (gateFloor, replacementBar, afterSpaceL1Bar, l2UpgradeDelta, parasiteWindow, uselessMinVisibleMs, badMaxDivergeMs, sourcePrior dict, lengthFitByWordCount array) — single source of truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RelevanceGateTests.swift — pure-function tests (≥12 tests)</name>
  <files>Souffleuse/Tests/SouffleuseTests/RelevanceGateTests.swift</files>
  <read_first>
    - Souffleuse/Tests/SouffleuseTests/KVCacheHolderTests.swift (pattern Swift Testing pure-value tests verbatim)
    - Souffleuse/Tests/SouffleuseTests/HistoryExactMatchTests.swift (pattern `@Suite`, fixture helpers)
    - Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift (le fichier créé Task 1)
    - Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift (le fichier créé Task 2)
    - .planning/phases/04-cascade-quality-architecture/04-PATTERNS.md (§"RelevanceGateTests.swift" lignes 586-624)
  </read_first>
  <behavior>
    - scoreIsProductOfThreeFactors : value = src × pref × len, vérifié pour 3 combos
    - gateFloorBlocksLowScores : Score(0.5, 0.5, 0.5).passesGate == false
    - gateFloorAccepts : Score(0.6, 1.0, 1.0).passesGate == true
    - replacementBarBeatsCurrent : B(0.75) beats A(0.6) → true
    - replacementBarRejectsClose : B(0.65) beats A(0.6) → false (0.65 < 0.69)
    - sourcePriorMatrix : history > undoCache > cache > llm > wordComplete > none
    - prefixFitMidWordMatch : userTail="Bonjou", ghost="r monde" → 1.0
    - prefixFitMidWordMismatch : userTail="Bonjou", ghost="xyz" → 0.0
    - prefixFitAfterSpaceLetter : userTail="Bonjour ", ghost="monde" → 1.0
    - prefixFitAfterSpaceMarkdown : userTail="Bonjour ", ghost="* item" → 0.0
    - prefixFitEmptyTail : userTail="", ghost="Bonjour" → 1.0
    - lengthFitBellCurve : 0 mot → 0.0 ; 1 → 0.6 ; 3 → 1.0 ; 6 → 0.85 ; 9 → 0.3 ; 15 (clamp) → 0.3
    - tuningConstantsAreReferenced : aucun literal hardcodé dans ce fichier (vérifié via grep CI dans Task 4)
  </behavior>
  <action>
    Créer `Souffleuse/Tests/SouffleuseTests/RelevanceGateTests.swift` avec exactement ≥12 cas de tests Swift Testing.

    Header :
    ```swift
    import Testing
    import Foundation
    @testable import Souffleuse

    @Suite("Phase 4 — Relevance Gate scoring (D-06, D-07, D-13)")
    struct RelevanceGateTests {
        // ...
    }
    ```

    Convention : aucun literal numérique de seuil. Tous les seuils référencés via `SuggestionPolicy.Tuning.gateFloor`, `.replacementBar`, etc. (per Pitfall 6).

    Tests à implémenter (≥12) :

    1. `@Test func scoreValueIsProductOfThreeFactors()` — vérifier value pour 3 combos différents.
    2. `@Test func passesGateBlocksUnderFloor()` — Score(0.5, 0.5, 0.5).passesGate == false. (0.125 < gateFloor)
    3. `@Test func passesGateAcceptsAboveFloor()` — Score(0.75, 1.0, 1.0).passesGate == true.
    4. `@Test func beatsReturnsFalseForEqualScores()` — Score A == B → A.beats(B) == false (égalité non suffisante).
    5. `@Test func beatsReturnsTrueWhenAboveReplacementBar()` — A(0.75) vs B(0.6) → A.beats(B) == true (0.75 ≥ 0.69).
    6. `@Test func beatsReturnsFalseJustBelowReplacementBar()` — A(0.65) vs B(0.6) → A.beats(B) == false.
    7. `@Test func sourcePriorOrderingMatchesD06()` — Tuning.sourcePrior[.history]! > .undoCache > .cache > .llm > .wordComplete > .none.
    8. `@Test func prefixFitMidWordMatchReturnsOne()` — userTail="Bonjou", ghost="r monde" → prefixFit == 1.0.
    9. `@Test func prefixFitMidWordDivergentReturnsZero()` — userTail="Bonjou", ghost="zyx" → prefixFit == 0.0.
    10. `@Test func prefixFitAfterSpaceLetterReturnsOne()` — userTail="Bonjour ", ghost="monde" → 1.0.
    11. `@Test func prefixFitAfterSpaceMarkdownReturnsZero()` — userTail="Bonjour ", ghost="* liste" → 0.0.
    12. `@Test func prefixFitEmptyTailReturnsOne()` — userTail="", ghost="Bonjour" → 1.0.
    13. `@Test func lengthFitBellCurveByWordCount()` — vérifier table : 0→0.0, 1→0.6, 3→1.0, 6→0.85, 9→0.3, 15 (clamp)→0.3.
    14. `@Test func scoreEndToEndHistoryAfterSpace()` — `SuggestionPolicy.score(source:.history, ghost:"continue la phrase", userTail:"voici ")` retourne `.value > Tuning.gateFloor` et `.value > Tuning.afterSpaceL1Bar`.

    Pas d'effet de bord, pas de `@MainActor` (toutes les fonctions testées sont `nonisolated static` ou pure).
  </action>
  <verify>
    <automated>swift test --package-path Souffleuse --filter RelevanceGateTests 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - "Souffleuse/Tests/SouffleuseTests/RelevanceGateTests.swift exists"
    - "grep -c '@Test' Souffleuse/Tests/SouffleuseTests/RelevanceGateTests.swift returns >=12"
    - "grep -q '@Suite' Souffleuse/Tests/SouffleuseTests/RelevanceGateTests.swift"
    - "grep -q 'SuggestionPolicy.Tuning.' Souffleuse/Tests/SouffleuseTests/RelevanceGateTests.swift"
    - "grep -vE '^[[:space:]]*///|^[[:space:]]*//' Souffleuse/Tests/SouffleuseTests/RelevanceGateTests.swift | grep -cE '\\b0\\.(25|4|15|55|6|7|75|85)\\b' returns 0 (no literal thresholds — only via Tuning)"
    - "swift test --package-path Souffleuse --filter RelevanceGateTests exits 0"
    - "swift test --package-path Souffleuse 2>&amp;1 | grep -E 'Test run with [0-9]+ tests' shows >=151 tests (139 + >=12)"
  </acceptance_criteria>
  <done>Tous les tests RelevanceGate verts. La fondation pure-function du Gate est prouvée correcte. Convention "no literal threshold" tenue.</done>
</task>

<task type="auto">
  <name>Task 4: Audit gate + grep CI sur literals — final check du plan</name>
  <files></files>
  <read_first>
    - Souffleuse/audit.sh (lecture complète, 76 lignes)
    - Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift
    - Souffleuse/Sources/Souffleuse/SuggestionPolicy+Tuning.swift
    - Souffleuse/Tests/SouffleuseTests/RelevanceGateTests.swift
  </read_first>
  <action>
    Vérifier trois invariants gates de fin de plan, dans cet ordre :

    1. `bash Souffleuse/audit.sh` — doit exit 0, 6 checks verts. Aucun nouveau `print(` / `NSLog(` / `os_log` interpolant. Les nouveaux fichiers n'introduisent aucun `Log.*` call (le scorer est pur, pas de logging) — donc l'audit doit rester silencieux à propos de ces fichiers.

    2. Grep CI sur les literals — exécuter :
       ```
       grep -nE '0\.(25|4|15|55|60|70|75|85)' Souffleuse/Tests/SouffleuseTests/RelevanceGateTests.swift Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift | grep -v 'Tuning\.' | grep -vE '^[^:]+:[0-9]+:\s*///|^[^:]+:[0-9]+:\s*//'
       ```
       Doit retourner 0 lignes (les seuls literals autorisés sont dans `SuggestionPolicy+Tuning.swift`).

    3. `swift test --package-path Souffleuse` — full suite : doit passer ≥151 tests (139 baseline + ≥12 nouveaux RelevanceGate). Aucune régression sur les 139 baseline.

    Si une de ces 3 conditions échoue, NE PAS commit. Diagnostiquer, fixer, re-checker.

    Threat model addendum : les fichiers de ce plan sont pure-function + constantes. Ils n'émettent AUCUN `Log.*` call ; ils ne lisent NI history.aes NI clipboard. Aucune surface d'attaque privacy nouvelle. Audit invariants préservés par construction.
  </action>
  <verify>
    <automated>bash Souffleuse/audit.sh && swift test --package-path Souffleuse 2>&amp;1 | grep -E 'Test run with [0-9]+ tests' && (grep -nE '0\.(25|4|15|55|60|70|75|85)' Souffleuse/Tests/SouffleuseTests/RelevanceGateTests.swift Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift | grep -v 'Tuning\.' | grep -vE '^[^:]+:[0-9]+:\s*///|^[^:]+:[0-9]+:\s*//' | wc -l | grep -qE '^\s*0\s*$')</automated>
  </verify>
  <acceptance_criteria>
    - "bash Souffleuse/audit.sh exits 0"
    - "swift test --package-path Souffleuse exits 0"
    - "swift test --package-path Souffleuse output contains 'Test run with' followed by integer >= 151"
    - "grep -nE '0\\.(25|4|15|55|60|70|75|85)' Souffleuse/Tests/SouffleuseTests/RelevanceGateTests.swift Souffleuse/Sources/Souffleuse/SuggestionPolicy.swift | grep -v 'Tuning\\.' | grep -cvE '^[^:]+:[0-9]+:\\s*///|^[^:]+:[0-9]+:\\s*//' returns 0"
  </acceptance_criteria>
  <done>Foundation plan complete : Score + scorer + Tuning + tests verts + audit vert + invariant "no literal threshold" tenu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process-internal (pure scorer) | Aucune frontière franchie — Score est value-type, Tuning est const, scoring est pure function |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | Logging du score | mitigate | Aucun `Log.*` call dans ce plan. Le scoring est pure ; quand 04-02 loggera, ce sera via `count: Int(score.value * 100)` — jamais le ghost text. |
| T-04-01-02 | Tampering | Tuning constants drift entre code et tests | mitigate | Grep CI dans Task 4 : aucun literal autorisé hors `SuggestionPolicy+Tuning.swift`. Tests référencent `Tuning.*`. |
| T-04-01-03 | Spoofing | Un autre fichier redéfinit `SuggestionSource` après migration | accept | Compile-time error si dup ; audit existant ne le check pas (low risk — Swift symbol uniqueness). |

**audit.sh invariants préservés :**
- Aucun nouveau `print(` / `NSLog(` / `os_log` interpolant — vérifié par audit Task 4.
- Aucune nouvelle lecture de `history.aes` (le fichier touche seulement Score + Tuning).
- Aucun nouveau `Log.*` call interpolant user fields.

Phase 4 release gate (D-11) ne s'applique pas à ce plan (pas de classification events émis ici — c'est 04-02+).
</threat_model>

<verification>
- Score + scorer + Tuning compilent.
- 12+ tests RelevanceGate verts.
- Audit 6/6.
- 151+ tests total (139 baseline + ≥12 nouveaux).
- Aucun literal de seuil hors Tuning.swift.
- PVM ne définit plus `enum SuggestionSource` (migré).
</verification>

<success_criteria>
1. `swift build --package-path Souffleuse` exit 0
2. `swift test --package-path Souffleuse` exit 0 avec ≥151 tests passing
3. `bash Souffleuse/audit.sh` exit 0
4. `SuggestionPolicy.swift`, `SuggestionPolicy+Tuning.swift`, `RelevanceGateTests.swift` créés
5. Aucun seuil tuning n'apparaît comme literal numérique hors `SuggestionPolicy+Tuning.swift`
</success_criteria>

<output>
After completion, create `.planning/phases/04-cascade-quality-architecture/04-01-SUMMARY.md` listing :
- New files (3) avec line counts
- Tests added (≥12)
- Test total before/after
- Audit status
- Confirmation que `SuggestionSource` est désormais défini dans `SuggestionPolicy.swift` (non plus dans `PredictorViewModel.swift`)
</output>

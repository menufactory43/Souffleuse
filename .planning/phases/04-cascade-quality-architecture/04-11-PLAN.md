---
phase: 04-cascade-quality-architecture
plan: 11
type: execute
wave: 10
depends_on: [02, 03, 04, 05, 06, 07, 08, 09, 10]
files_modified:
  - .planning/phases/04-cascade-quality-architecture/04-VERIFICATION-mail.md
  - .planning/phases/04-cascade-quality-architecture/04-VERIFICATION-notes.md
  - .planning/phases/04-cascade-quality-architecture/04-VERIFICATION-brave.md
  - .planning/phases/04-cascade-quality-architecture/04-VERIFICATION.md
autonomous: false
requirements:
  - D-14
  - D-15
  - D-16
  - D-17
tags:
  - phase-04
  - real-app-verification
  - tier1-acceptance-gate

must_haves:
  truths:
    - "Souffleuse signed dev build runs on macOS 14+ Apple Silicon."
    - "Mail, Notes, Brave scenarios (5 each) executed per the protocol in RESEARCH §Real-app verification."
    - "Per-app verification files document scripted scenario outcomes + blind A/B verdicts."
    - "Roll-up 04-VERIFICATION.md emits Tier 1 acceptance gate verdict (ACCEPT / REVIEW)."
  artifacts:
    - path: ".planning/phases/04-cascade-quality-architecture/04-VERIFICATION-mail.md"
      provides: "Mail Tier 1 verification — 5 scripted scenarios + blind A/B + parasite rate"
    - path: ".planning/phases/04-cascade-quality-architecture/04-VERIFICATION-notes.md"
      provides: "Notes Tier 1 verification"
    - path: ".planning/phases/04-cascade-quality-architecture/04-VERIFICATION-brave.md"
      provides: "Brave Tier 1 verification"
    - path: ".planning/phases/04-cascade-quality-architecture/04-VERIFICATION.md"
      provides: "Roll-up : verdict global du milestone Phase 4"
---

<objective>
Vérification finale du milestone : la cascade ghost (post-refactor + Gate + classification grid + L1 re-enabled) atteint la parité Cotypist sur les 3 apps Tier 1 (Mail, Notes, Brave) selon le protocole D-14..D-17.

Cette étape est en partie MANUELLE (blind A/B daily-use ~½ journée par app). Les scripted scenarios sont auto-classifiés via les events `ghost_classified_*` agrégés.

**Acceptance gate (D-16)** : pour CHAQUE app Tier 1, les trois doivent tenir :
1. Classification grid pass D-11 sur les 5 scripted scenarios.
2. Blind A/B not-worse-than Cotypist ≥5/5 scripted.
3. Parasite rate < 5% dans toute fenêtre 30-min de daily-use.

Si une seule app rate, verdict global = REVIEW. Sinon ACCEPT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cascade-quality-architecture/04-CONTEXT.md
@.planning/phases/04-cascade-quality-architecture/04-RESEARCH.md
@.planning/phases/04-cascade-quality-architecture/04-07-SUMMARY.md
@.planning/phases/04-cascade-quality-architecture/04-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + sign + install dev bundle ready for verification</name>
  <files></files>
  <read_first>
    - Souffleuse/make-app.sh (lecture intégrale — comprendre le build/sign/install pipeline)
    - Souffleuse/audit.sh
  </read_first>
  <action>
    1. `bash Souffleuse/audit.sh` — exit 0 (gate avant build).

    2. `swift test --package-path Souffleuse` — exit 0 (gate).

    3. **Capturer le path canonique du bundle** en lisant `make-app.sh` AVANT le build. Le script écrit dans `Souffleuse/Souffleuse.app` (cwd-relative depuis la racine du repo). Confirmer en grep : `grep -E '(\.app|APP_DIR|OUT)' Souffleuse/make-app.sh | head -10`. **Exporter** : `export BUNDLE_PATH="Souffleuse/Souffleuse.app"`.

    4. **Marker mtime pré-build** pour garantir qu'on teste un build FRAIS, pas un résidu : `touch /tmp/04-09-build-marker && sleep 1` (sleep pour différencier les mtimes).

    5. Build le bundle `.app` signé : `bash Souffleuse/make-app.sh`. Vérifier la sortie. Verifier la signature : `codesign -dv "$BUNDLE_PATH" 2>&1`.

    6. **Asserter le bundle frais** : `test -d "$BUNDLE_PATH" && find "$BUNDLE_PATH" -newer /tmp/04-09-build-marker -name Souffleuse | head -1` — doit retourner le binaire. Si vide → build silencieusement échoué → STOP.

    7. Si le bundle existant tourne déjà → tuer le process : `pkill -f Souffleuse || true`.

    8. Démarrer le nouveau bundle : `open "$BUNDLE_PATH"`. Vérifier le démarrage via `pgrep -fl Souffleuse`.

    9. Vérifier que les permissions TCC sont accordées (Accessibility, Apple Events, Screen Recording si OCR utilisée). Si non : message clair au user via le log de cette task → STOP, demander au user d'accorder + relancer.

    10. Préparer la collecte de logs : `tail -f ~/Library/Logs/Souffleuse.log > /tmp/04-09-verification-session.jsonl &` (background) — la session de verification consigne les events `ghost_classified_*` ici.

    Si une étape échoue, la diagnostiquer avant de procéder aux tasks 2-4.
  </action>
  <verify>
    <automated>bash Souffleuse/audit.sh && swift test --package-path Souffleuse 2>&amp;1 | grep -E 'Test run with' && test -d Souffleuse/Souffleuse.app && find Souffleuse/Souffleuse.app -newer /tmp/04-09-build-marker -name Souffleuse -type f | head -1</automated>
  </verify>
  <acceptance_criteria>
    - "bash Souffleuse/audit.sh exits 0"
    - "swift test --package-path Souffleuse exits 0"
    - "test -d Souffleuse/Souffleuse.app returns 0"
    - "find Souffleuse/Souffleuse.app -newer /tmp/04-09-build-marker -name Souffleuse -type f returns at least one path (proves build is fresh, not a stale residual)"
    - "codesign -dv Souffleuse/Souffleuse.app 2>&1 contains 'Identifier=app.cocotypist.Souffleuse'"
  </acceptance_criteria>
  <done>Dev bundle prêt et démarré. Permissions OK. Log file streaming.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Vérification Mail Tier 1 — 5 scripted scenarios M1-M5 + blind A/B</name>
  <what-built>
    Le dev bundle Souffleuse tourne. Tests + audit verts. Classification grid (5 events ghost_classified_*) émise live. La cascade ghost integre le Gate + L1 re-enabled.

    Cette étape vérifie le comportement réel dans **Mail (com.apple.mail)** selon le protocole D-14/D-16.
  </what-built>
  <how-to-verify>
    **Setup** :
    1. Ouvrir Mail.app. Configurer une session de typing avec un compte mail existant (idéalement avec ≥10 mails reçus pour avoir du contexte).
    2. Vérifier que la session log est active : `tail -f ~/Library/Logs/Souffleuse.log` dans un terminal latéral.
    3. Créer `/tmp/04-09-mail-events.jsonl` pour les 30-min daily-use logs : `jq -c 'select(.event | startswith("ghost_classified"))' ~/Library/Logs/Souffleuse.log > /tmp/04-09-mail-events-baseline.jsonl` (capture le baseline avant la session).

    **Scripted scenarios (5 — voir RESEARCH §Real-app verification — Mail)** :

    | # | Scenario | Action | Expected category |
    |---|----------|--------|-------------------|
    | M1 | Reply to client email | Open thread → Reply → type `Bonjour Marie, suite à votre message du ` | `correct` ou `acceptable` |
    | M2 | Subject line empty | New Email → Subject field → type `Rdv ` | `correct` ou `acceptable` |
    | M3 | Mid-word completion | In body → type `Je voulais te dem` (no space) | `acceptable` (L0 mid-word) |
    | M4 | After-comma continuation | Type `D'accord pour demain, ` | `correct` ou `acceptable` |
    | M5 | Long-prefix divergence | Type 3 paragraphs, misspell, backspace 5 chars, retype | No `parasite` event |

    Pour chaque scenario, **observer** :
    - Ghost apparaît-il dans ≤2s ?
    - Pertinence : approximativement la category attendue ?
    - Tab accept → log `ghost_classified_correct` ou `_acceptable` ?
    - Esc dismiss → log `ghost_classified_useless` ?
    - Aucun flicker visible / replacement parasite ?

    **Blind A/B daily-use (≤30 min)** :
    - Utiliser Mail normalement pour ≤30 min de typing varié.
    - Pour chaque ghost notable : noter manuellement `S+` (Souffleuse better-than-or-equal Cotypist) ou `S-` (worse).
    - Cible : ≥5/5 scripted not-worse-than ; parasite rate < 5% sur la session.

    **Output attendu** : créer manuellement `.planning/phases/04-cascade-quality-architecture/04-VERIFICATION-mail.md` avec :
    - Date + git SHA du build testé
    - Tableau M1-M5 : scenario / observation / category observed / verdict ✓ ou ✗
    - Section "Blind A/B" : nb sessions, durée totale, S+/S- count
    - Section "Classification stats" : counts agrégés depuis les logs (utiliser `jq`)
    - Verdict app : `ACCEPT` ou `REVIEW`

    **Aggregate stats commande** :
    ```bash
    jq -c 'select(.event | startswith("ghost_classified"))' ~/Library/Logs/Souffleuse.log | \
      jq -r '.event' | sort | uniq -c
    ```

    **D-11 gates simulés** (calculer à partir des counts) :
    - correct/total ≥ 30% ?
    - (useless + bad + parasite)/total ≤ 35% ?
    - parasite/total ≤ 5% ?
  </how-to-verify>
  <resume-signal>Écrire "approved" si Mail VERIFICATION.md complete + verdict ACCEPT ou REVIEW. Sinon décrire les problèmes observés.</resume-signal>
  <files>.planning/phases/04-cascade-quality-architecture/04-VERIFICATION-mail.md</files>
  <action>Manual verification per <how-to-verify>. Execute M1-M5 scenarios in Mail.app, capture observations in 04-VERIFICATION-mail.md, run aggregate jq query on ~/Library/Logs/Souffleuse.log for classification counts, evaluate D-11 release gate.</action>
  <verify>test -f .planning/phases/04-cascade-quality-architecture/04-VERIFICATION-mail.md</verify>
  <done>04-VERIFICATION-mail.md exists with verdict (ACCEPT or REVIEW) per D-16.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Vérification Notes Tier 1 — 5 scripted scenarios N1-N5 + blind A/B</name>
  <what-built>
    Mail Task 2 complete. Maintenant Notes (com.apple.Notes).
  </what-built>
  <how-to-verify>
    **Setup identique à Mail** sauf que l'app cible est Notes.app.

    **Scripted scenarios (5 — RESEARCH §Real-app verification — Notes)** :

    | # | Scenario | Action | Expected |
    |---|----------|--------|----------|
    | N1 | Empty new note | Cmd+N → type `Liste courses : ` | `correct` ou `acceptable` (réalistic item) — beware "fortune cookie" hazard |
    | N2 | Mid-paragraph edit | Open existing note → click mid-paragraph → type 3 chars | `correct` matching afterCursor |
    | N3 | List bullet | Type `- Réunion à 14h\n- ` | `correct` ou `acceptable` continuation |
    | N4 | Code block typing | Type `def hello` (in code if Notes supports) | `acceptable` if `():` or `_world` |
    | N5 | Backspace then resume | Type sentence, backspace 8 chars, resume | Undo-as-ghost should restore (cache_undo_hit) |

    Mêmes observations + aggregate stats que Task 2.

    **Output** : `.planning/phases/04-cascade-quality-architecture/04-VERIFICATION-notes.md` avec mêmes sections.

    **Important — N1 "fortune cookie" hazard** : si le ghost propose des items génériques (pomme, lait, pain) sans contexte du user → c'est `useless`, pas `correct`. Distinguer.
  </how-to-verify>
  <resume-signal>"approved" si Notes VERIFICATION.md complete. Sinon décrire issues.</resume-signal>
  <files>.planning/phases/04-cascade-quality-architecture/04-VERIFICATION-notes.md</files>
  <action>Manual verification per <how-to-verify>. Execute N1-N5 scenarios in Notes.app, capture observations in 04-VERIFICATION-notes.md.</action>
  <verify>test -f .planning/phases/04-cascade-quality-architecture/04-VERIFICATION-notes.md</verify>
  <done>04-VERIFICATION-notes.md exists with verdict per D-16.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Vérification Brave Tier 1 — 5 scripted scenarios B1-B5 + blind A/B</name>
  <what-built>
    Mail + Notes Tasks complete. Maintenant Brave (com.brave.Browser) — le cas le plus difficile (Chromium AX fallback + OCR caret).
  </what-built>
  <how-to-verify>
    **Setup** :
    1. Ouvrir Brave.app.
    2. **Critical** : vérifier que l'AX activation sur Chromium fonctionne — si Souffleuse n'arrive pas à lire le caret dans Brave, ce sont les permissions Accessibility OU une régression dans l'extraction qui touche AppDelegate Chromium activation.
    3. OCR caret resolver doit être actif (CaretResolver) — vérifier dans Preferences que l'OCR est enabled.

    **Scripted scenarios (5 — RESEARCH §Real-app verification — Brave)** :

    | # | Scenario | Action | Expected |
    |---|----------|--------|----------|
    | B1 | Gmail compose body | Open Gmail → Compose → type 2 paragraphs | `correct` ≥1, no `parasite` |
    | B2 | Search bar query | Click address bar → type `restaur` | `acceptable` (word complete) ou `correct` (history) |
    | B3 | Twitter/X compose | New tweet → type `Just shipped ` | `acceptable` (EN continuation) |
    | B4 | Notion-in-browser block | Open Notion → type into block | `correct` ou `acceptable` — Electron AX activation test |
    | B5 | Mid-text edit Gmail | In partly-written email body → click mid-paragraph → type | `correct` matching afterCursor (mid-text Chromium = hardest) |

    Observer particulièrement :
    - B1 cascade churn : le LLM peut churner pendant le typing soutenu → `parasite` rate critical.
    - B5 caret OCR fallback : le ghost doit s'aligner sur le caret réel, pas sur une estimation fausse.

    **Output** : `.planning/phases/04-cascade-quality-architecture/04-VERIFICATION-brave.md`.

    Si Brave échoue de manière systémique (AX bloque), le verdict app est `REVIEW` mais documenter PRÉCISÉMENT la cause (permissions vs régression vs limitation Chromium).
  </how-to-verify>
  <resume-signal>"approved" si Brave VERIFICATION.md complete. Sinon décrire issues + indiquer si AX blockers ou cascade quality issues.</resume-signal>
  <files>.planning/phases/04-cascade-quality-architecture/04-VERIFICATION-brave.md</files>
  <action>Manual verification per <how-to-verify>. Execute B1-B5 scenarios in Brave.app, capture observations in 04-VERIFICATION-brave.md.</action>
  <verify>test -f .planning/phases/04-cascade-quality-architecture/04-VERIFICATION-brave.md</verify>
  <done>04-VERIFICATION-brave.md exists with verdict per D-16.</done>
</task>

<task type="auto">
  <name>Task 5: Roll-up 04-VERIFICATION.md — verdict global Phase 4</name>
  <files>.planning/phases/04-cascade-quality-architecture/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-cascade-quality-architecture/04-VERIFICATION-mail.md
    - .planning/phases/04-cascade-quality-architecture/04-VERIFICATION-notes.md
    - .planning/phases/04-cascade-quality-architecture/04-VERIFICATION-brave.md
    - .planning/phases/04-cascade-quality-architecture/04-08-REPLAY-RESULTS.md
  </read_first>
  <action>
    Créer `.planning/phases/04-cascade-quality-architecture/04-VERIFICATION.md` qui consolide les 3 fichiers per-app :

    ```markdown
    # Phase 4 Real-App Verification — Roll-up

    **Date** : <date>
    **Build SHA** : <git rev-parse HEAD>
    **Replay baseline** : 04-08-REPLAY-RESULTS.md

    ## Tier 1 Acceptance Gate (D-16)

    | App | Scripted (≥5/5) | Blind A/B (S+ ≥5/5) | 30-min parasite < 5% | Verdict |
    |-----|-----------------|---------------------|----------------------|---------|
    | Mail (com.apple.mail) | X/5 | X/5 | X.X% | ACCEPT / REVIEW |
    | Notes (com.apple.Notes) | X/5 | X/5 | X.X% | ACCEPT / REVIEW |
    | Brave (com.brave.Browser) | X/5 | X/5 | X.X% | ACCEPT / REVIEW |

    ## Tier 2 (Report-only)

    (Si Tier 2 a été exécuté en bonus : Safari, TextEdit, Intercom, Notion. Sinon skip cette section.)

    ## Classification stats (aggregate over verification sessions)

    | Event | Count | % of total ghosts |
    |-------|-------|-------------------|
    | ghost_classified_correct | X | X.X% |
    | ghost_classified_acceptable | X | X.X% |
    | ghost_classified_useless | X | X.X% |
    | ghost_classified_bad | X | X.X% |
    | ghost_classified_parasite | X | X.X% |
    | **Total** | X | 100% |

    **D-11 release gate (global)** :
    - correct/total ≥ 30% : <verdict>
    - (useless+bad+parasite)/total ≤ 35% : <verdict>
    - parasite/total ≤ 5% : <verdict>

    ## Verdict global Phase 4

    <choose> :
    - ✓ ACCEPT — les 3 apps Tier 1 passent. Milestone Cocotypist parité Cotypist atteint.
    - ✗ REVIEW — au moins une app rate. Action : <list> — investiguer + replanifier.

    ## Open issues for next milestone

    - (issues observés non-bloquants)
    ```

    Remplir avec les vraies valeurs extraites des 3 fichiers per-app.

    **Si verdict REVIEW** : créer aussi `.planning/phases/04-cascade-quality-architecture/04-FOLLOWUP.md` listant les bugs ou tunings à appliquer en hotfix (qui peut potentiellement être un Plan 04-10 si nécessaire — ou un milestone follow-up).
  </action>
  <verify>
    <automated>test -f .planning/phases/04-cascade-quality-architecture/04-VERIFICATION.md && test -f .planning/phases/04-cascade-quality-architecture/04-VERIFICATION-mail.md && test -f .planning/phases/04-cascade-quality-architecture/04-VERIFICATION-notes.md && test -f .planning/phases/04-cascade-quality-architecture/04-VERIFICATION-brave.md</automated>
  </verify>
  <acceptance_criteria>
    - "04-VERIFICATION.md exists"
    - "04-VERIFICATION-mail.md exists"
    - "04-VERIFICATION-notes.md exists"
    - "04-VERIFICATION-brave.md exists"
    - "grep -E 'ACCEPT|REVIEW' .planning/phases/04-cascade-quality-architecture/04-VERIFICATION.md returns >=1"
    - "grep -q 'D-11 release gate' .planning/phases/04-cascade-quality-architecture/04-VERIFICATION.md"
  </acceptance_criteria>
  <done>Phase 4 verification roll-up complete. Verdict du milestone connu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev bundle running with TCC granted | Accessibility/AppleEvents/ScreenRecording permissions accordées |
| Log file ~/Library/Logs/Souffleuse.log | StaticString events count-only — aucun user text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Information Disclosure | Log file contient potentiellement des bundle IDs visités | accept | Bundle IDs sont public knowledge ; count fields sont durations/lengths. Pas de user text. |
| T-04-09-02 | Information Disclosure | VERIFICATION-*.md files committed in repo | mitigate | Ne PAS coller de user text dans les VERIFICATION files. Stocker seulement les counts agrégés + verdicts. |
| T-04-09-03 | Repudiation | Manual verification subjective | accept | Protocole D-14 (scripted + blind A/B) fournit la rigueur ; verdict est manuel. |

**audit.sh** doit rester vert tout au long.
</threat_model>

<verification>
- Dev bundle built + signed + running
- 5 scenarios scripted exécutés pour chaque app Tier 1 (15 total)
- Blind A/B ≤ 30 min par app (~1.5 jour total)
- Per-app VERIFICATION files créés
- Roll-up VERIFICATION.md avec verdict final
</verification>

<success_criteria>
1. Dev bundle Souffleuse.app présent et fonctionnel
2. 04-VERIFICATION-{mail,notes,brave}.md créés et complétés
3. 04-VERIFICATION.md créé avec verdict ACCEPT ou REVIEW (explicit)
4. Classification stats agrégées dans le roll-up
5. D-11 release gate evaluation rendue explicite
</success_criteria>

<output>
After completion, create `.planning/phases/04-cascade-quality-architecture/04-09-SUMMARY.md` :
- Verdict global Phase 4 (ACCEPT / REVIEW)
- Apps Tier 1 status
- Total ghosts observed across verification sessions
- Open issues for next milestone (if any)
- Final phase 4 close-out reference
</output>

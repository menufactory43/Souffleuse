---
phase: 04-cascade-quality-architecture
plan: 10
type: execute
wave: 9
depends_on: [02, 09]
files_modified:
  - Souffleuse/Sources/SouffleuseCoherence/main.swift
  - Souffleuse/Sources/SouffleuseCoherence/replay-scenarios.json
autonomous: true
requirements:
  - D-12
tags:
  - phase-04
  - replay-harness
  - confusion-matrix

must_haves:
  truths:
    - "Scenario schema bumped from v1 → v2 with optional expectedCategory + expectedGhostPrefix fields."
    - "v1 scenarios continue to decode without error (backward compat)."
    - "renderReplayResults outputs a confusion matrix BEFORE the per-scenario detail table."
    - "Auto-classification helper: correct / acceptable / useless naive heuristic ; bad requires human signal ; parasite untestable in single-pass replay."
    - "≥6 existing scenarios extended with expectedGhostPrefix + expectedCategory."
  artifacts:
    - path: "Souffleuse/Sources/SouffleuseCoherence/main.swift"
      provides: "Scenario v2 fields + ExpectedCategory enum + classifyReplayGhost + confusion matrix rendering"
      min_lines: 700
    - path: "Souffleuse/Sources/SouffleuseCoherence/replay-scenarios.json"
      provides: "Scenarios v2 (≥6 scenarios with expectedGhostPrefix annotations)"
---

<objective>
Étendre le `SouffleuseCoherence --replay` harness pour produire une **confusion matrix** dans `REPLAY-RESULTS.md` (D-12). Le schema des scenarios passe de v1 → v2 avec deux champs optionnels (`expectedCategory`, `expectedGhostPrefix`) ; les v1 continuent de décoder sans erreur grâce à `Optional`.

Output : harness étendu + scenarios annotés + nouveau bloc de rendu markdown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cascade-quality-architecture/04-RESEARCH.md
@.planning/phases/04-cascade-quality-architecture/04-PATTERNS.md

<interfaces>
```swift
enum ExpectedCategory: String, Codable, Sendable, CaseIterable {
    case correct, acceptable, useless, bad, parasite, skip
}

extension Scenario {
    let expectedCategory: ExpectedCategory?     // NEW v2 optional
    let expectedGhostPrefix: String?            // NEW v2 optional
}

func classifyReplayGhost(ghost: String, expectedPrefix: String?) -> ExpectedCategory
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Étendre Scenario struct + ExpectedCategory enum + version bump 1→2 (backward compat)</name>
  <files>Souffleuse/Sources/SouffleuseCoherence/main.swift</files>
  <read_first>
    - Souffleuse/Sources/SouffleuseCoherence/main.swift (lignes 1-100 setup ; 200-260 Scenario struct + ScenarioFile ; 367-444 renderReplayResults)
    - .planning/phases/04-cascade-quality-architecture/04-RESEARCH.md (§"Replay harness extension shape (D-12)" lignes 819-927)
    - .planning/phases/04-cascade-quality-architecture/04-PATTERNS.md (§"main.swift MODIFIED" lignes 727-783)
  </read_first>
  <action>
    Modifier `Souffleuse/Sources/SouffleuseCoherence/main.swift` :

    1. **Bump version field** : la struct `ScenarioFile` continue d'accepter `version: Int`. Aucune validation stricte — accepte 1 ou 2. Ajouter un commentaire :
       ```swift
       struct ScenarioFile: Codable, Sendable {
           let version: Int   // v1 or v2; v2 adds optional expectedCategory + expectedGhostPrefix
           let scenarios: [Scenario]
       }
       ```

    2. **Ajouter `enum ExpectedCategory`** au top-level (ou imbriqué dans Scenario) :
       ```swift
       enum ExpectedCategory: String, Codable, Sendable, CaseIterable {
           case correct
           case acceptable
           case useless
           case bad
           case parasite
           case skip
       }
       ```

    3. **Étendre `Scenario`** avec les deux champs optionnels :
       ```swift
       struct Scenario: Codable, Sendable {
           // ... existing fields verbatim — ne pas toucher l'ordre ...
           let textAfterCaret: String?

           // ── Phase 4 v2 (optional — v1 scenarios decode unchanged) ──
           let expectedCategory: ExpectedCategory?
           let expectedGhostPrefix: String?
       }
       ```

       **Critique** : la conformance `Codable` synthesized de Swift accepte les `Optional` manquants → les v1 JSONs décoderont OK sans modification.

    4. **Ajouter `func classifyReplayGhost(ghost: String, expectedPrefix: String?) -> ExpectedCategory`** au top-level :
       ```swift
       func classifyReplayGhost(ghost: String, expectedPrefix: String?) -> ExpectedCategory {
           guard let expected = expectedPrefix, !expected.isEmpty else { return .skip }
           if ghost.isEmpty { return .useless }
           if ghost.lowercased().hasPrefix(expected.lowercased()) { return .correct }
           // bad detection requires human signal — naive replay classifies anything non-empty
           // and not matching as acceptable. parasite untestable in single-pass replay.
           return .acceptable
       }
       ```

    5. Compiler.
  </action>
  <verify>
    <automated>swift build --package-path Souffleuse --product SouffleuseCoherence 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - "grep -q 'enum ExpectedCategory: String, Codable, Sendable, CaseIterable' Souffleuse/Sources/SouffleuseCoherence/main.swift"
    - "grep -q 'let expectedCategory: ExpectedCategory?' Souffleuse/Sources/SouffleuseCoherence/main.swift"
    - "grep -q 'let expectedGhostPrefix: String?' Souffleuse/Sources/SouffleuseCoherence/main.swift"
    - "grep -q 'func classifyReplayGhost' Souffleuse/Sources/SouffleuseCoherence/main.swift"
    - "swift build --package-path Souffleuse --product SouffleuseCoherence exits 0"
  </acceptance_criteria>
  <done>Scenario v2 + ExpectedCategory + classifyReplayGhost compile. v1 backward compat préservé via Optional.</done>
</task>

<task type="auto">
  <name>Task 2: Étendre renderReplayResults avec confusion matrix + per-scenario classification</name>
  <files>Souffleuse/Sources/SouffleuseCoherence/main.swift</files>
  <read_first>
    - Souffleuse/Sources/SouffleuseCoherence/main.swift (post Task 1 — particulièrement renderReplayResults autour de L367-444)
    - .planning/phases/04-cascade-quality-architecture/04-RESEARCH.md (§"Output: confusion matrix" lignes 869-900)
  </read_first>
  <action>
    Étendre la fonction `renderReplayResults(...)` (ou équivalent) pour insérer un bloc Confusion Matrix AVANT la table per-scenario.

    1. **Pour chaque scenario result**, calculer l'`actual: ExpectedCategory` via `classifyReplayGhost(ghost: result.ghost, expectedPrefix: scenario.expectedGhostPrefix)`. Stocker dans une structure intermediate `ClassifiedResult { let scenarioId, expected: ExpectedCategory?, actual: ExpectedCategory, ghost: String }`.

    2. **Confusion matrix rendering** — inséré après le header preamble, avant la per-scenario detail table :
       ```swift
       var out = "..."  // existing preamble

       // ── Phase 4 D-12 — Confusion Matrix ──
       let classified = results.map { result -> (scenario: Scenario, actual: ExpectedCategory) in
           let scenario = findScenarioFor(result)
           return (scenario, classifyReplayGhost(ghost: result.ghost, expectedPrefix: scenario.expectedGhostPrefix))
       }
       let categoriesForMatrix: [ExpectedCategory] = [.correct, .acceptable, .useless, .bad]
       out += "\n## Confusion Matrix (D-12)\n\n"
       out += "|              | actual: correct | acceptable | useless | bad | total |\n"
       out += "|--------------|-----------------|------------|---------|-----|-------|\n"
       for expectedCat in categoriesForMatrix {
           let row = classified.filter { $0.scenario.expectedCategory == expectedCat }
           let counts = (
               correct: row.filter { $0.actual == .correct }.count,
               acceptable: row.filter { $0.actual == .acceptable }.count,
               useless: row.filter { $0.actual == .useless }.count,
               bad: row.filter { $0.actual == .bad }.count
           )
           let total = counts.correct + counts.acceptable + counts.useless + counts.bad
           out += "| **expected: \(expectedCat.rawValue)** | \(counts.correct) | \(counts.acceptable) | \(counts.useless) | \(counts.bad) | \(total) |\n"
       }
       // Totals row
       let totals = (
           correct: classified.filter { $0.actual == .correct }.count,
           acceptable: classified.filter { $0.actual == .acceptable }.count,
           useless: classified.filter { $0.actual == .useless }.count,
           bad: classified.filter { $0.actual == .bad }.count
       )
       let grandTotal = classified.count
       out += "| **total**                | \(totals.correct) | \(totals.acceptable) | \(totals.useless) | \(totals.bad) | \(grandTotal) |\n\n"

       // ── Release gate D-11 simulation ──
       let correctRate = Double(totals.correct) / Double(max(1, grandTotal))
       let lowQualityRate = Double(totals.useless + totals.bad) / Double(max(1, grandTotal))
       out += "### Release gate D-11 (simulated on replay — parasite untestable in single-pass)\n\n"
       out += "- \(correctRate >= 0.30 ? "✓" : "✗") correct/total ≥ 30% → \(totals.correct)/\(grandTotal) = \(String(format: "%.1f", correctRate * 100))%\n"
       out += "- \(lowQualityRate <= 0.35 ? "✓" : "✗") (useless+bad)/total ≤ 35% → \((totals.useless + totals.bad))/\(grandTotal) = \(String(format: "%.1f", lowQualityRate * 100))%\n"
       out += "- parasite/total ≤ 5% — untestable in single-pass replay (live production only)\n\n"
       ```

    3. **Per-scenario detail table** — ajouter une colonne "actual category" pour chaque scenario classifié.

    Ajouter à la ligne d'header de la table per-scenario : `| Actual category |` après `| Ghost |`.

    Build + smoke test.
  </action>
  <verify>
    <automated>swift build --package-path Souffleuse --product SouffleuseCoherence 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - "grep -q 'Confusion Matrix' Souffleuse/Sources/SouffleuseCoherence/main.swift"
    - "grep -q 'Release gate D-11' Souffleuse/Sources/SouffleuseCoherence/main.swift"
    - "grep -q 'classifyReplayGhost' Souffleuse/Sources/SouffleuseCoherence/main.swift"
    - "swift build --package-path Souffleuse --product SouffleuseCoherence exits 0"
  </acceptance_criteria>
  <done>renderReplayResults émet la confusion matrix + release gate simulation.</done>
</task>

<task type="auto">
  <name>Task 3: Annoter ≥6 scenarios existants avec expectedGhostPrefix + expectedCategory</name>
  <files>Souffleuse/Sources/SouffleuseCoherence/replay-scenarios.json</files>
  <read_first>
    - Souffleuse/Sources/SouffleuseCoherence/replay-scenarios.json (le fichier scenarios actuel — lire tous les scenarios pour comprendre leurs contextes)
    - .planning/phases/04-cascade-quality-architecture/04-RESEARCH.md (§"Real-app verification scenario list" pour les expected outcomes types)
  </read_first>
  <action>
    Annoter au moins 6 scenarios du fichier `replay-scenarios.json` avec les nouveaux champs.

    Pour chaque scenario annoté :
    - `expectedCategory`: la catégorie attendue d'après la quality grid (`correct`, `acceptable`, `useless`).
    - `expectedGhostPrefix`: la chaîne avec laquelle le ghost DOIT commencer pour être classifié `correct`.

    Exemple :
    ```json
    {
      "id": "fr-empty-email-subject",
      "label": "Empty email subject — Mail",
      "bundleID": "com.apple.mail",
      "windowTitle": "Nouveau message",
      "contextPrefix": "...",
      "userTail": "Rdv ",
      "placeholder": "Sujet",
      "expectedCategory": "correct",
      "expectedGhostPrefix": "demain"
    }
    ```

    Sélectionner les scenarios à annoter en priorité :
    1. Scenarios avec un contexte/prefix riche où l'output attendu est prévisible (par exemple, des templates ou patterns récurrents).
    2. Scenarios de Phase 1/2 dont le verdict humain (REPLAY-RESULTS antérieurs) indique un output reproductible.
    3. Au moins 1 scenario `expectedCategory: "useless"` (le ghost SHOULD be empty/generic, e.g. "ChatGPT-like fortune cookie").

    Si le fichier scenarios ne contient pas 6 scenarios, créer le delta. Si l'output exact n'est pas connu pour certains scenarios, laisser `expectedGhostPrefix: null` mais set `expectedCategory: "skip"`.

    Bump `version` du file de 1 à 2.
  </action>
  <verify>
    <automated>swift run --package-path Souffleuse SouffleuseCoherence --replay 2>&amp;1 | tail -10 ; grep -c 'expectedGhostPrefix' Souffleuse/Sources/SouffleuseCoherence/replay-scenarios.json</automated>
  </verify>
  <acceptance_criteria>
    - "test -f Souffleuse/Sources/SouffleuseCoherence/replay-scenarios.json"
    - "grep -c '\"expectedGhostPrefix\"' Souffleuse/Sources/SouffleuseCoherence/replay-scenarios.json returns >=6"
    - "grep -c '\"expectedCategory\"' Souffleuse/Sources/SouffleuseCoherence/replay-scenarios.json returns >=6"
    - "grep -q '\"version\": 2' Souffleuse/Sources/SouffleuseCoherence/replay-scenarios.json"
    - "swift build --package-path Souffleuse --product SouffleuseCoherence exits 0"
  </acceptance_criteria>
  <done>≥6 scenarios annotés. File bumped to v2.</done>
</task>

<task type="auto">
  <name>Task 4: Run replay + verify confusion matrix in REPLAY-RESULTS.md + audit</name>
  <files>.planning/phases/04-cascade-quality-architecture/04-08-REPLAY-RESULTS.md</files>
  <read_first>
    - Souffleuse/Sources/SouffleuseCoherence/main.swift (post Tasks 1-2) — voir `runReplay(scenariosPath:outPath:...)` lignes 446-490 pour comprendre que `--out` écrit verbatim au path donné, sinon legacy colocated `<dirname-of-scenarios>/REPLAY-RESULTS.md`
    - Souffleuse/Sources/SouffleuseCoherence/replay-scenarios.json (post Task 3)
  </read_first>
  <action>
    1. **Output path déterministe** — `runReplay()` (`Souffleuse/Sources/SouffleuseCoherence/main.swift:450-490`) écrit dans le path passé via `--out`, sinon dans `<dirname-of-scenarios>/REPLAY-RESULTS.md` (legacy colocated default). Run avec `--out` explicite pour éviter toute ambiguïté :

       ```bash
       OUT=".planning/phases/04-cascade-quality-architecture/04-08-REPLAY-RESULTS.md"
       SCENARIOS="Souffleuse/Sources/SouffleuseCoherence/replay-scenarios.json"
       swift run --package-path Souffleuse SouffleuseCoherence --replay \
         --scenarios "$SCENARIOS" --out "$OUT"
       ```

       Le binaire écrit le fichier verbatim au path `--out` ; pas de copie post-hoc nécessaire. Si l'option `--scenarios` n'est pas supportée par le CLI actuel, omettre — `runReplay` charge depuis le path par défaut compilé.

    2. Vérifier visuellement (via Read tool) que :
       - Le markdown contient une section "Confusion Matrix (D-12)"
       - La matrix table a la structure attendue (5 rows : correct/acceptable/useless/bad/total ; 5 cols : actual cat + total)
       - La section "Release gate D-11" affiche les 3 metrics avec ✓/✗

    3. (Plus de copie post-hoc — l'étape 1 écrit déjà directement dans `04-08-REPLAY-RESULTS.md` via `--out`.)

    4. `bash Souffleuse/audit.sh` — exit 0. (Le harness Coherence n'est PAS un shipping target → l'audit n'analyse pas ses sources. Mais vérifier quand même.)

    5. `swift test --package-path Souffleuse` — exit 0, tests inchangés (le harness est une CLI, pas de nouveau test ici).
  </action>
  <verify>
    <automated>swift run --package-path Souffleuse SouffleuseCoherence --replay 2>&amp;1 | tail -5 ; bash Souffleuse/audit.sh && swift test --package-path Souffleuse 2>&amp;1 | grep -E 'Test run with' ; test -f .planning/phases/04-cascade-quality-architecture/04-08-REPLAY-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - "bash Souffleuse/audit.sh exits 0"
    - "swift test --package-path Souffleuse exits 0"
    - "test -f .planning/phases/04-cascade-quality-architecture/04-08-REPLAY-RESULTS.md"
    - "grep -q 'Confusion Matrix' .planning/phases/04-cascade-quality-architecture/04-08-REPLAY-RESULTS.md"
    - "grep -q 'Release gate D-11' .planning/phases/04-cascade-quality-architecture/04-08-REPLAY-RESULTS.md"
  </acceptance_criteria>
  <done>Replay harness produit la confusion matrix. Baseline REPLAY-RESULTS Phase 4 captée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local file I/O — replay scenarios JSON | Aucun input externe ; fichier checked in repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Tampering | v1 scenarios fail to decode | mitigate | Optional fields synthesized — verbatim Codable behavior. Test : un v1 JSON test décodé sans erreur. |
| T-04-08-02 | Information Disclosure | REPLAY-RESULTS.md leak user data | accept | Replay scenarios are scripted, no real user data. The harness is dev-only. |

**audit.sh** : SouffleuseCoherence n'est pas dans la liste des shipping targets de l'audit (vérifier `audit.sh` pour confirmation), donc cette extension n'affecte pas les invariants production.
</threat_model>

<verification>
- Scenario schema v2 (optional expectedCategory + expectedGhostPrefix)
- v1 scenarios continue de décoder
- renderReplayResults émet confusion matrix + release gate D-11 simulation
- ≥6 scenarios annotés
- 04-08-REPLAY-RESULTS.md captured
- Audit 6/6 (Coherence n'est pas shipping)
</verification>

<success_criteria>
1. `swift build --package-path Souffleuse --product SouffleuseCoherence` exit 0
2. `swift run --package-path Souffleuse SouffleuseCoherence --replay` produit un markdown valide
3. `bash Souffleuse/audit.sh` exit 0
4. 04-08-REPLAY-RESULTS.md contient une Confusion Matrix
5. ≥6 scenarios annotés avec v2 fields
</success_criteria>

<output>
After completion, create `.planning/phases/04-cascade-quality-architecture/04-08-SUMMARY.md` :
- Scenarios annotated (count + IDs)
- Confusion matrix sample (excerpt)
- D-11 release gate verdict on replay
</output>

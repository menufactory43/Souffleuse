---
quick_id: 260604-k1v
type: execute
autonomous: false
files_modified:
  - Souffleuse/Package.swift
  - Souffleuse/Sources/Souffleuse/UpdaterController.swift
  - Souffleuse/Sources/Souffleuse/SouffleuseAppDelegate.swift
  - Souffleuse/Resources/Info.plist
  - Souffleuse/make-app.sh
  - .planning/quick/260604-k1v-int-grer-sparkle-2-maj-manuel-only-spm-n/SPARKLE-RELEASE.md
locked_decisions:
  - "Update mode = MANUAL ONLY (SUEnableAutomaticChecks=false, no SUScheduledCheckInterval)"
  - "Vendoring = SPM (sparkle-project/Sparkle, from 2.6.0)"
  - "Distribution = signed Developer ID, NON notarized beta channel (NOTARIZE=0)"
  - "SUFeedURL = https://souffleuse.app/appcast.xml"
  - "EdDSA: private key in Keychain (manual step), placeholder SUPublicEDKey + TODO in Info.plist"
---

<objective>
Intégrer Sparkle 2 (mises à jour manuelles uniquement) dans Souffleuse.app : dépendance SPM
sur le target exécutable, un `UpdaterController` `@MainActor` autour de `SPUStandardUpdaterController`
avec auto-checks DÉSACTIVÉS, un item de menu « Vérifier les mises à jour… », les clés Sparkle
dans Info.plist (avec placeholder de clé publique), et l'embarquement + signature inside-out de
`Sparkle.framework` (et ses bundles XPC imbriqués) dans `make-app.sh`.

Purpose: donner un canal de mise à jour beta sans violer le zero-leak (ARCHITECTURE.md:339 —
"pas de Sparkle auto-check en v1 ; l'utilisateur clique pour vérifier") ni la promesse onboarding
(ARCHITECTURE.md:381 — "Ne se met pas à jour sans votre demande").

Output: app capable de vérifier les MAJ sur demande explicite ; doc dev pour générer les clés
EdDSA et signer une release.

⚠️ Cadrage quick : 5 tâches pour des concerns distincts (SPM, code+wiring, plist, make-app.sh, doc).
C'est la limite haute du mode quick. Chaque tâche reste atomique. Pas de split recommandé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@./ARCHITECTURE.md

<interfaces>
<!-- Sparkle 2 — API consommée (binary XCFramework via SPM). -->
<!-- SPUStandardUpdaterController est @MainActor ; instancier dans init de l'app. -->
import Sparkle

// Wrapper standard recommandé par Sparkle :
//   SPUStandardUpdaterController(startingUpdater: Bool,
//                               updaterDelegate: SPUUpdaterDelegate?,
//                               userDriverDelegate: SPUStandardUserDriverDelegate?)
// expose `.updater: SPUUpdater` et `.checkForUpdates(_:)` (selector AppKit-friendly).
// `updater.automaticallyChecksForUpdates = false` (redondant avec Info.plist mais explicite).
// `updater.checkForUpdates()` lance la vérification UI sur action utilisateur.

<!-- Menu existant (SouffleuseAppDelegate.swift ~L706-770) : NSStatusItem + NSMenu, -->
<!-- items créés via NSMenuItem(title:action:keyEquivalent:) + #selector, target=self. -->
<!-- Insérer le nouvel item près de « Préférences… » (L749) avant le séparateur Quitter. -->

<!-- Action @objc pattern existant (AppDelegate L966 openPreferences, L939 openOnboarding). -->
</interfaces>

<facts>
- Entitlements (Souffleuse.entitlements) contiennent DÉJÀ allow-jit,
  allow-unsigned-executable-memory, disable-library-validation → Sparkle 2 tourne sous
  hardened runtime sans entitlement supplémentaire. AUCUNE modif entitlements requise.
- audit.sh : Sources/Souffleuse EST un SHIPPING_DIR → UpdaterController.swift ne doit
  contenir NI print() NI NSLog NI os_log de texte user. Utiliser la façade `Log` (event
  StaticString) si journalisation. Sparkle lui-même n'oblige aucun print côté app.
- audit.sh ne vérifie PAS le réseau → l'I/O réseau de Sparkle ne déclenche aucune alerte.
- Build hybride : make-app.sh lance `xcodebuild -scheme Souffleuse` puis fabrique le bundle
  À LA MAIN (cp binaire + dylibs llama dans Contents/Frameworks, signature inside-out
  L86-96). Sparkle.framework doit être copié + signé de la même façon (XPC imbriqués d'abord).
- Sparkle 2 NE doit toucher AUCUN library target ni le testTarget (sinon import Sparkle dans
  les tests / xctest). Dépendance UNIQUEMENT sur le target exécutable `Souffleuse`.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter Sparkle comme dépendance SPM (target exécutable Souffleuse uniquement)</name>
  <files>Souffleuse/Package.swift</files>
  <action>
    Dans `dependencies` (L36-38), ajouter après la ligne mlx-swift-examples :
      `.package(url: "https://github.com/sparkle-project/Sparkle", from: "2.6.0"),`
    Dans le SEUL target `.executableTarget(name: "Souffleuse", …)` (L177-194), ajouter à
    la fin de la liste `dependencies` :
      `.product(name: "Sparkle", package: "Sparkle"),`
    NE PAS toucher au testTarget `SouffleuseTests`, ni à aucun `.library`/autre exécutable.
    Sparkle est livré en binary XCFramework via SPM — pas de compilation source.
    Rationale (commentaire inline `//`): canal de MAJ beta, manuel-only ; ne doit pas fuiter
    dans les library targets ni les tests (sinon import Sparkle dans la suite).
  </action>
  <verify>
    <automated>cd Souffleuse && swift package resolve && grep -c 'package: "Sparkle"' Package.swift</automated>
  </verify>
  <done>`swift package resolve` télécharge Sparkle 2.x ; Package.resolved muté ; Sparkle
  apparaît uniquement dans le target Souffleuse (grep retourne 1).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: UpdaterController @MainActor + item de menu « Vérifier les mises à jour… »</name>
  <files>Souffleuse/Sources/Souffleuse/UpdaterController.swift, Souffleuse/Sources/Souffleuse/SouffleuseAppDelegate.swift</files>
  <action>
    A) Créer `Sources/Souffleuse/UpdaterController.swift` :
       - `import Sparkle`, `import AppKit`.
       - `@MainActor final class UpdaterController` (un type principal par fichier, nommé d'après
         le type, conforme CLAUDE.md).
       - Stocke `private let controller: SPUStandardUpdaterController`.
       - `init()` : instancie
         `SPUStandardUpdaterController(startingUpdater: true, updaterDelegate: nil, userDriverDelegate: nil)`.
         Puis FORCE `controller.updater.automaticallyChecksForUpdates = false` (défense en
         profondeur, redondant avec Info.plist SUEnableAutomaticChecks=false — commenter le pourquoi :
         ARCHITECTURE.md:339 zero-leak, pas de poll passif).
       - `func checkForUpdates()` : appelle `controller.updater.checkForUpdates()`.
       - PAS de print/NSLog/os_log (Sources/Souffleuse = SHIPPING_DIR). Si log voulu : `Log.info`
         avec event StaticString uniquement.
       - `///` doc sur la classe : rôle = mise à jour MANUELLE only, déclenchée par clic utilisateur.
    B) Dans `SouffleuseAppDelegate` :
       - Ajouter une propriété stockée `private let updater = UpdaterController()` (l'AppDelegate
         est déjà `@MainActor`). L'init au plus tôt (au moins avant l'affichage du menu) pour que
         `startingUpdater: true` arme Sparkle au lancement.
       - Construction du menu (~L749, juste après l'item « Préférences… ») : insérer
         `let updateItem = NSMenuItem(title: "Vérifier les mises à jour…", action: #selector(checkForUpdates), keyEquivalent: "")`
         `updateItem.target = self ; menu.addItem(updateItem)`.
       - Ajouter `@objc private func checkForUpdates() { updater.checkForUpdates() }` (miroir du
         pattern openPreferences L966).
  </action>
  <verify>
    <automated>cd Souffleuse && swift build --target Souffleuse 2>&1 | tail -20 && ./audit.sh | grep -E 'AUDIT (PASSED|FAILED)'</automated>
  </verify>
  <done>`swift build` compile le target Souffleuse avec Sparkle ; audit.sh affiche
  AUDIT PASSED (pas de print/NSLog introduit dans UpdaterController.swift) ; l'item de menu
  « Vérifier les mises à jour… » est branché sur `checkForUpdates()`.</done>
</task>

<task type="auto">
  <name>Task 3: Clés Sparkle dans Info.plist (manuel-only, placeholder SUPublicEDKey)</name>
  <files>Souffleuse/Resources/Info.plist</files>
  <action>
    Ajouter dans le `<dict>` racine (avant `</dict>` final) ces clés Sparkle :
      `<key>SUFeedURL</key><string>https://souffleuse.app/appcast.xml</string>`
      `<key>SUEnableAutomaticChecks</key><false/>`
      `<key>SUPublicEDKey</key><string>PLACEHOLDER_REMPLACER_PAR_LA_CLE_PUBLIQUE_EDDSA</string>`
    NE PAS ajouter `SUScheduledCheckInterval` (décision verrouillée : aucun poll passif).
    Ajouter un commentaire XML au-dessus de SUPublicEDKey :
      `<!-- TODO: remplacer par la cle publique base64 produite par generate_keys -->`
      `<!-- (cf. SPARKLE-RELEASE.md). NE JAMAIS committer de cle PRIVEE. -->`
    NE PAS inventer de clé d'apparence réelle — laisser le placeholder explicite.
    Rationale SUFeedURL : HTTPS obligatoire (ATS/Sparkle refusent http://).
  </action>
  <verify>
    <automated>cd Souffleuse && /usr/libexec/PlistBuddy -c "Print :SUFeedURL" -c "Print :SUEnableAutomaticChecks" -c "Print :SUPublicEDKey" Resources/Info.plist && (/usr/libexec/PlistBuddy -c "Print :SUScheduledCheckInterval" Resources/Info.plist 2>/dev/null && echo "FAIL-schedule-present" || echo "OK-no-schedule")</automated>
  </verify>
  <done>PlistBuddy lit SUFeedURL (https://souffleuse.app/appcast.xml), SUEnableAutomaticChecks=false,
  SUPublicEDKey=placeholder ; SUScheduledCheckInterval ABSENT (sortie « OK-no-schedule »).</done>
</task>

<task type="auto">
  <name>Task 4: Embarquer + signer Sparkle.framework (XPC imbriqués inside-out) dans make-app.sh</name>
  <files>Souffleuse/make-app.sh</files>
  <action>
    Sparkle 2 se résout via SPM en `Sparkle.framework` (binary XCFramework). xcodebuild peut NE
    PAS l'embarquer automatiquement dans ce flux bundle-à-la-main → il faut le copier + signer
    explicitement, comme les dylibs llama (L48-56 copie, L86-96 signature inside-out).

    Le framework contient des bundles imbriqués qui DOIVENT être signés AVANT le framework outer
    (signature inside-out, sinon signature outer invalide) :
      Sparkle.framework/Versions/B/Autoupdate
      Sparkle.framework/Versions/B/Updater.app           (+ son binaire MacOS/Updater)
      Sparkle.framework/Versions/B/XPCServices/Downloader.xpc
      Sparkle.framework/Versions/B/XPCServices/Installer.xpc

    A) LOCALISER le framework résolu. Après le build, le chemin SPM/xcodebuild typique :
       `build/SourcePackages/checkouts/Sparkle/...` (artifact) OU dans
       `$BUILD_DIR/Sparkle.framework` si xcodebuild le copie. AJOUTER une ligne qui RÉSOUT le
       chemin réel et ÉCHOUE bruyamment s'il est introuvable :
         `SPARKLE_FW=$(find build -name 'Sparkle.framework' -type d | head -1)`
         `[ -n "$SPARKLE_FW" ] || { echo "Sparkle.framework introuvable — verifier la resolution SPM"; exit 1; }`
       (Si introuvable car xcodebuild ne le sort pas, c'est exactement l'incertitude à révéler ici
       plutôt que deviner — l'échec explicite déclenche l'enquête.)

    B) COPIER : `cp -R "$SPARKLE_FW" "$APP_BUNDLE/Contents/Frameworks/Sparkle.framework"`
       (après la boucle de copie des dylibs llama, ~L56).

    C) SIGNER inside-out, AVANT le bloc de signature dylib L86-89 (ou juste après, mais avant la
       signature de l'exécutable principal L91). Ajouter, en utilisant `$SIGN_IDENTITY`,
       `--options runtime` et `$TIMESTAMP_FLAG` (cohérent avec le reste du script) :
         SP="$APP_BUNDLE/Contents/Frameworks/Sparkle.framework"
         for nested in \
           "$SP/Versions/B/XPCServices/Downloader.xpc" \
           "$SP/Versions/B/XPCServices/Installer.xpc" \
           "$SP/Versions/B/Updater.app" \
           "$SP/Versions/B/Autoupdate"; do
           [ -e "$nested" ] || continue
           codesign --force --sign "$SIGN_IDENTITY" --options runtime $TIMESTAMP_FLAG "$nested"
         done
         codesign --force --sign "$SIGN_IDENTITY" --options runtime $TIMESTAMP_FLAG "$SP"
       NE PAS passer d'entitlements aux composants Sparkle (comme les dylibs : entitlements
       seulement sur l'exécutable principal). Garder un commentaire expliquant l'ordre inside-out
       et que les bundles XPC imbriqués de Sparkle 2 doivent chacun être signés.

    NE PAS toucher au flux NOTARIZE (décision : non notarisé en beta, NOTARIZE=0 déjà supporté).
  </action>
  <verify>
    <automated>cd Souffleuse && RELEASE=1 NOTARIZE=0 ./make-app.sh && APP="build/Build/Products/Release/Souffleuse.app" && otool -L "$APP/Contents/MacOS/Souffleuse" | grep -i sparkle && codesign -dv --deep --strict "$APP/Contents/Frameworks/Sparkle.framework" 2>&1 | grep -i 'Identifier\|Signature' && codesign --verify --deep --strict "$APP" && echo "BUNDLE-SIGN-OK"</automated>
  </verify>
  <done>make-app.sh produit le bundle avec Sparkle.framework dans Contents/Frameworks ;
  `otool -L` montre le binaire principal liant Sparkle via @rpath ; `codesign --verify --deep
  --strict` sur l'app passe (XPC imbriqués + framework + app tous signés inside-out) →
  « BUNDLE-SIGN-OK ». Si Sparkle.framework est introuvable à l'étape A, le script échoue
  bruyamment : l'exécuteur doit alors investiguer l'embed xcodebuild (option Embed & Sign ou
  copie depuis SourcePackages) et NE PAS deviner.</done>
</task>

<task type="auto">
  <name>Task 5: Doc dev — génération clés EdDSA, signature release, template appcast.xml</name>
  <files>.planning/quick/260604-k1v-int-grer-sparkle-2-maj-manuel-only-spm-n/SPARKLE-RELEASE.md</files>
  <action>
    Créer une doc dev Markdown couvrant :
    1) **Génération des clés EdDSA (étape MANUELLE, une seule fois)** : exécuter le binaire
       `generate_keys` fourni par Sparkle (situé dans le checkout SPM
       `build/SourcePackages/artifacts/sparkle/Sparkle/bin/generate_keys` — donner la commande
       `find build -name generate_keys`). Il stocke la clé PRIVÉE dans le trousseau de session
       macOS et imprime la clé PUBLIQUE base64. Copier cette clé publique dans Info.plist
       (`SUPublicEDKey`, remplace le placeholder). ⚠️ NE JAMAIS committer la clé privée ni
       l'exporter dans le repo.
    2) **Signer une release** : après `RELEASE=1 NOTARIZE=0 ./make-app.sh` (DMG Developer ID non
       notarisé), signer l'archive avec `sign_update` (même répertoire bin) :
       `find build -name sign_update`, puis `sign_update build/Souffleuse.dmg` → fournit
       `sparkle:edSignature` + `length` à coller dans l'appcast.
    3) **Template appcast.xml** (vit sur le repo SITE Vercel, PAS ici — fournir l'exemple) :
       bloc `<rss xmlns:sparkle=…>` avec un `<item>` : `<title>`, `<sparkle:version>` (=
       CFBundleVersion), `<sparkle:shortVersionString>`, `<enclosure url="https://souffleuse.app/…dmg"
       sparkle:edSignature="…" length="…" type="application/octet-stream"/>`,
       `<sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>`.
    4) **Rappels** : mode manuel-only (SUEnableAutomaticChecks=false, pas de
       SUScheduledCheckInterval — ne pas réintroduire). Beta = non notarisé → friction Gatekeeper
       1er lancement acceptée. SUFeedURL en HTTPS obligatoire.
    5) **Follow-ups hors-scope** : déployer appcast.xml + DMG sur le site Vercel ; exécuter
       generate_keys/sign_update (manuels, touchent le trousseau) ; notarisation future ;
       éventuel passage en auto-update (refusé en v1 par ARCHITECTURE.md:339).
  </action>
  <verify>
    <automated>test -f .planning/quick/260604-k1v-int-grer-sparkle-2-maj-manuel-only-spm-n/SPARKLE-RELEASE.md && grep -c 'generate_keys\|sign_update\|appcast' .planning/quick/260604-k1v-int-grer-sparkle-2-maj-manuel-only-spm-n/SPARKLE-RELEASE.md</automated>
  </verify>
  <done>SPARKLE-RELEASE.md existe et documente generate_keys, sign_update, le template
  appcast.xml, les rappels manuel-only/HTTPS/non-notarisé, et les follow-ups hors-scope.</done>
</task>

</tasks>

<verification>
- `cd Souffleuse && swift package resolve` résout Sparkle 2.x sans casser les autres deps.
- `swift build --target Souffleuse` compile (Sparkle importé uniquement dans l'exécutable).
- `swift test` (ou la suite ~640 @Test) reste verte — Sparkle n'est PAS dans le testTarget.
- `./audit.sh` → AUDIT PASSED (aucun print/NSLog/os_log user dans UpdaterController.swift).
- `RELEASE=1 NOTARIZE=0 ./make-app.sh` produit un bundle dont `codesign --verify --deep --strict` passe.
- Info.plist : SUEnableAutomaticChecks=false, SUFeedURL HTTPS, SUPublicEDKey placeholder,
  SUScheduledCheckInterval ABSENT.
</verification>

<success_criteria>
- L'app expose « Vérifier les mises à jour… » dans le menu-bar ; aucun check passif (manuel-only).
- Sparkle.framework + ses XPC imbriqués embarqués et signés inside-out dans Contents/Frameworks.
- Décisions verrouillées respectées : SPM, manuel-only, Developer ID non notarisé, HTTPS feed,
  clé publique placeholder (aucun secret committé).
- Suite de tests verte, audit.sh vert, build signé valide.
</success_criteria>

<risks>
- **Embed xcodebuild incertain** : il n'est pas garanti que `xcodebuild` (flux bundle-à-la-main)
  dépose `Sparkle.framework` à un endroit prévisible. Task 4 le RÉVÈLE par un `find` + échec
  bruyant plutôt que de deviner. Si introuvable : investiguer l'option « Embed & Sign » dans le
  scheme Xcode, ou copier depuis `build/SourcePackages/artifacts/…`. NE PAS fabriquer un chemin.
- **TCC re-grant** : ajouter un framework embarqué change le bundle/cdhash → un re-grant TCC
  unique peut être nécessaire au prochain lancement. Non bloquant (cf. make-app.sh L104-105).
- **Tests** : si l'import Sparkle fuit dans une library/testTarget, la suite casse. Atténué par
  Task 1 (Sparkle uniquement sur le target exécutable). Vérifier que `swift test` reste vert.
</risks>

<output>
After completion, create
`.planning/quick/260604-k1v-int-grer-sparkle-2-maj-manuel-only-spm-n/260604-k1v-SUMMARY.md`
</output>

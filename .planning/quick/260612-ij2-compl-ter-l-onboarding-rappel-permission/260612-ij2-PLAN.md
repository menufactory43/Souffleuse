---
phase: quick-260612-ij2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Souffleuse/Sources/Souffleuse/OnboardingWindow.swift
  - Souffleuse/Sources/Souffleuse/SouffleuseAppDelegate.swift
autonomous: true
requirements: [IJ2-01, IJ2-02, IJ2-03, IJ2-04, IJ2-05]
must_haves:
  truths:
    - "Si une permission requise (Accessibilité OU Surveillance des entrées) manque APRÈS un onboarding déjà complété, le wizard se rouvre directement à l'étape permissions (pas à Bienvenue)."
    - "L'étape « Comment ça marche » propose un vrai champ texte où le souffle apparaît au caret (avec repli sur la maquette statique si la voix n'est pas prête)."
    - "L'étape finale offre une case « Lancer au démarrage » décochée par défaut, câblée sur SMAppService."
    - "L'étape permissions affiche une ligne de réassurance mots de passe sous les cartes."
    - "L'écran de bienvenue affiche le logo de l'app au-dessus du titre."
  artifacts:
    - path: "Souffleuse/Sources/Souffleuse/OnboardingWindow.swift"
      provides: "WelcomeStepView logo, PermissionsStepView réassurance, HowItWorksStepView essai réel + fallback, DoneStepView toggle launch-at-login"
      contains: "GhostTryField"
    - path: "Souffleuse/Sources/Souffleuse/SouffleuseAppDelegate.swift"
      provides: "shouldShowOnboarding inclut le check Input Monitoring; reprise à l'étape permissions si déjà onboardé; canTryGhost closure"
      contains: "IOHIDCheckAccess"
  key_links:
    - from: "SouffleuseAppDelegate.makeOnboardingWindow"
      to: "OnboardingWindow.init(canTryGhost:)"
      via: "closure { [predictor] in predictor.isModelReady }"
      pattern: "canTryGhost"
    - from: "shouldShowOnboarding"
      to: "IOHIDCheckAccess(kIOHIDRequestTypeListenEvent)"
      via: "gate Input Monitoring"
      pattern: "IOHIDCheckAccess"
---

<objective>
Compléter le wizard d'onboarding (livré en 260612-gto) avec 5 ajouts ciblés, sans casser la reprise d'étape versionnée ni la complétion existante :

1. **Rappel permission perdue** — `shouldShowOnboarding()` ne re-montre PAS le wizard quand « Surveillance des entrées » saute (symptôme : ghost visible mais Tab inerte). Ajouter ce check ET, pour un utilisateur DÉJÀ onboardé à qui il manque une permission, rouvrir le wizard directement à l'étape permissions.
2. **Essai réel du souffle** — remplacer la maquette statique de « Comment ça marche » par un vrai champ texte où le souffle apparaît au caret. Repli sur la maquette si la voix n'est pas prête.
3. **Lancer au démarrage** — Toggle SMAppService (pattern exact de PreferencesWindow) sur l'étape finale, décoché par défaut.
4. **Réassurance mots de passe** — ligne discrète lock + texte sur l'étape permissions.
5. **Logo Bienvenue** — icône de l'app au-dessus du titre de WelcomeStepView.

Purpose: l'onboarding doit rassurer, faire essayer pour de vrai, et ne jamais laisser un utilisateur avec un ghost muet sans recours.
Output: OnboardingWindow.swift et SouffleuseAppDelegate.swift modifiés ; suite verte ; audit.sh vert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Contrats existants, extraits du codebase. L'executor utilise ceux-ci directement. -->

OnboardingWindow.swift (état actuel, déjà lu) :
- `enum OnboardingStep: Int` : welcome=0, permissions=1, language=2, voice=3, howItWorks=4, done=5.
- `OnboardingModel` (@MainActor @Observable) : `axGranted`, `imGranted`, `screenGranted`, `selectedLanguage`. `refreshPermissions()` lit déjà `IOHIDCheckAccess(kIOHIDRequestTypeListenEvent) == kIOHIDAccessTypeGranted` (donc `import IOKit.hid` déjà présent dans CE fichier).
- `OnboardingWindow.init(...)` actuel : params `modelDownloads`, `ghostProvider`, `ghostReady`, `translation`, `initialLanguage`, `onLanguageChange`, `onGhostInstalled`, `onFinished`, `initialStep`, `onProgress`.
- `OnboardingRootView` détient ces mêmes params + `close`. `HowItWorksStepView()` est instancié sans argument dans `stepContent` (case .howItWorks). `DoneStepView(manager:ghostProvider:ghostReady:axGranted:imGranted:onFinished:)`.
- Couleur action : `Color.sangDeBoeuf` (private extension, déjà dans le fichier). Boutons : `SangDeBoeufButtonStyle`. Titres serif : `.font(.system(size: 22, weight: .semibold, design: .serif))`. Didascalies : `.font(.system(size: 13, design: .serif)).italic().foregroundStyle(.secondary)`.

SouffleuseAppDelegate.swift :
- `private let predictor = PredictorViewModel()` (l.107). `predictor.isModelReady` (PredictorViewModel l.351) renvoie `runtime.canGenerate` — c'est le booléen « voix prête à générer ». @MainActor.
- `private static let onboardingVersion = 1` (l.183).
- `shouldShowOnboarding()` (l.582-603) : actuellement `onboarded && AXClient.isTrusted && ghostReady` → false. NE teste PAS Input Monitoring.
- `makeOnboardingWindow()` (l.608-656) construit l'`OnboardingWindow` avec `initialStep: resumeStep` (= `UserDefaults.standard.integer(forKey: "onboardingProgressStep")`).
- Le fichier N'IMPORTE PAS `IOKit.hid` (imports l.1-13). Il faut l'ajouter.

PreferencesWindow.swift (pattern launch-at-login à reproduire) :
```swift
@State private var launchAtLogin: Bool = SMAppService.mainApp.status == .enabled
// ...
Toggle("Lancer au démarrage du Mac", isOn: $launchAtLogin)
    .onChange(of: launchAtLogin) { _, on in
        do {
            if on { try SMAppService.mainApp.register() }
            else { try SMAppService.mainApp.unregister() }
        } catch {
            launchAtLogin = SMAppService.mainApp.status == .enabled
        }
    }
```
(`import ServiceManagement` requis — déjà importé dans PreferencesWindow, PAS dans OnboardingWindow.)

tick() gates (l.1376-1384) que l'essai réel doit satisfaire — notre propre bundle `app.cocotypist.Souffleuse` n'est PAS blocklisté : bundleID non-nil, !isSecureField, !isSearchField, !isAddressBar, !isPickerField, text+caretIndex non-nil, isTextElement. Un `NSTextField`/`NSTextView` AppKit expose ces propriétés AX de façon fiable (contrairement à un TextField SwiftUI).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shouldShowOnboarding — check Input Monitoring + reprise à l'étape permissions</name>
  <files>Souffleuse/Sources/Souffleuse/SouffleuseAppDelegate.swift</files>
  <action>
Point (1) du brief. Deux changements dans SouffleuseAppDelegate.swift.

**1a. Import.** Ajouter `import IOKit.hid` à la liste d'imports (l.1-13). Le placer en ordre alphabétique cohérent avec le bloc (après `import Foundation`, avant `import Observation` convient ; respecter l'ordre existant Apple-frameworks-puis-Souffleuse — mettre `import IOKit.hid` juste après `import Foundation`).

**1b. Helper Input Monitoring.** Ajouter une propriété calculée privée `@MainActor` sur l'AppDelegate, juste au-dessus de `shouldShowOnboarding()` :
```swift
/// Surveillance des entrées accordée ? Requis pour que KeyInterceptor capte
/// Tab/Esc — sans ça le ghost s'affiche mais l'accept est inerte (le symptôme
/// « ghost visible, Tab ne fait rien »). Miroir de OnboardingModel.refreshPermissions.
private var inputMonitoringGranted: Bool {
    IOHIDCheckAccess(kIOHIDRequestTypeListenEvent) == kIOHIDAccessTypeGranted
}
```

**1c. Gate dans shouldShowOnboarding().** Modifier la ligne de décision (l.600-602). Remplacer :
```swift
let ghostReady = GGUFModelOption.option(forID: store.ggufModelID).isResolvable
if onboarded && AXClient.isTrusted && ghostReady { return false }
return true
```
par :
```swift
let ghostReady = GGUFModelOption.option(forID: store.ggufModelID).isResolvable
// Input Monitoring fait partie des permissions REQUISES : sans elle Tab/Esc
// sont inertes. On la teste comme AXClient.isTrusted (sinon le wizard ne se
// rouvrait jamais pour ce trou — bug : ghost muet et aucun recours).
if onboarded && AXClient.isTrusted && inputMonitoringGranted && ghostReady { return false }
return true
```

**1d. Reprise à l'étape permissions pour un utilisateur déjà onboardé.** Dans `makeOnboardingWindow()` (l.608-656), le `resumeStep` actuel relit `onboardingProgressStep`. Quand l'utilisateur a DÉJÀ complété l'onboarding mais qu'une permission requise manque, on ne veut pas le renvoyer à Bienvenue ni à une étape arbitraire : on l'ouvre directement à l'étape permissions.

Remplacer le calcul de `resumeStep` (l.616-617) :
```swift
let isFresh = ProcessInfo.processInfo.environment["SOUFFLEUSE_ONBOARDING"] == "fresh"
let resumeStep = isFresh ? 0 : UserDefaults.standard.integer(forKey: "onboardingProgressStep")
```
par :
```swift
let isFresh = ProcessInfo.processInfo.environment["SOUFFLEUSE_ONBOARDING"] == "fresh"
// Un utilisateur DÉJÀ onboardé qu'on rouvre uniquement parce qu'une permission
// requise a sauté : on l'amène droit à l'étape permissions plutôt que de lui
// refaire l'intro. Le titre de l'étape (« Ce qu'il faut autoriser ») suffit à
// contextualiser. La complétion versionnée n'est pas touchée — onFinished la
// réécrira en sortie.
let alreadyOnboarded = UserDefaults.standard.bool(forKey: "onboardingDone")
    || UserDefaults.standard.integer(forKey: "onboardingCompletedVersion") >= Self.onboardingVersion
let missingRequiredPermission = !AXClient.isTrusted || !inputMonitoringGranted
let resumeStep: Int
if isFresh {
    resumeStep = 0
} else if alreadyOnboarded && missingRequiredPermission {
    resumeStep = OnboardingStep.permissions.rawValue
} else {
    resumeStep = UserDefaults.standard.integer(forKey: "onboardingProgressStep")
}
```

Ne PAS écrire `onboardingProgressStep` ici (ce serait fait par `onProgress` dès que l'utilisateur navigue — comportement existant conservé). Ne PAS toucher `onFinished`/`onboardingCompletedVersion`.
  </action>
  <verify>
    <automated>cd Souffleuse && swift build 2>&1 | tail -20</automated>
  </verify>
  <done>Le fichier compile. `shouldShowOnboarding` retourne true si Input Monitoring manque même quand AX+ghost sont OK. `makeOnboardingWindow` ouvre à l'étape permissions (rawValue 1) quand l'utilisateur est déjà onboardé et qu'AX ou Input Monitoring manque.</done>
</task>

<task type="auto">
  <name>Task 2: OnboardingWindow — logo Bienvenue, réassurance mots de passe, toggle Lancer au démarrage</name>
  <files>Souffleuse/Sources/Souffleuse/OnboardingWindow.swift</files>
  <action>
Points (3), (4), (5) du brief — trois ajouts SwiftUI dans des vues existantes. (Le point 2 « essai réel » est la Task 3.)

**2a. Import.** Ajouter `import ServiceManagement` au bloc d'imports en tête de fichier (l.1-8), en ordre cohérent (après `import Observation`).

**(5) Logo Bienvenue — `WelcomeStepView` (l.350-373).** Au-dessus du `VStack(spacing: 10)` contenant le titre, ajouter le logo. Modifier le corps :
```swift
var body: some View {
    VStack(spacing: 28) {
        VStack(spacing: 18) {
            // Logo de l'app, coins arrondis façon icône macOS. NSApp.applicationIconImage
            // est la source fiable (suit Resources/AppIcon.icns sans dépendre de son layout interne).
            Image(nsImage: NSApp.applicationIconImage)
                .resizable()
                .interpolation(.high)
                .frame(width: 72, height: 72)
                .clipShape(RoundedRectangle(cornerRadius: 16, style: .continuous))

            VStack(spacing: 10) {
                Text("Bienvenue dans Souffleuse")
                    .font(.system(size: 22, weight: .semibold, design: .serif))
                    .multilineTextAlignment(.center)

                Text("Souffleuse vit dans votre barre de menus et souffle le mot juste là où vous écrivez. Quelques réglages, puis elle s'efface.")
                    .font(.system(size: 13, design: .serif))
                    .italic()
                    .foregroundStyle(.secondary)
                    .multilineTextAlignment(.center)
            }
        }

        Button("Commencer") { onStart() }
            .buttonStyle(SangDeBoeufButtonStyle())
            .controlSize(.large)
    }
    .frame(maxWidth: 380)
}
```
(Le seul changement structurel : envelopper logo + titres dans un `VStack(spacing: 18)`. Ne pas casser le `.frame(maxWidth: 380)` ni le bouton.)

**(4) Réassurance mots de passe — `PermissionsStepView` (l.377-437).** Dans le `VStack(alignment: .leading, spacing: 20)`, INSÉRER la ligne de réassurance ENTRE la carte « Enregistrement de l'écran » (se termine l.420) et le `DisclosureGroup` d'aide (commence l.423). Style footnote, icône lock, sang-de-bœuf neutre (pas d'alarme) :
```swift
// Réassurance factuelle : le gate isSecureField (tick l.1378) + la blocklist
// secureBundles écartent champs de mot de passe et apps bancaires en amont.
HStack(alignment: .center, spacing: 6) {
    Image(systemName: "lock.fill")
        .font(.system(size: 11))
        .foregroundStyle(.secondary)
    Text("Les champs de mot de passe ne sont jamais lus, ni les apps bancaires.")
        .font(.system(size: 12))
        .foregroundStyle(.secondary)
        .fixedSize(horizontal: false, vertical: true)
}
.padding(.top, 2)
```

**(3) Lancer au démarrage — `DoneStepView` (l.772-830).** Ajouter un `@State` et un `Toggle` décoché par défaut, câblé exactement comme PreferencesWindow.

Ajouter le state au struct (après `let onFinished: () -> Void`) :
```swift
/// L'état de l'item de login EST sa source de vérité (pas de pref stockée) :
/// on lit SMAppService.mainApp.status. Décoché par défaut — on ne pré-active
/// jamais sans consentement explicite.
@State private var launchAtLogin: Bool = SMAppService.mainApp.status == .enabled
```

Dans le `body`, INSÉRER le bloc toggle ENTRE le `if isDownloading { … }` (se termine l.820) et le `Button("Commencer à écrire")` (l.822). Centré, sobre :
```swift
VStack(spacing: 4) {
    Toggle("Lancer Souffleuse à l'ouverture du Mac", isOn: $launchAtLogin)
        .onChange(of: launchAtLogin) { _, on in
            do {
                if on { try SMAppService.mainApp.register() }
                else { try SMAppService.mainApp.unregister() }
            } catch {
                // Échec d'enregistrement (build dev hors /Applications) →
                // resync silencieux, fidèle au house-style « fallback silencieux ».
                launchAtLogin = SMAppService.mainApp.status == .enabled
            }
        }
        .toggleStyle(.checkbox)
    Text("Conseillé : la souffleuse ne souffle que si elle est ouverte.")
        .font(.system(size: 12))
        .foregroundStyle(.secondary)
        .multilineTextAlignment(.center)
        .frame(maxWidth: .infinity, alignment: .leading)
}
.frame(maxWidth: 300)
```
  </action>
  <verify>
    <automated>cd Souffleuse && swift build 2>&1 | tail -20</automated>
  </verify>
  <done>Le fichier compile. WelcomeStepView affiche le logo 72×72 arrondi au-dessus du titre. PermissionsStepView affiche la ligne lock+texte sous les cartes. DoneStepView affiche un checkbox « Lancer Souffleuse à l'ouverture du Mac » décoché par défaut, câblé sur SMAppService.</done>
</task>

<task type="auto">
  <name>Task 3: Essai réel du souffle dans « Comment ça marche » (avec repli statique)</name>
  <files>Souffleuse/Sources/Souffleuse/OnboardingWindow.swift, Souffleuse/Sources/Souffleuse/SouffleuseAppDelegate.swift</files>
  <action>
Point (2) du brief — la partie risquée. Remplacer la maquette statique de `HowItWorksStepView` par un VRAI champ texte AppKit où le pipeline normal (tick → AX → predictor → overlay → Tab) peut souffler. Repli sur la maquette si la voix n'est pas prête. NE PAS gater le bouton Continuer sur l'apparition du ghost.

**3a. Câbler `canTryGhost` depuis l'AppDelegate (les deux fichiers).**

Dans OnboardingWindow.swift, `OnboardingWindow.init` : AJOUTER un paramètre `canTryGhost: @escaping () -> Bool` (après `ghostReady`, avant `translation` pour rester près des autres closures liées au souffle). Le stocker n'est pas nécessaire au-delà de le passer à `OnboardingRootView`. Donc :
- Ajouter le param à la signature `init`.
- Dans la construction de `OnboardingRootView` (l.894-905), passer `canTryGhost: canTryGhost`.

Dans `OnboardingRootView` (struct, l.111-122) : AJOUTER `let canTryGhost: () -> Bool` aux propriétés. Dans `stepContent` case `.howItWorks` (l.216-217), passer la closure :
```swift
case .howItWorks:
    HowItWorksStepView(canTryGhost: canTryGhost)
```

Dans SouffleuseAppDelegate.swift `makeOnboardingWindow()` : passer la closure au constructeur. `predictor.isModelReady` (@MainActor) est le booléen « voix prête » :
```swift
canTryGhost: { [predictor] in predictor.isModelReady },
```
La placer dans l'appel `OnboardingWindow(...)` juste après `ghostReady:` (cohérent avec l'ordre des params).

**3b. Champ texte réel via NSViewRepresentable.** Ajouter en haut de OnboardingWindow.swift (après les imports, ou à côté de HowItWorksStepView) une représentation AppKit d'un `NSTextField` multi-usage. Un `NSTextField` ordinaire expose caret + bounds AX de façon fiable (contrairement à TextField SwiftUI) :
```swift
// MARK: - Ghost try field (NSTextField AppKit)

/// Champ texte AppKit pour l'étape « Comment ça marche » : un vrai champ où le
/// pipeline normal (tick → AX → predictor → overlay → Tab) souffle pour de vrai.
/// On passe par AppKit et NON par un TextField SwiftUI car l'AX d'un TextField
/// SwiftUI n'expose pas toujours caret/bounds de façon fiable pour AXClient.
private struct GhostTryField: NSViewRepresentable {
    let placeholderSeed: String

    func makeNSView(context: Context) -> NSTextField {
        let field = NSTextField(string: placeholderSeed)
        field.font = .systemFont(ofSize: 15)
        field.isEditable = true
        field.isSelectable = true
        field.isBordered = true
        field.bezelStyle = .roundedBezel
        field.focusRingType = .default
        field.lineBreakMode = .byTruncatingTail
        // Auto-focus + caret en fin de seed pour que la frappe continue la phrase.
        DispatchQueue.main.async {
            guard let win = field.window else { return }
            win.makeFirstResponder(field)
            field.currentEditor()?.selectedRange = NSRange(location: placeholderSeed.count, length: 0)
        }
        return field
    }

    func updateNSView(_ nsView: NSTextField, context: Context) {}
}
```
(Pas de binding nécessaire : on ne lit pas le texte côté SwiftUI — c'est le tick() AX global qui le voit. Garder le struct `private` et minimal.)

**3c. Réécrire `HowItWorksStepView` (l.724-752) avec essai réel + fallback.**
```swift
private struct HowItWorksStepView: View {
    let canTryGhost: () -> Bool

    private let seed = "Bonjour, je voulais vous dire que "

    var body: some View {
        VStack(alignment: .leading, spacing: 24) {
            Text("Comment ça marche")
                .font(.system(size: 22, weight: .semibold, design: .serif))

            if canTryGhost() {
                // Essai RÉEL : un vrai champ où le souffle apparaît au caret.
                VStack(alignment: .leading, spacing: 8) {
                    Text("Essayez : écrivez quelques mots. Quand le mot gris apparaît, appuyez sur Tab pour l'accepter.")
                        .font(.system(size: 13, design: .serif))
                        .italic()
                        .foregroundStyle(.secondary)
                        .fixedSize(horizontal: false, vertical: true)
                    GhostTryField(placeholderSeed: seed)
                        .frame(height: 30)
                }
            } else {
                // Repli : la voix n'est pas encore prête → maquette statique
                // (le souffle ne viendrait pas, autant ne pas frustrer).
                HStack(spacing: 0) {
                    Text("Bonjour, je vous ")
                        .font(.system(size: 15))
                    + Text("écris ce mot")
                        .font(.system(size: 15))
                        .foregroundStyle(.tertiary)
                }
                .padding(12)
                .background(
                    RoundedRectangle(cornerRadius: 8, style: .continuous)
                        .fill(Color(nsColor: .textBackgroundColor))
                        .shadow(color: .black.opacity(0.06), radius: 3, y: 1)
                )
            }

            VStack(alignment: .leading, spacing: 12) {
                HowBullet("Le mot juste apparaît en gris : appuyez sur **Tab** pour l'accepter.")
                HowBullet("Il ne vous va pas ? **Esc**, ou continuez d'écrire : il s'efface.")
                HowBullet("Souffleuse reste dans la barre de menus, en haut à droite.")
            }
        }
    }
}
```
Garder `HowBullet` inchangé. Ne PAS gater le bouton Continuer (le footer `.howItWorks` reste `canContinue: true` — déjà le cas l.248-253, ne pas y toucher).

**3d. Diagnostic (pour l'orchestrateur, pas un test automatisé).** Documenter dans le SUMMARY : si l'essai réel ne souffle pas en live, lancer l'app avec `SOUFFLEUSE_PREDICT_LOG=/tmp` et inspecter `/tmp/souffleuse-tick.log` pour un `tick_gate_fail` (raison : no_caret / not_text_element / secure_field…) ou vérifier que l'overlay NSPanel s'affiche au-dessus de la fenêtre d'onboarding. NE PAS tenter de test interactif ici — l'orchestrateur le fera en live.

NE PAS écrire de test interactif. Ne pas ajouter de `@Test` qui dépend d'un rendu de fenêtre.
  </action>
  <verify>
    <automated>cd Souffleuse && swift build 2>&1 | tail -20</automated>
  </verify>
  <done>Les deux fichiers compilent. `HowItWorksStepView` affiche un `GhostTryField` (NSTextField pré-rempli, auto-focus) quand `canTryGhost()` est vrai, et la maquette statique sinon. `makeOnboardingWindow` passe `canTryGhost: { [predictor] in predictor.isModelReady }`. Le bouton Continuer reste actif sans condition.</done>
</task>

</tasks>

<verification>
Build et suite de tests, plus l'audit privacy :

```bash
cd Souffleuse && swift build 2>&1 | tail -20
cd Souffleuse && swift test 2>&1 | tail -25
cd Souffleuse && ./audit.sh
```

Attendu :
- Build OK (les 3 tâches compilent).
- Suite verte (~640/890 `@Test` selon le compteur courant — AUCUNE régression ; aucun test n'est censé changer de statut).
- `audit.sh` vert : aucun `print`/`NSLog` ajouté, aucun champ user loggé. Le `GhostTryField` n'introduit pas de log ; les commentaires diagnostic référencent `SOUFFLEUSE_PREDICT_LOG` (déjà exclu de l'audit) mais n'ajoutent PAS de nouveau log.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onboarding UI → permissions système | Les boutons « Autoriser » / SMAppService déclenchent des prompts TCC/login-item ; pas d'entrée user non-fiable traversée. |
| GhostTryField → pipeline AX global | Le texte tapé dans le champ d'essai traverse le tick() AX comme n'importe quel champ ; soumis aux mêmes gates (isSecureField, blocklist). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ij2-01 | Information disclosure | GhostTryField (essai réel) | accept | Champ de notre propre fenêtre non-secure ; le texte d'essai (« Bonjour… ») n'est pas sensible. Soumis aux mêmes gates AX que tout champ. Pas d'enregistrement historique : notre bundle n'est pas la cible d'un accept réel et `personalizationBundleBlocklist` n'a pas à le couvrir car le seed est neutre. |
| T-ij2-02 | Elevation of privilege | SMAppService toggle (login item) | mitigate | Décoché par défaut, action explicite de l'utilisateur ; `try register()` peut échouer (build dev) → resync silencieux sans crash. Pattern identique à PreferencesWindow déjà en prod. |
| T-ij2-03 | Information disclosure | inputMonitoringGranted / IOHIDCheckAccess | accept | Lecture d'état de permission read-only, aucune donnée user. Miroir exact de OnboardingModel.refreshPermissions déjà présent. |
</threat_model>

<success_criteria>
- [ ] `shouldShowOnboarding()` re-montre le wizard quand Input Monitoring manque (même si AX + ghost OK).
- [ ] Pour un utilisateur déjà onboardé à qui manque AX ou Input Monitoring, `makeOnboardingWindow` ouvre à `OnboardingStep.permissions.rawValue`.
- [ ] WelcomeStepView affiche le logo de l'app (72×72, arrondi) au-dessus du titre.
- [ ] PermissionsStepView affiche la ligne lock + « Les champs de mot de passe ne sont jamais lus, ni les apps bancaires. » sous les cartes.
- [ ] DoneStepView affiche un checkbox « Lancer Souffleuse à l'ouverture du Mac » décoché par défaut, câblé sur SMAppService avec fallback silencieux.
- [ ] HowItWorksStepView montre un vrai champ (`GhostTryField`) pré-rempli + auto-focus quand `canTryGhost()` est vrai, sinon la maquette statique. Bouton Continuer jamais gaté dessus.
- [ ] `swift build` OK, `swift test` sans régression, `audit.sh` vert.
- [ ] Reprise d'étape versionnée (260612-gto) non cassée : `onboardingProgressStep` / `onboardingCompletedVersion` / `onFinished` intacts.
</success_criteria>

<output>
Après complétion, créer `.planning/quick/260612-ij2-compl-ter-l-onboarding-rappel-permission/260612-ij2-SUMMARY.md`.

Inclure dans le SUMMARY la note diagnostic de la Task 3 (point 3d) : comment vérifier l'essai réel en live (`SOUFFLEUSE_PREDICT_LOG=/tmp`, `/tmp/souffleuse-tick.log`, overlay au-dessus de la fenêtre d'onboarding) — l'orchestrateur testera l'essai réel et le toggle login-item manuellement.
</output>

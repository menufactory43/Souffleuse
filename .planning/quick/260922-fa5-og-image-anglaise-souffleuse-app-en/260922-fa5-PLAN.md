---
phase: quick-260922-fa5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/assets/og/og-en.html
  - website/og-en.png
  - website/en/index.html
  - website/en/livret-v2.html
  - website/en/privacy.html
  - website/en/license.html
  - website/en/legal-notice.html
  - website/en/guides/mac-predictive-text-like-iphone.html
  - website/en/guides/local-llm-writing-assistant.html
  - website/en/guides/autocomplete-every-mac-app.html
  - website/en/guides/translate-while-typing-mac.html
  - website/en/guides/copilot-for-writing.html
  - website/en/guides/offline-ai-writing-mac.html
  - website/en/guides/professional-email-tone-mac.html
  - website/en/compare/cotypist-alternatives.html
autonomous: true
requirements: [QUICK-OG-EN]

must_haves:
  truths:
    - "https://souffleuse.app/og-en.png returns 200 image/png, 1200x630, same design as og.png with English text"
    - "Every website/en/**/*.html page that declares og:image points to https://souffleuse.app/og-en.png (and twitter:image too where declared)"
    - "French pages (website/*.html, website/guides/*.html) still reference og.png, untouched"
    - "The HTML template that produced og-en.png is committed and can regenerate it"
    - "No unrelated working-tree change (app sources, Info.plist, licensed.py, appcast, other website diffs) is staged or committed"
  artifacts:
    - path: "website/og-en.png"
      provides: "English Open Graph card, 1200x630, < 300 KB"
    - path: "website/assets/og/og-en.html"
      provides: "Regenerable 1200x630 template (uses ../../og.png as frame+silhouette base, site fonts)"
      contains: "whispered from the wings"
  key_links:
    - from: "website/en/index.html"
      to: "https://souffleuse.app/og-en.png"
      via: "og:image + twitter:image meta"
      pattern: "og-en\\.png"
---

<objective>
Produce an English Open Graph card `website/og-en.png`, visually identical to `website/og.png` except for the English copy, keep a regenerable HTML template in the repo, point every English page's og:image / twitter:image to it, commit ONLY those files, deploy to Vercel, and verify live.

Purpose: English shares of souffleuse.app/en/* currently show a French card (og.png) or a French portrait promo JPG.
Output: website/og-en.png, website/assets/og/og-en.html, updated website/en/**/*.html meta, live deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md  (section "Déployer le site (recette rapide)")

## Findings from planning (no need to re-explore)

- **No generator exists for og.png.** It was added as a binary in commit dc59cca ("Partage : Open Graph + Twitter Card + og.png"); nothing in website/remotion, website/promo or .planning references a template. Recreate via Chrome headless as instructed.
- **og.png layout** (1200x630, RGB, 135 KB): cream background, double thin ink frame (outer ~x24..1176 / y24..606, inner ~x30..1170 / y30..600), black silhouette of the souffleuse blowing a kiss on the left (x ~80..450, y ~80..580). Right-hand text block left-aligned at x≈541:
  - y≈165 small caps in rouge `#8c2b21`, serif (Spectral), letter-spaced: "AIDE À L'ÉCRITURE POUR MAC"
  - y≈230..380 "Souffleuse" huge Bodoni Moda italic (~150px), ink `#1a1613`
  - y≈376 short rouge rule ~150px wide (x 545..692), 2px
  - y≈410..500 two lines Spectral italic ~40px, ink-soft `#463d33`: "Le mot juste," / "soufflé à voix basse."
  - y≈548 one line Spectral italic ~32px, ink `#1a1613`: "100% sur votre Mac. Rien ne part."
- **Site palette** (website/index.html :root): --paper #f3ead9, --ink #1a1613, --ink-soft #463d33, --rouge #8c2b21. Sample the actual og.png background pixel with PIL (it may differ slightly from --paper) and use THAT value.
- **Fonts** (self-hosted, website/assets/fonts/): bodoni-moda-500-italic-latin.woff2, spectral-400-italic-latin.woff2, spectral-400-normal-latin.woff2, spectral-500-normal-latin.woff2 (+ latin-ext variants).
- **Silhouette asset**: website/assets/souffleuse-mascotte.webp (750x1008, transparent) exists but may not be the exact same pose/crop as og.png.
- **Tools available**: Chrome at "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome", python3 + PIL, pngquant, oxipng. Nothing currently staged in git index.
- **EN pages declaring og:image** (13):
  - og.png (og:image): en/index.html, en/livret-v2.html (both also have og:image:width/height/alt + twitter:image = og.png), en/privacy.html, en/license.html, en/legal-notice.html (og:image only, no alt/size).
  - French portrait promo JPGs (1080x1350, French captions, e.g. "Elle joue dans toutes les salles.") at line 19 + twitter:card summary_large_image at line 20, no twitter:image, no alt: en/guides/{mac-predictive-text-like-iphone, local-llm-writing-assistant, autocomplete-every-mac-app, translate-while-typing-mac, copilot-for-writing, offline-ai-writing-mac, professional-email-tone-mac}.html, en/compare/cotypist-alternatives.html.
- **CRITICAL: 11 of those 13 EN files already carry UNRELATED uncommitted edits** (vercel insights script, copy changes...). Only en/index.html and en/livret-v2.html are clean. The og meta lines are NOT part of those pending diffs (verified: `git diff -U0 -- website/en | grep og:image` is empty), so the meta edit can be staged surgically from the HEAD blob (see Task 2). `git add -p` is interactive and forbidden.
- **Deploy caveat**: `vercel --prod` uploads the working tree from disk, so the unrelated uncommitted website changes (appcast.xml, deleted guides/alternative-cotypist-gratuite.html, new guides/alternative-cotypist-sans-abonnement.html, sitemap.xml, insights script, etc.) would ship too. Task 3 checks whether they are already live before deploying.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the regenerable template and render website/og-en.png</name>
  <files>website/assets/og/og-en.html, website/og-en.png</files>
  <action>
Method (discretion, chosen for maximum fidelity): the template uses `../../og.png` itself as a full-bleed 1200x630 background (guarantees identical frame + silhouette), then paints a solid rectangle in the exact sampled background color over the French text block ONLY (approx. x 520..1160, y 130..590 — must stay strictly inside the inner frame line at ~x1170/y600 and right of the silhouette's hand at ~x450; confirm the bounds by inspecting og.png pixels with PIL: find the bbox of non-background pixels for x>=500 inside the inner frame), then lays out the English text with absolute positioning matching the measurements in context. If the rectangle cannot cleanly cover the French text without touching the frame or silhouette, fall back to redrawing the frame in CSS (two 1px #1a1613 borders) and placing website/assets/souffleuse-mascotte.webp at the same position/size as the og.png silhouette.

1. `mkdir -p website/assets/og`. Sample the background color: `python3 -c "from PIL import Image; im=Image.open('website/og.png').convert('RGB'); print(im.getpixel((700,100)), im.getpixel((1000,590)))"`.
2. Write website/assets/og/og-en.html: `<html>` with body margin 0, a 1200x630 `position:relative; overflow:hidden` stage; `@font-face` declarations pointing to `../fonts/bodoni-moda-500-italic-latin.woff2` (Bodoni Moda italic 500), `../fonts/spectral-400-italic-latin.woff2`, `../fonts/spectral-400-normal-latin.woff2`, `../fonts/spectral-500-normal-latin.woff2` (font-display: block). Text, left-aligned at x≈541, same sizes/colors/vertical positions as the French card:
   - kicker: "WRITING AID FOR MAC" (Spectral, rouge #8c2b21, uppercase, same size/letter-spacing as FR)
   - title: "Souffleuse" (Bodoni Moda italic 500, ink, same size and baseline as FR — the word is identical, so it should overlay the original pixel-for-pixel; tune until it does)
   - rouge rule under the name, same position/length
   - tagline on two lines (explicit `<br>`): "The right word," / "whispered from the wings." (Spectral italic, ink-soft #463d33). This line is longer than "soufflé à voix basse." — it must fit before the inner frame with ≥ 40px margin; reduce font-size only as much as needed, keep line-height consistent.
   - footer: "100% on your Mac. Nothing leaves." (Spectral italic, ink).
   Add an HTML comment at the top with the exact regeneration command (step 3) and that og.png is the base layer.
3. Render (from repo root, device scale 1 is mandatory on Retina): `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu --force-device-scale-factor=1 --hide-scrollbars --window-size=1200,630 --allow-file-access-from-files --virtual-time-budget=3000 --screenshot="$PWD/website/og-en.png" "file://$PWD/website/assets/og/og-en.html"`. (If `--headless` produces a wrong size, try `--headless=new`.) Only one Chrome process, close it afterwards (8 GB RAM).
4. Check `sips -g pixelWidth -g pixelHeight website/og-en.png` = 1200x630. Optimize: `pngquant --quality=80-98 --skip-if-larger --force --output website/og-en.png website/og-en.png` then `oxipng -o 3 --strip safe website/og-en.png`. Target < 300 KB.
5. MANDATORY visual check: Read website/og.png and website/og-en.png with the Read tool and compare side by side (also build a stacked comparison in the scratchpad with PIL if useful). Verify: frame lines intact on all four sides, silhouette identical, no residual French glyphs, fonts actually loaded (Bodoni italic "Souffleuse" with the ff ligature look, NOT a Times fallback), text alignment/weights/colors match, tagline on two lines, nothing clipped. Iterate on the template until it matches.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/cocotypist-llama && sips -g pixelWidth -g pixelHeight website/og-en.png | grep -E "pixelWidth: 1200|pixelHeight: 630" | wc -l | grep -q 2 && [ $(stat -f%z website/og-en.png) -lt 307200 ] && grep -q "whispered from the wings" website/assets/og/og-en.html && echo OK</automated>
  </verify>
  <done>website/og-en.png is 1200x630, under 300 KB, and matches og.png's design with the English copy after a side-by-side visual check. website/assets/og/og-en.html regenerates it with the documented command.</done>
</task>

<task type="auto">
  <name>Task 2: Point all EN pages to og-en.png and stage surgically (never stage unrelated edits)</name>
  <files>website/en/index.html, website/en/livret-v2.html, website/en/privacy.html, website/en/license.html, website/en/legal-notice.html, website/en/guides/*.html (7 files), website/en/compare/cotypist-alternatives.html</files>
  <action>
Meta rules, applied the same way to the working-tree file AND the staged blob:
  a. Replace `https://souffleuse.app/og.png` with `https://souffleuse.app/og-en.png` everywhere in the 13 EN files. These are only og:image and twitter:image lines; check with grep first.
  b. The user asked for ALL EN pages that declare og:image. In the 8 guide/compare pages, replace the `og:image` content `https://souffleuse.app/promo/souffleuse-en-action/*.jpg` with `https://souffleuse.app/og-en.png`. Reason: those promos are French-captioned portrait images, which crop badly in summary_large_image. Mention this in the SUMMARY.
  c. Check og:image:alt. index.html and livret-v2.html already have "Souffleuse: the right word, whispered from the wings." Keep it. The other 11 pages have no alt and no size. Right after their og:image line, insert these 3 lines: `<meta property="og:image:width" content="1200" />`, `<meta property="og:image:height" content="630" />`, `<meta property="og:image:alt" content="Souffleuse: the right word, whispered from the wings." />`. Do not add twitter:image where it is missing, because Twitter falls back to og:image.
  d. Leave FR pages alone: website/*.html, website/guides/*.html, and website/livret-v2.html all keep og.png.

Implement this as ONE python3 script in the scratchpad. The script holds a `transform(text) -> text` function that applies a–c idempotently. For each of the 13 files it:
  1. Reads the working-tree file, applies transform, and writes it back. This keeps the unrelated pending edits in the working tree.
  2. Takes the HEAD version via `git show HEAD:<path>` and applies the same transform. It writes the result to a scratch file, runs `git hash-object -w <tmp>`, then runs `git update-index --cacheinfo 100644,<sha>,<path>`. The index then holds HEAD plus ONLY the meta change, and the unrelated working-tree edits stay unstaged.
  3. Asserts that `git diff --cached -- <path>` has only og:image / twitter:image / og:image:width|height|alt lines, and that `git diff -- <path>` (working tree vs index) no longer has any og lines.
Then stage the new assets with a normal add: `git add website/og-en.png website/assets/og/og-en.html`.
Final gate: `git diff --cached --name-only` must list exactly those 15 paths (13 EN html + og-en.png + og-en.html). If anything else is staged (Souffleuse/*, deploy/*, tools/*, website/appcast.xml, FR pages, remotion, sitemap), unstage it with `git restore --staged <path>`. Never run `git add -A`, `git add .`, or `git commit -a`.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/cocotypist-llama && ! grep -rl 'souffleuse.app/og.png\|promo/souffleuse-en-action' website/en && [ $(grep -rl 'og:image" content="https://souffleuse.app/og-en.png' website/en | wc -l) -eq 13 ] && [ $(git diff --cached --name-only | wc -l) -eq 15 ] && ! git diff --cached --name-only | grep -vE '^website/(en/|og-en\.png|assets/og/og-en\.html)' && ! git diff --cached | grep '^[+-][^+-]' | grep -v 'og:image\|twitter:image\|Binary\|og-en' | grep -q . && grep -q 'souffleuse.app/og.png' website/index.html && echo OK</automated>
  </verify>
  <done>All 13 EN pages reference og-en.png in og:image, and in twitter:image where declared. Every EN page has og:image width, height and an English alt. FR pages are unchanged. The index holds exactly the 15 intended paths, with only meta-line changes in the HTML.</done>
</task>

<task type="auto">
  <name>Task 3: Commit, deploy to Vercel, verify live</name>
  <files>(git commit, Vercel production deploy)</files>
  <action>
1. Commit only what is in the index. Do NOT pass `-a` or pathspecs that would re-add working-tree changes. Message: `feat(site): og-en.png, carte Open Graph anglaise pour /en/*`. The body lists the template path, the regeneration command, and the swap from promo JPG to og-en.png on the EN guides. End it with the Co-Authored-By trailer from the session. Then check `git show --stat HEAD` shows exactly the 15 files, and that `git status --short` still shows the unrelated modifications (Info.plist, LicenseStore.swift, PreferencesStore.swift, LicenseStoreTests.swift, licensed.py, appcast.xml, ...) as unstaged.
2. Pre-deploy safety check. Vercel uploads the DISK tree, so the unrelated uncommitted website changes will also ship. Check whether they are already live: `curl -s "https://souffleuse.app/appcast.xml?cb=$(date +%s)" | diff -q - website/appcast.xml`, `curl -s "https://souffleuse.app/en/privacy.html?cb=$(date +%s)" | grep -c _vercel/insights`, and `curl -s -o /dev/null -w '%{http_code}' https://souffleuse.app/guides/alternative-cotypist-sans-abonnement.html`. If they are already live (appcast identical, insights present, new guide 200), deploy. If some differ, STOP before deploying and return a checkpoint to the user. List the unrelated website changes the deploy would publish, and ask whether to deploy anyway. Do not stash or revert their changes.
3. Deploy: from website/, run `npx --yes vercel@latest --prod --yes` (already linked, auth cached, aliases to souffleuse.app).
4. Verify live, using cache-busters:
   - `curl -s "https://souffleuse.app/en/?cb=$(date +%s)" | grep 'og:image'` must show og-en.png, and `grep twitter:image` too.
   - `curl -sI https://souffleuse.app/og-en.png` must return 200 with content-type image/png.
   - Download it to the scratchpad, check it is 1200x630, and check its md5 equals website/og-en.png.
   - One EN guide, e.g. `https://souffleuse.app/en/guides/offline-ai-writing-mac` (try the `.html` path if cleanUrls is off), must show og-en.png.
   - FR home: `curl -s "https://souffleuse.app/?cb=$(date +%s)" | grep og:image` must still show og.png.
   - CLAUDE.md sanity check: `curl -s "https://souffleuse.app/?cb=$(date +%s)" | grep -c data-version` is 3.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/cocotypist-llama && curl -s "https://souffleuse.app/en/?cb=$(date +%s)" | grep 'og:image"' | grep -q og-en.png && curl -sI https://souffleuse.app/og-en.png | grep -qiE '^content-type: image/png' && curl -sI https://souffleuse.app/og-en.png | head -1 | grep -q 200 && curl -s "https://souffleuse.app/?cb=$(date +%s)" | grep 'og:image"' | grep -q '/og.png' && echo LIVE-OK</automated>
  </verify>
  <done>The commit contains exactly the 15 intended files, and the unrelated changes remain unstaged in the working tree. Production serves og-en.png (200, image/png, identical bytes). /en/ and the EN guides reference it. The FR home still references og.png.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree -> public production site | vercel --prod publishes whatever is on disk, including uncommitted, unrelated changes |
| local git index -> shared repo history | a careless add would commit unrelated app/licensing work in progress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fa5-01 | Tampering | git index / commit | mitigate | Surgical blob staging via hash-object + update-index. Final gate: `git diff --cached --name-only` lists exactly 15 paths. No `-a`, `add .` or `add -p`. |
| T-fa5-02 | Information disclosure | Vercel deploy of disk tree | mitigate | Before deploying, diff the live site against the disk (appcast, insights, new guide). If unpublished unrelated changes would ship, stop and ask the user. |
| T-fa5-03 | Tampering | licensed.py / LicenseStore pending edits | mitigate | These are never staged. Task 3 step 1 checks they are still unstaged after the commit. |
| T-fa5-04 | Spoofing | og image content | accept | Static first-party image with no user data. |
</threat_model>

<verification>
- Side-by-side visual check of og.png and og-en.png is done and recorded in the SUMMARY.
- `git show --stat HEAD` lists 15 files. `git status` still shows the unrelated modifications.
- Live checks from Task 3 all pass.
</verification>

<success_criteria>
- website/og-en.png: 1200x630, under 300 KB, same design as og.png, English copy "WRITING AID FOR MAC" / "The right word," + "whispered from the wings." / "100% on your Mac. Nothing leaves."
- website/assets/og/og-en.html is committed with a regeneration command.
- All 13 EN pages that declare og:image point to og-en.png. FR pages keep og.png.
- Deployed and verified live: /en/ og:image = og-en.png, and /og-en.png returns 200 image/png.
</success_criteria>

<output>
After completion, create `.planning/quick/260922-fa5-og-image-anglaise-souffleuse-app-en/260922-fa5-SUMMARY.md`
</output>

